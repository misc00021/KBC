Here is the input problem:
  Axiom 1 (add_comm): add(X, Y) = add(Y, X).
  Axiom 2 (add_zero): add(X, zero) = X.
  Axiom 3 (mul_comm): mul(X, Y) = mul(Y, X).
  Axiom 4 (mul_one): mul(X, one) = X.
  Axiom 5 (mul_zero): mul(X, zero) = zero.
  Axiom 6 (add_inv): add(X, neg(X)) = zero.
  Axiom 7 (add_assoc): add(X, add(Y, Z)) = add(add(X, Y), Z).
  Axiom 8 (mul_assoc): mul(X, mul(Y, Z)) = mul(mul(X, Y), Z).
  Axiom 9 (distrib_left): mul(X, add(Y, Z)) = add(mul(X, Y), mul(X, Z)).
  Axiom 10 (distrib_right): mul(add(X, Y), Z) = add(mul(X, Z), mul(Y, Z)).
  Goal 1 (goal): true = false.

1. add(X, Y) <-> add(Y, X)
2. add(X, zero) -> X
3. mul(X, Y) <-> mul(Y, X)
4. mul(X, one) -> X
5. mul(X, zero) -> zero
6. add(X, neg(X)) -> zero
7. add(add(X, Y), Z) -> add(X, add(Y, Z))
8. mul(mul(X, Y), Z) -> mul(X, mul(Y, Z))
9. add(mul(X, Y), mul(X, Z)) -> mul(X, add(Y, Z))
10. add(mul(X, Y), mul(Z, Y)) -> mul(add(X, Z), Y)
11. add(zero, X) -> X
12. neg(zero) -> zero
13. mul(one, X) -> X
14. mul(zero, X) -> zero
15. add(X, add(Y, Z)) <-> add(Y, add(X, Z))
16. mul(X, mul(Y, Z)) <-> mul(Y, mul(X, Z))
17. mul(X, add(Y, Y)) = mul(Y, add(X, X))
18. mul(X, add(Y, one)) = add(X, mul(X, Y))
19. mul(X, add(one, one)) = add(X, X)
20. add(X, add(Y, neg(X))) -> Y
21. mul(X, add(Y, Y)) = mul(add(X, X), Y)
22. add(X, add(neg(X), Y)) -> Y
23. neg(neg(X)) -> X
24. add(X, mul(X, Y)) = mul(X, add(one, Y))
25. add(X, mul(X, Y)) = mul(add(Y, one), X)
26. mul(X, add(Y, one)) = add(X, mul(Y, X))
27. add(X, X) = mul(add(one, one), X)
28. mul(add(one, X), Y) = add(Y, mul(Y, X))
29. mul(X, add(one, Y)) = add(X, mul(Y, X))
30. mul(add(X, one), Y) = add(Y, mul(X, Y))
  (hard) mul(Y, add(one, X)) = mul(add(X, one), Y)
31. add(X, mul(X, neg(one))) -> zero
32. mul(neg(X), neg(one)) -> X
33. mul(X, neg(one)) -> neg(X)
34. mul(neg(one), X) -> neg(X)
35. mul(X, neg(Y)) -> neg(mul(X, Y))
36. add(mul(X, Y), mul(Y, Z)) -> mul(Y, add(X, Z))
37. mul(add(one, X), Y) = add(Y, mul(X, Y))
  (hard) mul(add(one, X), Y) = mul(Y, add(X, one))
38. add(X, add(Y, neg(add(X, Y)))) -> zero
39. add(neg(one), neg(one)) -> neg(add(one, one))
40. neg(add(X, neg(Y))) -> add(Y, neg(X))
41. mul(neg(X), Y) -> neg(mul(X, Y))
42. add(neg(X), neg(Y)) -> neg(add(X, Y))
43. neg(add(neg(X), Y)) -> add(X, neg(Y))
44. mul(X, mul(Y, add(Z, Z))) = mul(Y, mul(Z, add(X, X)))
45. mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(add(X, X), Z))
46. add(X, add(Y, add(Z, neg(X)))) -> add(Y, Z)
47. add(X, add(Y, mul(X, Z))) = add(mul(X, add(Z, one)), Y)
48. add(X, mul(X, add(Y, Z))) = mul(X, add(Y, add(Z, one)))
49. mul(add(X, mul(X, Y)), Z) -> mul(X, mul(add(Y, one), Z))
50. add(X, add(Y, mul(X, Z))) = add(Y, mul(X, add(Z, one)))
51. mul(X, mul(Y, add(Z, one))) = mul(Y, add(X, mul(X, Z)))
52. mul(X, add(one, mul(Y, Z))) = add(X, mul(Y, mul(X, Z)))
53. mul(X, add(Y, add(Y, one))) = add(X, mul(Y, add(X, X)))
  (hard) add(X, add(X, one)) = add(one, add(X, X))
54. mul(X, add(Y, add(one, one))) = add(X, add(X, mul(X, Y)))
55. add(X, add(X, Y)) = add(mul(X, add(one, one)), Y)
56. mul(X, mul(add(one, one), Y)) = mul(add(X, X), Y)
57. add(X, add(Y, X)) = add(Y, mul(X, add(one, one)))
58. mul(X, mul(Y, add(one, one))) = mul(Y, add(X, X))
59. mul(add(X, X), Y) = mul(X, mul(Y, add(one, one)))
60. mul(X, add(one, add(one, one))) = add(X, add(X, X))
61. mul(add(one, add(one, one)), X) = add(X, add(X, X))
62. mul(X, mul(add(Y, Y), Z)) = mul(add(X, X), mul(Y, Z))
63. mul(X, mul(add(Y, Y), Z)) = mul(Z, mul(X, add(Y, Y)))
64. mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(X, add(Z, Z)))
65. mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(Z, add(X, X)))
66. mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(Y, add(X, X)))
67. mul(X, mul(add(Y, Y), Z)) = mul(add(X, X), mul(Z, Y))
  (hard) add(X, add(Y, mul(X, Z))) = add(X, add(mul(Z, X), Y))
68. mul(X, add(Y, mul(Y, Z))) = mul(Y, add(X, mul(X, Z)))
69. mul(X, add(Y, mul(Y, Z))) = mul(Y, add(X, mul(Z, X)))
70. add(X, add(Y, add(neg(X), Z))) -> add(Y, Z)
71. add(X, neg(add(X, X))) -> neg(X)
72. mul(X, add(Y, add(Y, one))) = add(X, mul(add(X, X), Y))
73. add(X, add(Y, mul(X, Z))) = add(mul(X, add(one, Z)), Y)
74. add(X, add(Y, mul(X, Z))) = add(Y, mul(X, add(one, Z)))
75. add(X, mul(X, add(Y, Z))) = mul(X, add(Y, add(one, Z)))
76. mul(X, mul(Y, add(one, Z))) = mul(Y, add(X, mul(X, Z)))
77. mul(X, add(one, add(Y, Y))) = add(X, mul(Y, add(X, X)))
78. mul(X, add(one, add(Y, Y))) = add(X, mul(add(X, X), Y))
79. add(X, add(Y, mul(X, Z))) = add(mul(add(Z, one), X), Y)
80. add(X, mul(X, add(Y, Z))) = mul(add(Y, add(Z, one)), X)
81. add(X, add(Y, mul(X, Z))) = add(Y, mul(add(Z, one), X))
82. mul(add(one, mul(X, Y)), Z) = add(Z, mul(X, mul(Z, Y)))
83. mul(add(X, one), mul(Y, Z)) = mul(Y, add(Z, mul(Z, X)))
84. mul(add(X, add(X, one)), Y) = add(Y, mul(X, add(Y, Y)))
85. add(X, mul(add(Y, Z), X)) = mul(X, add(Y, add(Z, one)))
86. mul(add(X, mul(Y, X)), Z) -> mul(X, mul(add(Y, one), Z))
87. add(X, add(Y, mul(Z, X))) = add(Y, mul(X, add(Z, one)))
88. add(X, neg(add(X, one))) -> neg(one)
89. add(X, neg(add(one, X))) -> neg(one)
90. mul(X, mul(Y, add(Z, one))) = mul(Y, add(X, mul(Z, X)))
91. add(mul(X, add(Y, one)), Z) = add(X, add(mul(X, Y), Z))
92. add(mul(X, add(Y, one)), Z) = add(X, add(Z, mul(Y, X)))
  (hard) add(X, mul(X, add(Y, Z))) = mul(X, add(Z, add(one, Y)))
93. mul(X, mul(add(Y, one), Z)) = mul(Z, add(X, mul(X, Y)))
94. add(X, mul(Y, add(Z, one))) = add(Y, add(mul(Y, Z), X))
95. add(X, add(X, Y)) <-> add(Y, add(X, X))
  (hard) mul(X, add(one, mul(Y, Z))) = add(X, mul(X, mul(Z, Y)))
96. add(X, mul(Y, mul(X, Z))) = mul(X, add(one, mul(Z, Y)))
97. mul(X, add(one, mul(Y, Z))) = add(X, mul(Y, mul(Z, X)))
98. add(X, mul(Y, add(X, one))) <-> add(Y, mul(X, add(Y, one)))
  (hard) mul(X, add(one, add(Y, Z))) = mul(X, add(Y, add(Z, one)))
99. mul(add(X, mul(X, Y)), Z) -> mul(X, mul(Z, add(Y, one)))
  (hard) mul(X, mul(Y, add(Z, one))) = mul(Y, mul(X, add(one, Z)))
100. mul(X, mul(Y, add(Z, one))) = mul(Y, mul(add(Z, one), X))
101. add(X, mul(X, add(Y, neg(one)))) -> mul(X, Y)
102. add(X, mul(X, add(neg(one), Y))) -> mul(X, Y)
103. add(neg(X), mul(X, add(Y, one))) -> mul(X, Y)
104. add(neg(X), mul(X, Y)) = mul(X, add(Y, neg(one)))
105. add(X, add(X, add(Y, Y))) = mul(add(X, Y), add(one, one))
106. mul(X, add(one, add(Y, one))) = add(X, add(X, mul(X, Y)))
107. mul(X, add(one, add(one, Y))) = add(X, add(X, mul(X, Y)))
108. mul(add(X, add(one, one)), Y) = add(Y, add(Y, mul(Y, X)))
109. mul(add(one, add(X, one)), Y) = add(Y, add(Y, mul(Y, X)))
110. mul(X, add(Y, add(one, one))) = add(X, add(X, mul(Y, X)))
111. mul(X, add(one, add(Y, one))) = add(X, add(X, mul(Y, X)))
112. mul(add(X, add(one, one)), Y) = add(Y, add(Y, mul(X, Y)))
113. add(X, neg(add(X, Y))) -> neg(Y)
114. add(X, add(X, Y)) = add(mul(add(one, one), X), Y)
115. mul(add(one, one), mul(X, Y)) = mul(add(X, X), Y)
  (hard) mul(add(Y, Y), X) = mul(add(X, X), Y)
116. add(X, add(Y, X)) = add(Y, mul(add(one, one), X))
117. mul(add(one, one), mul(X, Y)) = mul(X, add(Y, Y))
118. mul(X, add(Y, Y)) = mul(Y, mul(add(one, one), X))
119. add(X, mul(Y, add(one, one))) = add(Y, add(Y, X))
120. mul(add(X, X), Y) = mul(add(one, one), mul(Y, X))
121. add(mul(X, Y), mul(X, Y)) -> mul(add(Y, Y), X)
  (hard) add(X, add(Y, X)) = add(mul(X, add(one, one)), Y)
122. mul(add(one, one), add(X, one)) = add(one, add(one, add(X, X)))
123. mul(add(X, X), mul(Y, Z)) = mul(X, mul(Z, add(Y, Y)))
124. mul(add(X, X), mul(Y, Z)) = mul(X, mul(Y, add(Z, Z)))
125. mul(X, mul(Y, add(Z, Z))) = mul(add(Z, Z), mul(X, Y))
  (hard) mul(X, mul(add(Y, Y), Z)) = mul(Z, mul(Y, add(X, X)))
126. mul(X, mul(add(Y, Y), Z)) = mul(Z, mul(add(X, X), Y))
  (hard) mul(add(X, X), mul(Y, Z)) = mul(Z, mul(add(X, X), Y))
  (hard) mul(Z, mul(Y, add(X, X))) = mul(add(X, X), mul(Y, Z))
127. mul(add(X, mul(Y, X)), Z) = mul(X, add(Z, mul(Z, Y)))
128. mul(X, add(Y, mul(Z, Y))) = mul(Y, add(X, mul(Z, X)))
129. add(neg(X), mul(Y, X)) = mul(X, add(Y, neg(one)))
  (hard) add(Z, add(Y, mul(Z, X))) = add(Y, add(Z, mul(X, Z)))
  (hard) add(X, add(Y, mul(Z, X))) = add(X, add(mul(X, Z), Y))
  (hard) add(Z, mul(Z, mul(Y, X))) = add(Z, mul(X, mul(Z, Y)))
  (hard) add(Z, mul(Y, mul(X, Z))) = add(Z, mul(X, mul(Z, Y)))
  (hard) add(Z, mul(Y, mul(Z, X))) = add(Z, mul(X, mul(Z, Y)))
  (hard) add(Z, mul(Y, mul(X, Z))) = add(Z, mul(Z, mul(Y, X)))
130. add(one, neg(add(X, one))) -> neg(X)
  (hard) add(X, add(neg(Y), add(Z, Y))) = add(X, Z)
131. add(mul(X, Y), add(Z, mul(X, W))) -> add(Z, mul(X, add(Y, W)))
132. add(mul(X, Y), add(Z, mul(W, Y))) -> add(Z, mul(Y, add(X, W)))
133. add(mul(X, mul(Y, Z)), mul(W, Z)) -> mul(Z, add(W, mul(X, Y)))
134. add(mul(X, Y), mul(Z, mul(W, Y))) -> mul(Y, add(X, mul(Z, W)))
135. add(X, add(Y, mul(X, Z))) = add(mul(add(one, Z), X), Y)
136. add(X, mul(X, add(Y, Z))) = mul(add(Y, add(one, Z)), X)
137. add(X, add(Y, mul(X, Z))) = add(Y, mul(add(one, Z), X))
138. mul(add(one, X), mul(Y, Z)) = mul(Y, add(Z, mul(Z, X)))
139. mul(add(one, add(X, X)), Y) = add(Y, mul(X, add(Y, Y)))
140. add(X, mul(add(Y, Z), X)) = mul(X, add(Y, add(one, Z)))
141. add(X, add(Y, mul(Z, X))) = add(Y, mul(X, add(one, Z)))
142. mul(X, mul(Y, add(one, Z))) = mul(Y, add(X, mul(Z, X)))
143. add(X, mul(add(Y, Z), X)) = mul(add(Y, add(Z, one)), X)
144. add(X, add(Y, mul(Z, X))) = add(Y, mul(add(Z, one), X))
145. mul(add(X, one), mul(Y, Z)) = mul(Y, add(Z, mul(X, Z)))
  (hard) add(Z, mul(Z, add(Y, X))) = mul(add(X, add(one, Y)), Z)
  (hard) add(X, mul(add(Y, Z), X)) = mul(X, add(Z, add(one, Y)))
146. mul(add(X, mul(X, Y)), Z) = mul(X, add(Z, mul(Z, Y)))
147. mul(X, add(Y, mul(Y, Z))) = mul(Y, mul(add(one, Z), X))
148. mul(add(X, mul(X, Y)), Z) = mul(X, add(Z, mul(Y, Z)))
  (hard) mul(Z, add(Y, mul(X, Y))) = mul(Y, mul(add(X, one), Z))
149. mul(X, add(Y, mul(Z, Y))) = mul(Y, mul(add(one, Z), X))
150. add(X, neg(mul(X, add(one, one)))) -> neg(X)
151. add(X, neg(mul(add(one, one), X))) -> neg(X)
152. add(mul(X, add(one, Y)), Z) = add(X, add(mul(X, Y), Z))
153. add(mul(X, add(one, Y)), Z) = add(X, add(Z, mul(Y, X)))
  (hard) add(X, mul(X, add(Y, Z))) = mul(X, add(one, add(Z, Y)))
  (hard) add(Z, mul(Z, add(Y, X))) = mul(add(one, add(X, Y)), Z)
  (hard) add(X, mul(add(Y, Z), X)) = mul(X, add(one, add(Z, Y)))
154. add(X, mul(Y, add(one, Z))) = add(Y, add(mul(Y, Z), X))
  (hard) mul(X, add(Y, add(one, Y))) = add(X, mul(Y, add(X, X)))
155. add(mul(add(X, one), Y), Z) = add(Y, add(mul(Y, X), Z))
156. add(mul(add(X, one), Y), Z) = add(Y, add(Z, mul(X, Y)))
157. add(X, mul(Y, add(Z, Z))) = add(mul(add(Y, Y), Z), X)
158. add(X, mul(Y, add(Z, Z))) = add(mul(add(Z, Z), Y), X)
159. add(X, mul(add(Y, one), Z)) = add(Z, add(mul(Z, Y), X))
160. add(X, mul(Y, mul(X, Z))) = mul(add(one, mul(Z, Y)), X)
161. mul(add(one, mul(X, Y)), Z) = add(Z, mul(X, mul(Y, Z)))
162. mul(X, add(Y, mul(Y, Z))) = mul(add(Z, one), mul(Y, X))
163. mul(X, add(Y, mul(Z, Y))) = mul(add(Z, one), mul(Y, X))
164. add(X, mul(Y, add(Z, one))) = add(Y, add(mul(Z, Y), X))
165. add(mul(X, add(Y, one)), Z) = add(X, add(mul(Y, X), Z))
  (hard) add(X, mul(X, add(Y, Z))) = mul(X, add(Z, add(Y, one)))
  (hard) add(Z, mul(Z, add(Y, X))) = mul(add(X, add(Y, one)), Z)
  (hard) add(X, mul(add(Y, Z), X)) = mul(X, add(Z, add(Y, one)))
  (hard) add(Z, mul(Z, mul(Y, X))) = mul(add(one, mul(X, Y)), Z)
  (hard) add(X, mul(Y, mul(Z, X))) = mul(X, add(one, mul(Z, Y)))
166. mul(X, mul(Y, add(one, Z))) = mul(add(one, Z), mul(X, Y))
167. add(X, mul(Y, add(X, one))) = add(Y, mul(X, add(one, Y)))
  (hard) add(Y, mul(X, add(Y, one))) = add(X, mul(add(X, one), Y))
  (hard) mul(Z, add(Y, add(X, one))) = mul(add(one, add(Y, X)), Z)
168. mul(X, mul(Y, add(Z, one))) = mul(Y, mul(add(one, Z), X))
  (hard) add(X, mul(Y, add(one, X))) = add(Y, mul(X, add(one, Y)))
  (hard) add(Y, mul(X, add(one, Y))) = add(X, mul(add(X, one), Y))
  (hard) add(Z, mul(Y, add(X, one))) = add(mul(Y, add(one, X)), Z)
  (hard) add(Z, mul(Y, add(one, X))) = add(mul(Y, add(X, one)), Z)
  (hard) mul(Z, add(Y, add(one, X))) = mul(add(Y, add(X, one)), Z)
  (hard) mul(Z, add(Y, add(one, X))) = mul(add(one, add(Y, X)), Z)
  (hard) mul(Z, mul(Y, add(one, X))) = mul(Y, mul(add(X, one), Z))
  (hard) mul(Z, mul(Y, add(one, X))) = mul(Y, mul(add(one, X), Z))
  (hard) add(Y, mul(add(Y, one), X)) = add(X, mul(add(X, one), Y))
169. add(X, mul(Y, add(Z, one))) = add(mul(add(Z, one), Y), X)
170. add(X, mul(Y, add(one, Z))) = add(mul(add(Z, one), Y), X)
  (hard) mul(Z, add(one, add(Y, X))) = mul(add(Y, add(X, one)), Z)
  (hard) mul(add(X, add(Y, one)), Z) = mul(add(one, add(X, Y)), Z)
171. add(X, mul(add(Y, one), Z)) = add(mul(Z, add(Y, one)), X)
172. add(X, mul(add(Y, one), Z)) = add(mul(Z, add(one, Y)), X)
173. mul(X, mul(Y, add(Z, one))) = mul(add(Z, one), mul(Y, X))
174. mul(add(X, one), mul(Y, Z)) = mul(Y, mul(Z, add(X, one)))
  (hard) mul(add(X, one), mul(Y, Z)) = mul(Y, mul(Z, add(one, X)))
  (hard) mul(add(X, one), mul(Y, Z)) = mul(Y, mul(add(one, X), Z))
  (hard) mul(Z, mul(Y, add(one, X))) = mul(add(X, one), mul(Y, Z))
  (hard) add(Y, mul(X, add(Y, one))) = add(X, mul(add(one, X), Y))
175. mul(add(X, mul(Y, X)), Z) -> mul(X, mul(Z, add(Y, one)))
  (hard) mul(Z, mul(add(Y, one), X)) = mul(X, mul(add(Y, one), Z))
  (hard) mul(Z, mul(add(Y, one), X)) = mul(X, mul(add(one, Y), Z))
  (hard) mul(Z, mul(add(Y, one), X)) = mul(add(Y, one), mul(X, Z))
  (hard) mul(Z, add(one, mul(Y, X))) = mul(add(one, mul(X, Y)), Z)
176. add(X, mul(add(Y, neg(one)), X)) -> mul(X, Y)
177. add(X, mul(add(neg(one), Y), X)) -> mul(X, Y)
178. add(X, neg(mul(Y, X))) = mul(X, add(one, neg(Y)))
179. add(neg(X), mul(add(Y, one), X)) -> mul(X, Y)
180. add(neg(X), mul(X, add(one, Y))) -> mul(X, Y)
181. add(X, neg(mul(Y, X))) = mul(add(one, neg(Y)), X)
182. add(X, neg(mul(X, Y))) = mul(X, add(one, neg(Y)))
183. add(neg(X), mul(X, Y)) = mul(X, add(neg(one), Y))
184. mul(add(X, neg(one)), Y) = add(neg(Y), mul(Y, X))
185. add(neg(X), mul(Y, X)) = mul(X, add(neg(one), Y))
186. mul(add(X, neg(one)), Y) = add(neg(Y), mul(X, Y))
  (hard) add(Y, neg(mul(Y, X))) = mul(add(one, neg(X)), Y)
187. mul(add(one, one), add(X, Y)) = add(X, add(X, add(Y, Y)))
188. mul(add(one, add(one, X)), Y) = add(Y, add(Y, mul(Y, X)))
189. mul(add(one, add(X, one)), Y) = add(Y, add(Y, mul(X, Y)))
190. mul(X, add(one, add(one, Y))) = add(X, add(X, mul(Y, X)))
191. mul(add(one, add(one, X)), Y) = add(Y, add(Y, mul(X, Y)))
  (hard) mul(add(X, Y), add(one, one)) = add(Y, add(X, add(X, Y)))
  (hard) mul(add(X, Y), add(one, one)) = add(X, add(Y, add(X, Y)))
192. add(X, neg(add(Y, X))) -> neg(Y)
193. add(X, add(Y, add(Z, neg(add(X, Y))))) -> Z
194. add(X, add(Y, neg(add(X, X)))) -> add(Y, neg(X))
  (hard) mul(add(X, add(one, one)), Y) = add(Y, mul(add(one, X), Y))
  (hard) mul(X, Y) = mul(X, add(neg(one), add(Y, one)))
  (hard) mul(add(neg(one), add(X, one)), Y) = mul(X, Y)
  (hard) mul(Y, add(one, add(one, X))) = mul(add(X, add(one, one)), Y)
  (hard) add(one, add(one, add(X, X))) = add(X, add(one, add(X, one)))
195. add(X, add(X, Y)) = add(Y, mul(add(one, one), X))
196. mul(X, add(Y, Y)) = mul(add(one, one), mul(Y, X))
197. add(mul(X, Y), mul(X, Y)) -> mul(Y, add(X, X))
  (hard) add(X, add(Y, X)) = add(mul(add(one, one), X), Y)
  (hard) mul(X, mul(Y, add(one, one))) = mul(add(Y, Y), X)
  (hard) mul(X, mul(add(one, one), Y)) = mul(add(Y, Y), X)
198. add(X, add(X, add(X, X))) = mul(X, add(one, add(one, add(one, one))))
199. add(X, add(X, add(X, X))) = mul(add(one, one), mul(X, add(one, one)))
200. add(X, neg(mul(X, add(Y, one)))) -> neg(mul(X, Y))
201. add(X, neg(mul(X, add(one, Y)))) -> neg(mul(X, Y))
202. add(X, neg(mul(add(Y, one), X))) -> neg(mul(X, Y))
  (hard) mul(add(X, one), add(one, one)) = add(one, add(one, add(X, X)))
  (hard) mul(add(one, one), add(X, one)) = add(X, add(one, add(one, X)))
  (hard) add(X, add(one, add(one, X))) = mul(add(one, one), add(one, X))
  (hard) mul(add(X, one), add(one, one)) = add(X, add(one, add(one, X)))
  (hard) add(X, add(Y, add(Z, Z))) = add(Z, add(Z, add(X, Y)))
  (hard) mul(Y, mul(add(Z, Z), X)) = mul(X, mul(add(Z, Z), Y))
203. add(X, mul(Y, add(Z, Z))) = add(mul(Z, add(Y, Y)), X)
204. add(neg(X), add(neg(X), Y)) -> add(Y, neg(add(X, X)))
  (hard) add(Z, add(Y, mul(X, Z))) = add(Y, add(Z, mul(Z, X)))
205. mul(add(one, X), mul(Y, Z)) = mul(Y, add(Z, mul(X, Z)))
  (hard) add(X, add(mul(X, Y), Z)) = add(Z, add(X, mul(X, Y)))
  (hard) add(X, add(mul(X, Y), Z)) = add(Z, add(X, mul(Y, X)))
206. add(mul(add(X, X), Y), Z) = add(Z, mul(add(Y, Y), X))
  (hard) mul(add(one, X), mul(Z, Y)) = mul(Y, add(Z, mul(Z, X)))
207. add(mul(add(one, one), X), neg(mul(X, add(one, one)))) -> zero
  (simplifying rules with respect to one another...)
  (delete rule 18)
  (delete rule 25)
  (delete rule 28)
  (delete rule 31)
  (delete rule 32)
  (delete rule 33)
  (delete rule 34)
  (delete rule 38)
  (delete rule 39)
  (delete rule 44)
  (delete rule 45)
  (delete rule 47)
  (delete rule 49)
  (delete rule 50)
  (delete rule 54)
  (delete rule 57)
  (delete rule 63)
  (delete rule 67)
  (delete rule 71)
  (delete rule 72)
  (delete rule 82)
  (delete rule 83)
  (delete rule 86)
  (delete rule 88)
  (delete rule 89)
  (delete rule 91)
  (delete rule 96)
  (delete rule 106)
  (delete rule 108)
  (delete rule 109)
  (delete rule 111)
  (delete rule 113)
  (delete rule 116)
  (delete rule 120)
  (delete rule 121)
  (delete rule 122)
  (delete rule 130)
  (delete rule 138)
  (delete rule 146)
  (delete rule 147)
  (delete rule 149)
  (delete rule 150)
  (delete rule 151)
  (delete rule 160)
  (delete rule 162)
  (delete rule 163)
  (delete rule 173)
  (delete rule 184)
  (delete rule 188)
  (delete rule 189)
208. mul(X, add(one, neg(add(one, add(Y, Y))))) = add(X, neg(add(X, mul(Y, add(X, X)))))
209. mul(X, add(one, neg(add(one, mul(Y, Z))))) = add(X, neg(add(X, mul(Y, mul(Z, X)))))
210. mul(add(one, neg(add(one, add(X, X)))), Y) = add(Y, neg(add(Y, mul(X, add(Y, Y)))))
211. mul(X, add(one, neg(add(one, mul(Y, Z))))) = add(X, neg(add(X, mul(Y, mul(X, Z)))))
212. mul(X, add(one, neg(add(one, add(Y, Y))))) = add(X, neg(add(X, mul(add(X, X), Y))))
213. add(mul(X, mul(Y, Z)), mul(Y, W)) -> mul(Y, add(W, mul(X, Z)))
214. add(mul(X, Y), mul(Z, mul(X, W))) -> mul(X, add(Y, mul(Z, W)))
215. mul(add(X, Y), add(Z, Z)) = mul(Z, add(Y, add(Y, add(X, X))))
  (hard) add(Y, add(Y, add(X, X))) = add(X, add(Y, add(X, Y)))
216. mul(X, add(Y, add(Y, Z))) = add(mul(Y, add(X, X)), mul(X, Z))
217. mul(X, add(Y, add(Z, Z))) = add(mul(X, Y), mul(Z, add(X, X)))
218. mul(X, add(Y, add(Y, Y))) = mul(Y, add(X, add(X, X)))
  (hard) add(X, add(X, mul(X, Y))) = add(X, mul(X, add(Y, one)))
219. mul(X, add(Y, add(Y, Y))) = mul(add(X, add(X, X)), Y)
  (hard) add(mul(add(one, one), X), Y) = add(Y, add(X, X))
220. mul(add(X, Y), add(Z, Z)) = mul(Z, add(X, add(X, add(Y, Y))))
221. add(mul(X, mul(Y, Z)), mul(Z, W)) -> mul(Z, add(W, mul(X, Y)))
222. add(mul(X, Y), add(Z, mul(Y, W))) -> add(Z, mul(Y, add(X, W)))
223. add(mul(X, Y), mul(Z, mul(Y, W))) -> mul(Y, add(X, mul(Z, W)))
224. mul(X, add(Y, add(Z, Z))) = add(mul(Y, X), mul(Z, add(X, X)))
  (hard) mul(add(X, add(X, Z)), Y) = mul(Y, add(Z, add(X, X)))
  (hard) mul(add(one, X), mul(Y, Z)) = mul(Y, mul(Z, add(X, one)))
225. add(X, mul(add(Y, Z), X)) = mul(add(Y, add(one, Z)), X)
226. add(X, add(Y, mul(Z, X))) = add(Y, mul(add(one, Z), X))
  (hard) mul(Z, add(Y, mul(Y, X))) = mul(Y, mul(add(one, X), Z))
  (hard) add(Z, mul(add(Y, X), Z)) = mul(add(one, add(X, Y)), Z)
  (hard) add(Z, mul(add(Y, X), Z)) = mul(add(X, add(one, Y)), Z)
  (hard) add(Z, mul(add(Y, X), Z)) = mul(add(X, add(Y, one)), Z)
227. add(mul(X, Y), add(mul(X, Y), Z)) -> add(Z, mul(X, add(Y, Y)))
  (hard) mul(X, add(Y, mul(Y, Z))) = mul(X, mul(Y, add(Z, one)))
228. mul(add(X, mul(Y, X)), Z) = mul(X, add(Z, mul(Y, Z)))
229. mul(X, add(Y, one)) = add(X, mul(X, Y))
  (hard) mul(X, add(Y, mul(Y, X))) = mul(X, mul(add(one, X), Y))
  (hard) mul(X, add(Y, mul(Y, Z))) = mul(X, mul(add(Z, one), Y))
  (hard) mul(X, add(Y, mul(Y, Z))) = mul(X, mul(add(one, Z), Y))
  (hard) mul(Z, add(Y, mul(Y, X))) = mul(add(X, one), mul(Z, Y))
  (hard) mul(Z, add(Y, mul(Y, X))) = mul(add(one, X), mul(Z, Y))
  (hard) add(X, add(Y, mul(X, Z))) = add(mul(X, add(Z, one)), Y)
  (hard) mul(Z, add(Y, mul(X, Y))) = mul(Y, mul(add(one, X), Z))
230. add(mul(X, Y), add(mul(X, Z), W)) -> add(W, mul(X, add(Y, Z)))
231. add(mul(X, Y), add(Z, mul(W, X))) -> add(Z, mul(X, add(Y, W)))
232. add(mul(X, Y), add(mul(Z, Y), W)) -> add(W, mul(Y, add(Z, X)))
  (hard) mul(W, add(Z, mul(X, Y))) = add(mul(X, mul(W, Y)), mul(Z, W))
233. add(mul(X, Y), mul(Z, mul(W, X))) -> mul(X, add(Y, mul(Z, W)))
234. add(mul(add(one, X), Y), Z) = add(Y, add(mul(Y, X), Z))
  (hard) add(Y, mul(Y, X)) = mul(add(one, X), Y)
235. add(mul(add(one, X), Y), Z) = add(Y, add(Z, mul(X, Y)))
  (hard) mul(add(X, add(one, X)), Y) = add(Y, mul(X, add(Y, Y)))
236. add(X, mul(add(one, Y), Z)) = add(Z, add(mul(Z, Y), X))
  (hard) add(X, mul(add(X, X), Y)) = mul(X, add(Y, add(Y, one)))
237. add(X, mul(Y, add(one, Z))) = add(Y, add(mul(Z, Y), X))
238. add(X, mul(add(Y, one), Z)) = add(Z, add(mul(Y, Z), X))
  (hard) mul(X, add(Y, mul(Y, X))) = mul(add(X, one), mul(Y, X))
  (hard) mul(X, add(Y, mul(X, Y))) = mul(add(X, one), mul(Y, X))
  (hard) mul(add(X, one), mul(Y, Z)) = mul(Z, add(Y, mul(Y, X)))
  (hard) mul(Z, add(Y, mul(X, Y))) = mul(add(X, one), mul(Y, Z))
239. add(mul(X, add(one, Y)), Z) = add(X, add(mul(Y, X), Z))
  (hard) add(X, add(mul(X, Y), Z)) = add(mul(X, add(Y, one)), Z)
240. add(mul(add(X, one), Y), Z) = add(Y, add(mul(X, Y), Z))
  (hard) add(X, add(Y, mul(X, Z))) = add(Y, mul(X, add(Z, one)))
  (hard) add(mul(add(X, one), Y), Z) = add(Z, add(Y, mul(Y, X)))
  (hard) add(mul(add(X, one), Y), Z) = add(Z, add(Y, mul(X, Y)))
  (hard) add(Y, mul(X, add(one, Y))) = add(X, mul(add(one, X), Y))
  (hard) add(Y, mul(add(Y, one), X)) = add(X, mul(add(one, X), Y))
  (hard) add(Y, mul(add(one, Y), X)) = add(X, mul(add(one, X), Y))
241. add(X, mul(Y, add(one, Z))) = add(mul(add(one, Z), Y), X)
  (hard) add(Z, mul(add(Y, one), X)) = add(mul(add(one, Y), X), Z)
  (hard) mul(Z, add(one, add(Y, X))) = mul(add(Y, add(one, X)), Z)
  (hard) mul(add(X, add(one, Y)), Z) = mul(Z, add(X, add(Y, one)))
242. add(X, mul(add(one, Y), Z)) = add(mul(Z, add(one, Y)), X)
  (hard) add(Z, mul(add(one, Y), X)) = add(mul(add(Y, one), X), Z)
  (hard) mul(Z, mul(X, add(Y, one))) = mul(add(Y, one), mul(X, Z))
243. add(X, add(X, mul(Y, add(X, X)))) -> mul(add(X, X), add(Y, one))
244. add(neg(X), mul(add(one, Y), X)) -> mul(X, Y)
245. mul(X, add(one, neg(add(one, add(one, Y))))) = add(X, neg(add(X, add(X, mul(X, Y)))))
246. mul(add(neg(one), X), Y) = add(neg(Y), mul(Y, X))
  (hard) neg(add(Y, mul(Y, X))) = neg(mul(add(X, one), Y))
247. mul(add(neg(one), X), Y) = add(neg(Y), mul(X, Y))
248. mul(X, add(one, neg(add(one, add(one, Y))))) = add(X, neg(add(X, add(X, mul(Y, X)))))
249. mul(add(one, neg(add(one, add(one, X)))), Y) = add(Y, neg(add(Y, add(Y, mul(X, Y)))))
250. add(neg(X), add(Y, neg(X))) -> add(Y, neg(add(X, X)))
251. mul(X, add(neg(one), Y)) = mul(add(Y, neg(one)), X)
252. add(X, neg(add(X, one))) -> neg(one)
  (hard) mul(X, add(one, add(Y, one))) = add(X, add(X, mul(Y, X)))
  (hard) mul(add(one, add(X, one)), Y) = add(Y, add(Y, mul(X, Y)))
  (hard) mul(add(X, Y), add(one, one)) = add(X, add(Y, add(Y, X)))
  (hard) add(X, add(X, mul(X, Y))) = mul(X, add(one, add(Y, one)))
  (hard) mul(add(one, add(one, X)), Y) = add(Y, add(Y, mul(Y, X)))
  (hard) mul(add(one, add(X, one)), Y) = add(Y, add(Y, mul(Y, X)))
  (hard) add(X, add(X, mul(X, Y))) = mul(X, add(Y, add(one, one)))
  (hard) add(Y, add(Y, mul(Y, X))) = mul(add(X, add(one, one)), Y)
253. add(X, neg(add(X, add(X, X)))) -> neg(add(X, X))
254. add(X, add(Y, add(neg(add(X, Y)), Z))) -> Z
255. add(X, add(neg(add(X, X)), Y)) -> add(Y, neg(X))
256. add(X, neg(add(X, add(Y, Y)))) -> neg(add(Y, Y))
257. add(X, add(one, mul(Y, add(one, X)))) -> mul(add(X, one), add(Y, one))
258. add(X, add(one, mul(add(X, one), Y))) -> mul(add(X, one), add(Y, one))
  (hard) add(Y, add(X, Y)) = add(X, mul(add(one, one), Y))
  (hard) add(Y, add(X, Y)) = add(X, mul(Y, add(one, one)))
259. add(X, add(X, add(X, X))) = mul(add(one, one), mul(add(one, one), X))
260. add(X, neg(add(X, mul(X, Y)))) -> neg(mul(X, Y))
261. add(X, neg(mul(add(one, Y), X))) -> neg(mul(X, Y))
262. neg(add(X, add(Y, neg(Z)))) -> add(Z, neg(add(X, Y)))
  (hard) add(X, add(X, add(one, one))) = mul(add(one, one), add(one, X))
  (hard) add(X, add(X, add(one, one))) = mul(add(one, X), add(one, one))
263. mul(X, mul(add(Y, Y), Z)) = mul(Z, mul(X, add(Y, Y)))
264. add(mul(X, add(Y, Y)), Z) = add(Z, mul(add(X, X), Y))
265. add(mul(X, add(Y, Y)), Z) = add(Z, mul(add(Y, Y), X))
266. mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(X, add(Y, Y)))
  (hard) add(X, add(mul(Y, X), Z)) = add(Z, add(X, mul(X, Y)))
  (hard) add(X, add(mul(Y, X), Z)) = add(Z, add(X, mul(Y, X)))
267. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(X, add(Z, Y)))
  (hard) add(mul(X, Y), mul(Z, X)) = mul(X, add(Y, Z))
  (hard) mul(Y, add(Z, add(X, Z))) = mul(add(X, add(Z, Z)), Y)
  (hard) add(mul(X, Y), add(Z, X)) = add(Z, add(X, mul(X, Y)))
  (hard) add(mul(X, Y), add(Z, X)) = add(Z, mul(X, add(one, Y)))
  (hard) add(mul(X, Y), add(Z, X)) = add(Z, add(X, mul(Y, X)))
  (hard) add(mul(X, Y), add(Z, X)) = add(X, add(Z, mul(X, Y)))
  (hard) add(X, mul(add(X, Z), Y)) = add(X, mul(Y, add(Z, X)))
  (hard) add(X, add(Y, mul(Z, X))) = add(mul(X, Z), add(Y, X))
  (hard) add(X, add(mul(X, Y), Z)) = add(mul(X, Y), add(Z, X))
  (hard) mul(add(X, add(X, Z)), Y) = mul(Y, add(X, add(Z, X)))
268. add(mul(add(X, one), Y), Z) = add(mul(Y, X), add(Z, Y))
  (hard) mul(add(X, one), Y) = add(Y, mul(Y, X))
  (hard) add(X, add(mul(Y, X), Z)) = add(mul(X, Y), add(Z, X))
269. mul(add(X, Y), add(Z, Z)) = mul(add(X, add(Y, add(X, Y))), Z)
270. mul(add(X, X), add(Y, Z)) = mul(X, add(Y, add(Y, add(Z, Z))))
  (hard) mul(add(Z, Z), add(Y, X)) = mul(add(X, Y), add(Z, Z))
271. mul(X, add(Y, add(Y, Z))) = add(mul(add(X, X), Y), mul(X, Z))
272. mul(X, add(Y, add(Z, Z))) = add(mul(X, Y), mul(add(X, X), Z))
  (hard) add(mul(X, add(one, one)), Y) = add(Y, add(X, X))
273. mul(X, add(Y, add(Y, Z))) = add(mul(Y, add(X, X)), mul(Z, X))
274. mul(add(X, Y), add(Z, Z)) = mul(add(X, add(X, add(Y, Y))), Z)
275. mul(X, add(Y, add(Z, Z))) = add(mul(Y, X), mul(add(X, X), Z))
  (hard) mul(X, add(Y, mul(X, Y))) = mul(add(one, X), mul(Y, X))
  (hard) mul(Z, add(Y, mul(X, Y))) = mul(add(one, X), mul(Y, Z))
  (hard) mul(add(X, add(X, add(Y, Y))), Z) = mul(add(Y, X), add(Z, Z))
276. add(mul(X, Y), mul(Z, add(X, X))) = mul(X, add(Z, add(Z, Y)))
277. add(mul(X, Y), add(mul(Y, Z), W)) -> add(W, mul(Y, add(Z, X)))
278. add(X, mul(add(one, Y), Z)) = add(Z, add(mul(Y, Z), X))
279. add(mul(add(one, X), Y), Z) = add(Y, add(mul(X, Y), Z))
  (hard) add(mul(Y, add(X, one)), Z) = add(Z, add(Y, mul(Y, X)))
  (hard) add(mul(Y, add(X, one)), Z) = add(Z, add(Y, mul(X, Y)))
280. add(mul(X, Y), mul(add(Z, Z), X)) -> mul(X, add(Z, add(Z, Y)))
  (hard) add(mul(add(one, X), Y), Z) = add(mul(Y, X), add(Z, Y))
  (hard) add(mul(X, Y), add(Z, X)) = add(Z, mul(add(Y, one), X))
  (hard) add(mul(X, Y), add(Z, X)) = add(mul(X, add(Y, one)), Z)
  (hard) add(Y, add(Z, mul(X, Y))) = add(mul(X, Y), add(Z, Y))
  (hard) add(mul(X, Y), add(Z, X)) = add(Z, mul(X, add(Y, one)))
281. add(X, add(Y, mul(Z, add(X, Y)))) -> mul(add(X, Y), add(Z, one))
  (hard) mul(add(one, one), add(X, one)) = add(one, add(one, add(X, X)))
  (hard) mul(add(one, one), add(X, one)) = add(one, add(X, add(X, one)))
282. add(X, add(X, mul(add(X, X), Y))) -> mul(add(X, X), add(Y, one))
283. mul(add(X, one), add(X, neg(one))) -> add(neg(one), mul(X, X))
  (hard) mul(Z, mul(X, add(Y, one))) = mul(add(one, Y), mul(X, Z))
  (hard) mul(add(one, X), mul(Y, Z)) = mul(Y, mul(add(X, one), Z))
  (hard) mul(X, mul(Y, add(one, X))) = mul(add(one, X), mul(Y, X))
  (hard) mul(X, mul(add(X, one), Y)) = mul(add(one, X), mul(Y, X))
  (hard) mul(Z, mul(Y, add(one, X))) = mul(add(one, X), mul(Y, Z))
  (hard) add(Z, mul(add(one, Y), X)) = add(mul(X, add(Y, one)), Z)
  (hard) add(Z, mul(Y, add(X, one))) = add(mul(add(one, X), Y), Z)
284. add(neg(mul(X, Y)), mul(X, add(Y, Z))) -> mul(X, Z)
285. add(mul(X, Y), neg(mul(X, Z))) -> mul(X, add(Y, neg(Z)))
286. neg(mul(X, add(Y, neg(Z)))) -> mul(X, add(Z, neg(Y)))
287. neg(mul(X, add(neg(one), Y))) -> mul(X, add(one, neg(Y)))
288. add(neg(mul(X, Y)), mul(add(X, X), Y)) -> mul(X, Y)
289. add(neg(mul(X, Y)), mul(Y, add(X, Z))) -> mul(Y, Z)
  (hard) add(neg(Y), mul(Y, X)) = mul(add(X, neg(one)), Y)
290. add(neg(mul(X, Y)), mul(Y, add(Z, X))) -> mul(Y, Z)
291. add(neg(mul(X, Y)), mul(X, Z)) -> mul(X, add(Z, neg(Y)))
292. add(mul(X, Y), neg(mul(Y, Z))) -> mul(Y, add(X, neg(Z)))
293. add(mul(X, Y), neg(mul(Z, X))) -> mul(X, add(Y, neg(Z)))
294. mul(X, add(Y, neg(one))) = mul(add(neg(one), Y), X)
  (hard) add(neg(X), add(Y, X)) = Y
295. add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(X, add(Y, one)))
296. add(X, mul(Y, add(Z, X))) = add(mul(Z, Y), mul(X, add(one, Y)))
  (hard) mul(add(one, one), add(X, Y)) = add(X, add(Y, add(Y, X)))
  (hard) mul(add(one, one), add(X, Y)) = add(Y, add(X, add(X, Y)))
297. add(X, mul(Y, add(Z, X))) = add(mul(Z, Y), mul(X, add(Y, one)))
298. add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(X, add(one, Y)))
  (simplifying rules with respect to one another...)
  (delete rule 79)
  (delete rule 81)
  (delete rule 94)
  (delete rule 127)
  (delete rule 135)
  (delete rule 137)
  (delete rule 155)
  (delete rule 159)
  (delete rule 170)
  (delete rule 172)
  (delete rule 206)
  (delete rule 207)
  (delete rule 208)
299. neg(mul(X, add(Y, Y))) = add(X, neg(add(X, mul(Y, add(X, X)))))
  (delete rule 210)
  (delete rule 212)
300. neg(mul(X, add(Y, Y))) = add(X, neg(add(X, mul(add(X, X), Y))))
  (delete rule 215)
  (delete rule 217)
  (delete rule 227)
  (delete rule 234)
  (delete rule 236)
  (delete rule 243)
  (delete rule 246)
  (delete rule 253)
  (delete rule 257)
  (delete rule 268)
  (delete rule 269)
  (delete rule 273)
  (delete rule 284)
  (delete rule 285)
  (delete rule 289)
301. add(X, mul(X, add(Y, add(one, Y)))) -> mul(add(Y, one), add(X, X))
302. add(X, mul(X, add(one, add(Y, Y)))) -> mul(add(one, Y), add(X, X))
303. add(X, neg(add(X, mul(Y, X)))) -> neg(mul(X, Y))
304. add(X, neg(add(Y, add(X, Y)))) -> neg(add(Y, Y))
305. add(X, neg(add(Y, add(Y, X)))) -> neg(add(Y, Y))
306. add(one, add(X, add(Y, mul(Y, X)))) -> mul(add(X, one), add(Y, one))
307. mul(add(X, one), add(one, neg(X))) -> add(one, neg(mul(X, X)))
308. add(X, add(one, add(Y, mul(X, Y)))) -> mul(add(X, one), add(Y, one))
309. add(X, neg(add(X, add(X, add(X, X))))) -> neg(add(X, add(X, X)))
310. add(X, add(one, mul(add(one, X), Y))) -> mul(add(X, one), add(Y, one))
311. mul(X, add(Y, add(Y, one))) = add(X, mul(X, mul(Y, add(one, one))))
312. add(X, mul(Y, mul(X, add(one, one)))) = mul(X, add(one, add(Y, Y)))
313. mul(X, add(Y, add(Y, one))) = add(X, mul(Y, mul(X, add(one, one))))
314. add(mul(X, Y), neg(mul(X, add(Y, Z)))) -> neg(mul(X, Z))
315. add(X, add(X, mul(Y, add(one, one)))) -> mul(add(X, Y), add(one, one))
316. add(mul(X, add(one, one)), add(Y, Y)) -> mul(add(X, Y), add(one, one))
  (hard) mul(add(X, Y), add(one, one)) = add(Y, add(Y, add(X, X)))
317. add(X, add(X, mul(add(one, one), Y))) -> mul(add(one, one), add(X, Y))
  (hard) mul(add(one, one), add(X, Y)) = add(X, add(Y, add(X, Y)))
318. add(neg(X), add(Y, neg(Z))) -> add(Y, neg(add(X, Z)))
319. neg(add(X, add(neg(Y), Z))) -> add(Y, neg(add(X, Z)))
320. neg(mul(X, add(neg(Y), Z))) -> mul(X, add(Y, neg(Z)))
321. neg(mul(add(X, neg(Y)), Z)) -> mul(Z, add(Y, neg(X)))
322. mul(add(one, X), add(X, neg(one))) -> add(neg(one), mul(X, X))
323. mul(add(X, one), add(neg(one), X)) -> add(neg(one), mul(X, X))
324. mul(add(one, X), add(one, neg(X))) -> add(one, neg(mul(X, X)))
325. mul(X, mul(Y, add(one, add(one, one)))) = mul(Y, add(X, add(X, X)))
326. mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(Y, add(X, X)))
327. add(one, add(one, neg(add(one, add(X, add(one, X)))))) -> neg(mul(add(one, one), X))
328. add(neg(mul(X, Y)), mul(Y, add(X, mul(add(Z, Z), W)))) -> mul(Z, mul(W, add(Y, Y)))
  (hard) neg(X) = add(mul(X, Y), neg(add(X, mul(Y, X))))
329. mul(add(X, X), mul(Y, mul(Y, Z))) = mul(X, mul(Y, mul(Y, add(Z, Z))))
330. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, add(X, mul(X, Z))))
  (hard) mul(Z, add(Y, add(Y, add(X, X)))) = mul(add(Y, add(X, add(Y, X))), Z)
331. mul(add(X, X), mul(Y, mul(Z, Y))) = mul(X, mul(Y, mul(Y, add(Z, Z))))
332. mul(add(X, X), add(Y, add(Y, Y))) = mul(add(X, add(X, X)), add(Y, Y))
333. add(mul(X, Y), mul(Z, add(X, W))) = add(mul(Z, W), mul(X, add(Y, Z)))
  (hard) add(Z, add(mul(Z, Y), X)) = add(X, mul(Z, add(Y, one)))
  (hard) mul(add(X, Y), add(one, one)) = add(Y, add(X, add(Y, X)))
334. mul(X, add(Y, neg(add(Y, Z)))) -> neg(mul(X, Z))
  (hard) add(mul(add(X, one), Y), Z) = add(Y, add(mul(Y, X), Z))
335. mul(add(X, neg(add(X, Y))), Z) -> neg(mul(Y, Z))
336. add(X, neg(add(X, mul(Y, Z)))) -> neg(mul(Y, Z))
337. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, add(X, mul(Z, X))))
338. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Y, add(X, Z)), mul(Z, W))
  (hard) add(Z, add(Y, mul(Y, X))) = add(mul(add(one, X), Y), Z)
339. add(mul(X, add(Y, Z)), mul(W, Y)) = add(mul(X, Z), mul(Y, add(W, X)))
  (hard) add(mul(X, Y), add(Z, X)) = add(Z, mul(add(one, Y), X))
340. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Z, W), mul(Y, add(Z, X)))
  (hard) mul(Y, add(Z, add(Z, X))) = mul(add(X, add(Z, Z)), Y)
341. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(X, add(Y, Z)))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Z, add(Y, mul(X, Y)))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Z, mul(Y, add(X, one)))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Y, add(mul(X, Y), Z))
  (hard) add(mul(X, Y), add(Z, Y)) = add(mul(add(X, one), Y), Z)
  (hard) add(mul(add(X, one), Y), Z) = add(mul(Y, X), add(Z, Y))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Z, add(Y, mul(Y, X)))
342. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(W, Z), mul(Y, add(X, Z)))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Y, add(Z, mul(Y, X)))
  (hard) add(X, mul(add(Y, X), Z)) = add(X, mul(Z, add(X, Y)))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Z, mul(Y, add(one, X)))
  (hard) add(mul(add(one, X), Y), Z) = add(mul(X, Y), add(Z, Y))
343. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Z, W), mul(add(X, Z), Y))
  (hard) mul(add(X, add(Z, X)), Y) = mul(Y, add(Z, add(X, X)))
  (hard) mul(add(X, add(Z, X)), Y) = mul(Y, add(X, add(X, Z)))
344. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(add(Z, Y), X))
  (hard) add(mul(X, mul(Y, Z)), add(W, X)) = add(W, add(X, mul(Y, mul(X, Z))))
345. add(X, add(one, mul(X, add(one, X)))) -> mul(add(X, one), add(X, one))
346. add(X, add(Y, add(Z, add(W, neg(X))))) -> add(Z, add(Y, W))
347. mul(X, add(Y, neg(mul(Y, Z)))) = mul(Y, add(X, neg(mul(X, Z))))
  (hard) add(X, mul(add(Y, one), Z)) = add(Z, add(mul(Z, Y), X))
  (hard) add(X, mul(Y, add(X, Z))) = add(X, add(mul(Y, Z), mul(X, Y)))
348. add(mul(X, Y), mul(add(X, X), Z)) = mul(X, add(Z, add(Z, Y)))
349. add(mul(add(X, X), Y), mul(X, Z)) = mul(X, add(Y, add(Z, Y)))
350. mul(X, add(Y, add(Z, Z))) = add(mul(X, Y), mul(Z, add(X, X)))
  (hard) add(X, mul(Y, add(Z, X))) = add(X, add(mul(Y, X), mul(Z, Y)))
351. add(mul(X, add(Y, Y)), mul(Z, X)) -> mul(X, add(Y, add(Z, Y)))
  (hard) add(X, mul(add(X, Y), Z)) = add(X, add(mul(Z, Y), mul(X, Z)))
352. add(X, add(Y, mul(X, add(Z, W)))) = add(mul(X, add(Z, add(W, one))), Y)
353. add(X, mul(X, add(Y, add(Z, W)))) = mul(X, add(Y, add(Z, add(W, one))))
354. add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(X, add(Z, add(W, one))))
355. add(X, add(X, mul(X, add(one, one)))) -> mul(add(one, one), add(X, X))
  (hard) mul(X, mul(Y, add(Z, mul(Z, W)))) = mul(Y, mul(X, mul(add(W, one), Z)))
  (hard) mul(X, mul(Y, mul(Z, add(W, one)))) = mul(Z, mul(X, add(Y, mul(Y, W))))
356. mul(X, mul(Y, add(one, mul(Z, W)))) = mul(Y, add(X, mul(Z, mul(X, W))))
357. mul(X, mul(Y, add(Z, add(Z, one)))) = mul(Y, add(X, mul(Z, add(X, X))))
358. mul(X, mul(Y, add(Z, add(Z, one)))) = mul(Y, add(X, mul(add(X, X), Z)))
359. mul(add(X, mul(Y, mul(X, Z))), W) -> mul(X, mul(add(one, mul(Y, Z)), W))
360. add(X, mul(Y, mul(Z, mul(X, W)))) = mul(X, add(one, mul(Y, mul(Z, W))))
  (hard) mul(add(one, mul(X, Y)), Z) = add(Z, mul(X, mul(Z, Y)))
361. add(X, add(Y, mul(Z, mul(X, W)))) = add(Y, mul(X, add(one, mul(Z, W))))
362. add(X, mul(Y, mul(X, add(Z, Z)))) = mul(X, add(one, mul(Z, add(Y, Y))))
363. mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Y, mul(Z, add(X, X))))
364. mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(Y, mul(Z, add(X, X))))
365. add(X, mul(Y, mul(X, add(Z, Z)))) = mul(X, add(one, mul(add(Y, Y), Z)))
366. add(X, mul(add(Y, Y), mul(X, Z))) = mul(X, add(one, mul(Y, add(Z, Z))))
367. mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Y, mul(add(X, X), Z)))
  (hard) add(Y, mul(add(Y, Y), X)) = mul(add(X, add(X, one)), Y)
368. add(X, add(Y, mul(add(X, X), Z))) = add(mul(X, add(Z, add(Z, one))), Y)
  (hard) add(X, add(X, add(one, Y))) = add(one, add(X, add(Y, X)))
369. add(X, neg(add(X, add(X, one)))) -> neg(add(X, one))
  (hard) add(X, add(X, add(one, Y))) = add(one, add(X, add(X, Y)))
370. add(X, neg(add(X, add(one, X)))) -> neg(add(X, one))
371. add(X, neg(add(one, add(X, X)))) -> neg(add(X, one))
372. mul(X, mul(add(Y, add(Y, one)), Z)) = mul(add(X, mul(Y, add(X, X))), Z)
373. add(X, add(Y, mul(Z, add(X, X)))) = add(Y, mul(X, add(Z, add(Z, one))))
  (hard) add(X, add(Y, add(Y, one))) = add(one, add(X, add(Y, Y)))
  (hard) add(Y, add(X, add(X, one))) = add(one, add(X, add(Y, X)))
  (hard) add(Y, add(X, add(X, one))) = add(one, add(X, add(X, Y)))
374. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, mul(X, add(one, Z))))
375. add(X, add(X, mul(X, add(Y, Y)))) -> mul(add(one, Y), add(X, X))
  (hard) add(X, mul(X, mul(add(Y, W), Z))) = mul(X, add(one, mul(Z, add(W, Y))))
376. add(X, mul(Y, mul(Z, add(X, X)))) = mul(X, add(one, mul(Z, add(Y, Y))))
  (hard) add(Z, mul(Y, add(one, X))) = add(mul(add(X, one), Y), Z)
377. add(X, add(X, mul(X, add(Y, Y)))) -> mul(add(X, X), add(one, Y))
378. add(X, add(Y, mul(add(X, Y), Z))) -> mul(add(X, Y), add(Z, one))
379. add(X, add(X, mul(add(Y, Y), X))) -> mul(add(X, X), add(Y, one))
380. add(one, mul(X, add(one, add(X, one)))) -> mul(add(X, one), add(X, one))
381. add(one, mul(X, add(one, add(one, X)))) -> mul(add(X, one), add(X, one))
382. mul(X, add(one, add(Y, mul(Y, Z)))) = add(X, mul(Y, mul(X, add(Z, one))))
383. mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, mul(X, add(Z, one))))
384. add(X, mul(Y, mul(X, add(one, Z)))) = mul(X, add(one, add(Y, mul(Y, Z))))
  (hard) add(X, mul(Y, mul(X, Z))) = mul(X, add(one, mul(Z, Y)))
385. add(X, mul(Y, mul(X, add(Z, one)))) = mul(X, add(one, add(Y, mul(Z, Y))))
386. add(neg(mul(X, Y)), mul(add(Z, X), Y)) -> mul(Z, Y)
387. add(neg(mul(X, Y)), mul(Y, Z)) -> mul(Y, add(Z, neg(X)))
388. add(neg(mul(X, Y)), mul(Z, X)) -> mul(X, add(Z, neg(Y)))
389. add(mul(X, Y), neg(mul(Z, Y))) -> mul(Y, add(X, neg(Z)))
390. add(X, mul(X, add(Y, add(Z, neg(one))))) -> mul(X, add(Y, Z))
391. mul(X, add(Y, add(Z, neg(one)))) = add(neg(X), mul(X, add(Y, Z)))
392. add(X, neg(mul(Y, mul(X, Z)))) = mul(X, add(one, neg(mul(Y, Z))))
393. add(mul(X, add(one, Y)), mul(Z, Y)) = add(X, mul(Y, add(X, Z)))
394. add(X, neg(add(X, Y))) -> neg(Y)
395. add(X, add(Y, neg(add(X, Z)))) -> add(Y, neg(Z))
396. add(X, neg(add(Y, add(X, Z)))) -> neg(add(Z, Y))
397. add(mul(X, add(one, Y)), mul(Z, X)) -> add(X, mul(X, add(Y, Z)))
398. add(mul(X, add(Y, one)), mul(Y, Z)) = add(X, mul(Y, add(X, Z)))
399. add(mul(X, add(Y, one)), mul(Z, X)) -> add(X, mul(X, add(Y, Z)))
400. add(mul(X, add(Y, one)), mul(Z, Y)) = add(X, mul(Y, add(X, Z)))
401. add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(add(Y, one), X))
402. add(X, mul(Y, add(Z, X))) = add(mul(Z, Y), mul(add(Y, one), X))
403. add(mul(X, Y), mul(Z, add(X, one))) = add(Z, mul(X, add(Z, Y)))
  (hard) mul(X, add(one, add(X, Y))) = mul(X, add(Y, add(X, one)))
  (hard) mul(X, add(X, add(one, Y))) = mul(X, add(Y, add(X, one)))
404. add(mul(X, Y), mul(Z, add(one, Y))) = add(Z, mul(Y, add(Z, X)))
405. add(X, mul(Y, add(Z, X))) = add(mul(Z, Y), mul(add(one, Y), X))
  (hard) add(X, mul(Y, add(Z, X))) = add(mul(X, add(Y, one)), mul(Z, Y))
  (hard) add(X, mul(Y, add(Z, X))) = add(mul(X, add(one, Y)), mul(Z, Y))
406. add(X, mul(Y, add(Z, X))) = add(mul(add(Y, one), X), mul(Z, Y))
407. add(mul(X, Y), mul(Z, add(Y, one))) = add(Z, mul(Y, add(Z, X)))
408. add(mul(X, add(one, Y)), mul(Y, Z)) = add(X, mul(Y, add(Z, X)))
409. add(mul(X, Y), mul(Z, add(one, X))) = add(Z, mul(X, add(Z, Y)))
  (hard) add(X, mul(X, add(X, add(one, X)))) = mul(add(X, X), add(X, one))
410. add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(add(one, Y), X))
  (hard) add(X, mul(Y, add(Z, X))) = add(mul(X, add(Y, one)), mul(Y, Z))
411. mul(X, add(one, add(Y, add(Z, one)))) = add(X, add(X, mul(X, add(Y, Z))))
412. mul(X, add(Y, add(Z, add(one, one)))) = add(X, add(X, mul(X, add(Y, Z))))
413. add(X, add(X, mul(X, add(Y, Y)))) -> mul(add(X, X), add(Y, one))
414. mul(X, add(one, mul(Y, add(Z, one)))) = add(X, mul(Y, add(X, mul(X, Z))))
415. mul(X, mul(Y, add(one, add(Z, one)))) = mul(Y, add(X, add(X, mul(X, Z))))
416. mul(X, mul(Y, add(Z, add(one, one)))) = mul(Y, add(X, add(X, mul(Z, X))))
417. mul(X, add(one, add(one, mul(Y, Z)))) = add(X, add(X, mul(Y, mul(X, Z))))
418. mul(X, add(one, mul(Y, add(one, Z)))) = add(X, mul(Y, add(X, mul(X, Z))))
419. mul(X, add(one, mul(Y, add(Z, one)))) = add(X, mul(Y, add(X, mul(Z, X))))
420. mul(X, add(one, mul(Y, add(Z, one)))) = add(X, mul(X, add(Y, mul(Y, Z))))
  (hard) mul(X, add(one, add(Y, add(Y, one)))) = mul(add(X, X), add(Y, one))
421. add(X, mul(add(Y, add(one, Y)), X)) -> mul(add(Y, one), add(X, X))
  (hard) add(Y, add(Z, mul(Y, add(X, one)))) = add(mul(Y, X), add(Z, add(Y, Y)))
422. mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(Z, add(X, X)), mul(X, Y))
  (hard) add(X, mul(add(X, X), Y)) = mul(X, add(Y, add(one, Y)))
  (hard) add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(Y, add(Z, add(X, X)))
  (hard) add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(Y, add(X, add(Z, X)))
423. mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(X, Y), mul(Z, add(X, X)))
  (hard) add(mul(X, Y), mul(Z, add(X, X))) = mul(X, add(Z, add(Y, Z)))
424. mul(X, add(Y, add(Y, Y))) = mul(Y, add(X, mul(X, add(one, one))))
425. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(add(one, one), add(X, X)))
426. mul(X, add(Y, add(Y, add(Z, Z)))) = mul(add(Y, Z), mul(X, add(one, one)))
427. add(X, mul(add(one, add(Y, Y)), X)) -> mul(add(one, Y), add(X, X))
428. mul(X, add(one, add(one, mul(Y, Z)))) = add(X, add(X, mul(Y, mul(Z, X))))
  (hard) add(Y, add(Z, mul(Y, add(X, one)))) = add(mul(X, Y), add(Z, add(Y, Y)))
429. mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(Y, X), mul(Z, add(X, X)))
  (hard) add(mul(X, Y), mul(Z, add(Y, Y))) = mul(Y, add(Z, add(X, Z)))
  (hard) add(mul(X, Y), mul(Z, add(Y, Y))) = mul(Y, add(Z, add(Z, X)))
430. add(X, mul(X, add(one, add(Y, Y)))) -> mul(add(X, X), add(one, Y))
431. add(X, mul(add(one, add(Y, Y)), X)) -> mul(add(X, X), add(one, Y))
432. add(X, mul(Y, add(one, add(X, Y)))) -> mul(add(X, Y), add(Y, one))
433. add(X, mul(Y, add(X, add(Y, one)))) -> mul(add(X, Y), add(Y, one))
434. add(X, mul(X, add(Y, add(Y, one)))) -> mul(add(X, X), add(Y, one))
435. add(X, mul(Y, add(one, add(Y, X)))) -> mul(add(X, Y), add(Y, one))
436. add(X, mul(Y, add(X, add(one, Y)))) -> mul(add(X, Y), add(Y, one))
437. add(X, mul(add(Y, add(Y, one)), X)) -> mul(add(X, X), add(Y, one))
  (hard) mul(add(X, Y), add(Y, one)) = add(X, mul(Y, add(Y, add(one, X))))
438. add(X, mul(Y, add(Y, add(X, one)))) -> mul(add(X, Y), add(Y, one))
439. add(X, add(neg(add(X, Y)), Z)) -> add(Z, neg(Y))
440. add(X, add(Y, neg(add(Z, X)))) -> add(Y, neg(Z))
441. add(X, neg(add(Y, add(Z, X)))) -> neg(add(Y, Z))
442. add(one, add(X, add(Y, mul(X, Y)))) -> mul(add(X, one), add(Y, one))
443. add(X, add(Y, add(one, mul(Y, X)))) -> mul(add(X, one), add(Y, one))
444. add(X, add(Y, add(one, mul(X, Y)))) -> mul(add(X, one), add(Y, one))
445. add(X, add(mul(Y, add(one, one)), Z)) = add(Y, add(Y, add(X, Z)))
  (hard) add(Z, add(Z, add(Y, X))) = add(Y, add(Z, add(Z, X)))
  (hard) add(Z, add(Z, add(Y, X))) = add(Y, add(Z, add(X, Z)))
446. mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(add(one, one), mul(X, Z)))
447. mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(add(X, X), Z))
  (hard) mul(X, mul(add(Y, Y), Z)) = mul(add(X, X), mul(Z, Y))
448. mul(add(X, X), mul(Y, Z)) = mul(X, mul(Y, mul(add(one, one), Z)))
449. mul(X, mul(Y, mul(add(one, one), Z))) = mul(Y, mul(add(X, X), Z))
450. mul(X, mul(Y, add(Z, Z))) = mul(Y, mul(Z, mul(X, add(one, one))))
  (hard) mul(Y, mul(X, add(Y, Y))) = mul(add(X, X), mul(Y, Y))
451. mul(X, mul(Y, mul(Z, add(one, one)))) = mul(Z, mul(Y, add(X, X)))
452. mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(X, mul(Y, add(one, one))))
453. mul(X, mul(Y, mul(Z, add(one, one)))) = mul(Y, mul(add(X, X), Z))
454. mul(add(X, X), mul(Y, Z)) = mul(X, mul(Y, mul(Z, add(one, one))))
  (hard) mul(add(X, X), mul(Y, Z)) = mul(Z, mul(X, add(Y, Y)))
455. mul(X, mul(add(Y, Y), Z)) = mul(add(one, one), mul(X, mul(Y, Z)))
456. mul(X, mul(Y, add(Z, Z))) = mul(Y, mul(add(one, one), mul(X, Z)))
  (hard) mul(add(X, X), mul(Y, Z)) = mul(Y, mul(X, add(Z, Z)))
457. mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(X, mul(add(one, one), Y)))
  (hard) mul(Z, mul(add(Y, Y), X)) = mul(Y, mul(X, mul(Z, add(one, one))))
458. mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(add(one, one), mul(X, Y)))
  (hard) mul(Z, mul(add(Y, Y), X)) = mul(Y, mul(X, mul(add(one, one), Z)))
  (hard) mul(Z, mul(Y, add(X, X))) = mul(X, mul(Y, mul(add(one, one), Z)))
459. mul(X, add(Y, mul(add(one, one), Y))) = mul(Y, add(X, add(X, X)))
460. mul(X, add(Y, mul(add(one, one), Z))) = add(mul(Y, X), mul(Z, add(X, X)))
461. mul(X, add(Y, mul(Z, add(one, one)))) = mul(add(Z, add(Z, Y)), X)
462. mul(X, add(Y, mul(Z, add(one, one)))) = mul(add(Y, add(Z, Z)), X)
  (simplifying rules with respect to one another...)
  (delete rule 193)
  (delete rule 194)
  (delete rule 209)
  (delete rule 211)
  (delete rule 245)
  (delete rule 248)
  (delete rule 249)
  (delete rule 250)
  (delete rule 252)
  (delete rule 255)
  (delete rule 256)
  (delete rule 258)
  (delete rule 260)
  (delete rule 267)
  (delete rule 271)
  (delete rule 272)
  (delete rule 282)
  (delete rule 287)
  (delete rule 288)
  (delete rule 290)
  (delete rule 295)
  (delete rule 296)
  (delete rule 297)
  (delete rule 298)
  (delete rule 299)
  (delete rule 300)
  (delete rule 301)
  (delete rule 302)
  (delete rule 303)
  (delete rule 304)
  (delete rule 305)
  (delete rule 306)
  (delete rule 309)
  (delete rule 310)
  (delete rule 327)
  (delete rule 328)
  (delete rule 330)
  (delete rule 333)
  (delete rule 334)
  (delete rule 335)
  (delete rule 336)
  (delete rule 345)
  (delete rule 349)
  (delete rule 355)
  (delete rule 369)
  (delete rule 370)
  (delete rule 371)
  (delete rule 375)
  (delete rule 377)
  (delete rule 380)
  (delete rule 381)
  (delete rule 400)
  (delete rule 404)
  (delete rule 407)
  (delete rule 414)
  (delete rule 421)
  (delete rule 427)
  (delete rule 443)
  (delete rule 450)
  (delete rule 451)
463. mul(X, add(Y, mul(add(one, one), Z))) = add(mul(X, Y), mul(Z, add(X, X)))
464. add(X, mul(Y, add(Z, Z))) = add(mul(Z, mul(Y, add(one, one))), X)
465. mul(X, mul(X, add(Y, Y))) = mul(add(one, one), mul(X, mul(X, Y)))
  (hard) add(mul(X, add(one, one)), add(Y, X)) = add(X, add(X, add(X, Y)))
  (hard) add(X, add(mul(add(one, one), X), Y)) = add(Y, add(X, add(X, X)))
  (hard) add(mul(add(one, one), X), add(Y, X)) = add(Y, add(X, add(X, X)))
  (hard) mul(add(Z, add(X, Z)), Y) = mul(Y, add(X, mul(Z, add(one, one))))
  (hard) add(mul(X, add(one, one)), add(Y, X)) = add(Y, add(X, add(X, X)))
  (hard) add(X, mul(add(Y, Y), X)) = add(X, mul(add(one, one), mul(X, Y)))
  (hard) add(Y, add(X, add(Y, Y))) = add(X, add(Y, mul(Y, add(one, one))))
  (hard) add(Y, add(X, add(Y, Y))) = add(X, add(Y, mul(add(one, one), Y)))
466. mul(X, add(Y, mul(Y, add(one, one)))) = mul(add(X, add(X, X)), Y)
  (hard) mul(add(Y, add(Y, Y)), X) = mul(add(X, add(X, X)), Y)
467. mul(X, add(Y, add(Y, one))) = add(X, mul(X, mul(add(one, one), Y)))
468. add(X, mul(add(one, one), mul(X, Y))) = mul(X, add(one, add(Y, Y)))
469. mul(X, add(Y, add(Y, one))) = add(X, mul(Y, mul(add(one, one), X)))
470. add(X, mul(Y, add(Z, Z))) = add(mul(Y, mul(Z, add(one, one))), X)
471. add(X, mul(Y, mul(add(one, one), X))) = mul(add(Y, add(Y, one)), X)
472. mul(X, add(one, add(Y, Y))) = add(X, mul(Y, mul(add(one, one), X)))
473. mul(add(one, one), add(X, mul(X, Y))) -> mul(add(X, X), add(Y, one))
474. mul(add(X, add(X, one)), Y) = add(Y, mul(X, mul(Y, add(one, one))))
475. mul(X, add(Y, add(one, Y))) = add(X, mul(X, mul(Y, add(one, one))))
476. mul(X, add(Y, add(one, Y))) = add(X, mul(X, mul(add(one, one), Y)))
477. mul(X, add(Y, add(one, Y))) = add(X, mul(Y, mul(X, add(one, one))))
478. add(X, mul(X, mul(Y, add(one, one)))) = mul(X, add(one, add(Y, Y)))
479. mul(X, add(one, add(Y, Y))) = add(X, mul(X, mul(add(one, one), Y)))
480. add(X, mul(add(one, one), mul(Y, X))) = mul(X, add(one, add(Y, Y)))
  (hard) mul(add(Y, add(Y, one)), X) = add(X, mul(X, mul(Y, add(one, one))))
  (hard) mul(add(Y, add(Y, one)), X) = add(X, mul(X, mul(add(one, one), Y)))
481. mul(add(one, one), add(X, mul(Y, X))) -> mul(add(X, X), add(Y, one))
482. add(mul(X, Y), neg(mul(X, add(Z, Y)))) -> neg(mul(X, Z))
  (hard) add(X, mul(add(one, one), mul(Y, X))) = mul(X, add(Y, add(Y, one)))
  (hard) add(X, mul(add(one, one), mul(X, Y))) = mul(X, add(Y, add(Y, one)))
  (hard) mul(X, add(Y, add(one, add(Y, one)))) = mul(add(X, X), add(Y, one))
483. add(mul(add(one, one), X), add(Y, Y)) -> mul(add(one, one), add(X, Y))
  (hard) mul(add(one, one), add(X, Y)) = add(Y, add(Y, add(X, X)))
484. add(mul(X, add(one, one)), add(Y, neg(X))) -> add(X, Y)
485. add(neg(X), add(mul(X, add(one, one)), Y)) -> add(X, Y)
  (hard) add(X, Y) = add(neg(X), add(Y, add(X, X)))
486. mul(X, add(one, neg(add(Y, Y)))) = add(X, neg(mul(add(X, X), Y)))
487. add(X, add(X, mul(add(one, one), Y))) -> mul(add(X, Y), add(one, one))
  (hard) mul(add(X, one), add(Y, one)) = add(X, add(one, mul(Y, add(one, X))))
488. add(X, add(mul(Y, add(one, one)), X)) -> mul(add(X, Y), add(one, one))
489. add(X, add(mul(add(one, one), Y), X)) -> mul(add(one, one), add(X, Y))
490. add(neg(X), add(neg(Y), Z)) -> add(Z, neg(add(Y, X)))
491. neg(mul(add(neg(X), Y), Z)) -> mul(Z, add(X, neg(Y)))
492. mul(add(one, X), add(neg(one), X)) -> add(neg(one), mul(X, X))
  (hard) add(neg(one), mul(X, X)) = add(neg(add(X, one)), mul(X, add(one, X)))
493. mul(X, add(one, mul(add(one, one), Y))) = add(X, mul(Y, add(X, X)))
494. add(X, mul(X, add(Y, Y))) = mul(X, add(one, mul(add(one, one), Y)))
495. mul(X, add(one, mul(add(one, one), Y))) = add(X, mul(add(X, X), Y))
496. mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(X, add(Y, Y)))
497. mul(add(X, X), add(Y, Y)) = mul(X, mul(Y, add(one, add(one, add(one, one)))))
  (hard) mul(add(Y, Y), add(X, X)) = mul(X, mul(Y, add(one, add(one, add(one, one)))))
498. mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(add(X, X), Y))
  (hard) mul(add(one, add(X, X)), Y) = add(Y, mul(add(Y, Y), X))
499. mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(add(Y, Y), X))
500. add(X, add(X, add(X, Y))) = add(mul(X, add(one, add(one, one))), Y)
  (hard) add(Y, add(Y, add(Y, X))) = add(X, add(Y, add(Y, Y)))
501. mul(X, add(Y, neg(add(one, one)))) = add(mul(X, Y), neg(add(X, X)))
502. mul(X, mul(add(one, add(one, one)), Y)) = mul(add(X, add(X, X)), Y)
503. add(X, add(Y, add(X, X))) = add(Y, mul(X, add(one, add(one, one))))
504. add(X, add(X, add(X, Y))) = add(mul(add(one, add(one, one)), X), Y)
505. mul(add(one, add(one, one)), mul(X, Y)) = mul(add(X, add(X, X)), Y)
506. add(X, add(Y, add(X, X))) = add(Y, mul(add(one, add(one, one)), X))
507. mul(add(one, add(one, one)), mul(X, Y)) = mul(X, add(Y, add(Y, Y)))
  (hard) add(mul(X, add(one, add(one, one))), Y) = add(X, add(Y, add(X, X)))
508. add(X, mul(X, add(Y, Y))) = mul(add(one, mul(add(one, one), Y)), X)
509. add(X, mul(Y, add(X, X))) = mul(add(one, mul(add(one, one), Y)), X)
510. add(X, mul(add(Y, Y), X)) = mul(X, add(one, mul(add(one, one), Y)))
511. mul(X, mul(add(one, add(one, one)), Y)) = mul(Y, add(X, add(X, X)))
512. mul(X, mul(Y, add(one, add(one, one)))) = mul(add(X, add(X, X)), Y)
513. add(X, mul(Y, add(X, X))) = mul(add(one, mul(Y, add(one, one))), X)
514. mul(X, mul(Y, mul(add(Z, Z), W))) = mul(add(X, X), mul(Z, mul(Y, W)))
515. mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Z, mul(Y, mul(add(X, X), W)))
516. mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Y, mul(add(X, X), mul(Z, W)))
517. mul(X, mul(add(Y, Y), mul(Z, W))) = mul(add(X, X), mul(Z, mul(Y, W)))
  (hard) mul(add(Z, Z), mul(Y, X)) = mul(add(Y, Y), mul(Z, X))
518. mul(add(X, X), mul(Y, mul(Z, W))) = mul(X, mul(Y, mul(add(Z, Z), W)))
519. mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Z, mul(X, mul(Y, add(W, W))))
520. mul(X, mul(Y, mul(Z, add(W, W)))) = mul(Y, mul(W, mul(add(X, X), Z)))
521. mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Y, mul(Z, mul(X, add(W, W))))
522. mul(X, mul(add(Y, Y), add(Z, Z))) = mul(Y, mul(Z, add(X, add(X, add(X, X)))))
523. mul(X, mul(Y, mul(Z, add(W, W)))) = mul(W, mul(Z, mul(add(X, X), Y)))
524. mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Z, mul(add(X, X), mul(Y, W)))
525. mul(X, mul(Y, mul(Z, add(W, W)))) = mul(W, mul(Z, mul(Y, add(X, X))))
  (hard) mul(X, mul(Y, mul(Z, add(W, W)))) = mul(Y, mul(X, mul(W, add(Z, Z))))
526. mul(X, mul(Y, mul(Z, add(W, W)))) = mul(W, mul(Y, mul(add(X, X), Z)))
527. mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Y, mul(Z, mul(W, add(X, X))))
528. mul(X, mul(Y, mul(Z, add(W, W)))) = mul(W, mul(Y, mul(Z, add(X, X))))
529. mul(X, mul(Y, mul(Z, add(W, W)))) = mul(Y, mul(W, mul(Z, add(X, X))))
  (hard) mul(X, mul(Y, add(Z, mul(Z, W)))) = mul(Z, mul(X, add(Y, mul(Y, W))))
530. mul(X, add(Y, mul(Y, mul(Z, W)))) = mul(Y, add(X, mul(Z, mul(X, W))))
531. mul(X, add(Y, mul(Y, add(Z, Z)))) = mul(Y, add(X, mul(Z, add(X, X))))
532. mul(X, add(Y, mul(Y, add(Z, Z)))) = mul(Y, add(X, mul(add(X, X), Z)))
  (hard) mul(Z, mul(Y, add(Y, mul(Y, X)))) = mul(Y, mul(Y, add(Z, mul(X, Z))))
  (hard) mul(X, mul(Y, add(Z, mul(Z, W)))) = mul(Y, mul(Z, add(X, mul(W, X))))
533. mul(X, add(Y, mul(Z, mul(W, Y)))) = mul(Y, add(X, mul(Z, mul(X, W))))
534. mul(X, add(Y, mul(add(Z, Z), Y))) = mul(Y, add(X, mul(Z, add(X, X))))
535. mul(X, add(Y, mul(add(Z, Z), Y))) = mul(Y, add(X, mul(add(X, X), Z)))
  (hard) add(X, add(Y, mul(X, mul(Z, W)))) = add(X, add(mul(Z, mul(X, W)), Y))
  (hard) add(X, add(Y, mul(X, mul(Z, W)))) = add(X, add(mul(Z, mul(W, X)), Y))
536. mul(X, add(Y, mul(Y, mul(Z, W)))) = mul(Y, add(X, mul(Z, mul(W, X))))
  (hard) mul(add(one, one), mul(add(X, one), Y)) = mul(add(Y, Y), add(X, one))
  (hard) add(Z, add(Y, add(X, add(Z, Y)))) = add(Z, add(Z, add(Y, add(Y, X))))
537. mul(X, mul(Y, add(Z, add(Z, Z)))) = mul(Z, mul(X, add(Y, add(Y, Y))))
538. mul(X, mul(add(Y, add(Y, Y)), Z)) = mul(Y, mul(add(X, add(X, X)), Z))
539. mul(add(X, X), add(Y, add(Y, Y))) = mul(add(Y, Y), add(X, add(X, X)))
  (hard) mul(add(X, add(X, add(Y, Y))), Z) = mul(Z, add(X, add(Y, add(X, Y))))
540. mul(X, add(Y, mul(add(Y, Y), Z))) = mul(Y, add(X, mul(Z, add(X, X))))
  (hard) add(Z, add(Y, add(Z, add(Y, X)))) = add(X, add(Z, add(Z, add(Y, Y))))
541. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, add(X, add(X, add(X, X))))
542. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(X, add(Z, Y)))
543. add(one, neg(mul(add(X, one), add(X, one)))) -> neg(mul(X, add(X, add(one, one))))
  (hard) mul(add(X, X), add(one, Y)) = mul(X, mul(add(one, one), add(Y, one)))
544. mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(Y, mul(Y, mul(Z, add(X, X))))
545. mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(Y, mul(add(X, X), mul(Y, Z)))
546. mul(add(X, X), mul(Y, mul(Y, Z))) = mul(X, mul(Y, mul(Z, add(Y, Y))))
547. mul(add(X, X), mul(Y, mul(Y, Z))) = mul(X, mul(Z, mul(Y, add(Y, Y))))
548. mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(add(X, X), mul(Z, mul(Y, Y)))
549. mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(Y, mul(add(X, X), mul(Z, Y)))
550. mul(add(X, X), mul(Y, mul(Z, Y))) = mul(X, mul(Y, mul(Z, add(Y, Y))))
551. mul(add(X, X), mul(Y, mul(Z, Y))) = mul(X, mul(Z, mul(Y, add(Y, Y))))
552. mul(X, add(Y, add(Y, mul(Z, Y)))) = mul(add(X, add(X, mul(X, Z))), Y)
553. mul(add(X, add(X, mul(Y, X))), Z) = mul(X, add(Z, add(Z, mul(Z, Y))))
554. mul(X, add(Y, add(Y, mul(Z, Y)))) = mul(Y, add(X, add(X, mul(Z, X))))
555. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, add(X, mul(X, Z))))
556. add(mul(X, add(Y, Z)), mul(W, Y)) = add(mul(Y, add(W, X)), mul(X, Z))
557. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Y, add(Z, X)), mul(Z, W))
558. add(mul(X, add(Y, Z)), mul(Z, W)) = add(mul(Y, X), mul(Z, add(W, X)))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Z, mul(add(X, one), Y))
559. add(mul(X, add(Y, Z)), mul(Z, W)) = add(mul(X, Y), mul(Z, add(X, W)))
560. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(add(X, Z), Y), mul(Z, W))
561. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Y, add(X, Z)), mul(W, Z))
  (hard) add(Z, add(Y, mul(X, Y))) = add(mul(add(one, X), Y), Z)
562. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(X, add(Y, Z)), mul(W, Z))
563. add(mul(X, add(Y, Z)), mul(W, Y)) = add(mul(X, Z), mul(Y, add(X, W)))
  (hard) add(Z, add(mul(Z, Y), X)) = add(X, mul(add(one, Y), Z))
564. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(add(Z, Y), X), mul(W, Z))
565. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(X, add(Z, Y)), mul(Z, W))
  (hard) add(mul(X, mul(Y, Z)), add(W, X)) = add(X, add(mul(Y, mul(X, Z)), W))
  (hard) add(mul(X, add(Y, Y)), add(Z, X)) = add(X, add(mul(Y, add(X, X)), Z))
  (hard) add(mul(X, add(Y, Y)), add(Z, X)) = add(X, add(mul(add(X, X), Y), Z))
  (hard) add(mul(X, mul(Y, Z)), add(W, X)) = add(X, add(mul(Y, mul(Z, X)), W))
  (hard) add(Z, add(Y, add(X, add(Z, Y)))) = add(X, add(Z, add(Z, add(Y, Y))))
566. add(mul(X, Y), mul(Z, add(X, W))) = add(mul(W, Z), mul(X, add(Y, Z)))
567. add(mul(X, Y), mul(Z, add(X, W))) = add(mul(Z, W), mul(X, add(Z, Y)))
568. add(mul(X, Y), mul(Z, add(X, W))) = add(mul(W, Z), mul(add(Z, Y), X))
569. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Z, W), mul(add(Z, X), Y))
  (hard) add(Y, add(mul(W, mul(Z, Y)), X)) = add(X, add(Y, mul(W, mul(Y, Z))))
  (hard) add(X, mul(add(add(Z, Z), one), Y)) = add(X, add(Y, mul(Z, add(Y, Y))))
  (hard) add(X, mul(add(add(Z, Z), one), Y)) = add(X, add(Y, mul(add(Y, Y), Z)))
  (hard) add(Y, add(mul(W, mul(Z, Y)), X)) = add(X, add(Y, mul(W, mul(Z, Y))))
570. add(mul(X, Y), mul(Z, add(W, Y))) = add(mul(W, Z), mul(Y, add(X, Z)))
571. add(mul(X, Y), mul(Z, add(W, Y))) = add(mul(Z, W), mul(add(X, Z), Y))
572. add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(add(Y, Z), X))
573. add(mul(X, Y), mul(Z, add(W, Y))) = add(mul(W, Z), mul(add(Z, X), Y))
  (hard) add(mul(X, Y), add(Z, Y)) = add(Z, mul(add(one, X), Y))
574. add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(W, Z), mul(add(X, Z), Y))
  (hard) add(mul(X, mul(Y, Z)), add(W, Z)) = add(W, add(Z, mul(X, mul(Z, Y))))
  (hard) add(mul(add(X, X), Y), add(Z, Y)) = add(Z, add(Y, mul(X, add(Y, Y))))
  (hard) add(mul(add(X, X), Y), add(Z, Y)) = add(Z, add(Y, mul(add(Y, Y), X)))
  (hard) add(mul(X, mul(Y, Z)), add(W, Z)) = add(W, add(Z, mul(X, mul(Y, Z))))
  (hard) add(mul(X, add(Y, Y)), add(Z, X)) = add(Z, add(X, mul(Y, add(X, X))))
  (hard) add(mul(X, add(Y, Y)), add(Z, X)) = add(Z, add(X, mul(add(X, X), Y)))
  (hard) add(mul(X, mul(Y, Z)), add(W, X)) = add(W, add(X, mul(Y, mul(Z, X))))
575. add(mul(X, add(Y, Z)), mul(Z, W)) = add(mul(Z, W), mul(add(Y, Z), X))
  (hard) add(mul(X, Y), mul(Z, add(X, W))) = add(mul(X, Y), mul(add(W, X), Z))
576. add(X, mul(add(X, Y), mul(Z, W))) = add(X, mul(Z, mul(W, add(Y, X))))
  (hard) add(one, mul(add(one, X), Y)) = add(Y, add(one, mul(Y, X)))
577. add(mul(X, Y), mul(add(Y, Z), W)) = add(mul(W, add(Y, Z)), mul(X, Y))
  (hard) add(X, mul(add(one, Y), Z)) = add(Z, add(X, mul(Z, Y)))
  (hard) add(mul(X, Y), mul(add(Y, Z), W)) = add(mul(X, Y), mul(W, add(Z, Y)))
578. mul(X, add(Y, mul(Z, mul(Y, W)))) = mul(Y, add(X, mul(Z, mul(X, W))))
579. mul(X, add(Y, mul(Z, add(Y, Y)))) = mul(Y, add(X, mul(Z, add(X, X))))
580. mul(X, add(Y, mul(add(Y, Y), Z))) = mul(Y, add(X, mul(add(X, X), Z)))
  (hard) add(W, mul(Z, mul(Y, mul(W, X)))) = add(W, mul(W, mul(Z, mul(Y, X))))
  (hard) add(W, add(Z, mul(Z, mul(Y, X)))) = add(Z, add(W, mul(Y, mul(Z, X))))
  (hard) add(X, add(Y, mul(Z, mul(X, W)))) = add(X, add(mul(Z, mul(W, X)), Y))
  (hard) add(X, mul(Y, mul(Z, add(W, X)))) = add(X, mul(Y, mul(add(X, W), Z)))
  (hard) add(mul(X, mul(Y, Z)), add(W, Z)) = add(Z, add(W, mul(X, mul(Z, Y))))
  (hard) add(one, add(X, mul(add(one, one), Y))) = add(X, add(Y, add(Y, one)))
  (hard) add(X, add(Y, mul(add(X, X), Z))) = add(X, add(mul(add(Z, Z), X), Y))
581. mul(add(X, mul(Y, add(X, X))), Z) = mul(X, add(Z, mul(Z, add(Y, Y))))
582. mul(add(X, mul(Y, add(X, X))), Z) = mul(X, add(Z, mul(Y, add(Z, Z))))
  (hard) add(one, add(X, mul(Y, add(one, one)))) = add(X, add(Y, add(Y, one)))
  (hard) add(X, add(Y, mul(Z, add(X, X)))) = add(Y, add(X, mul(add(X, X), Z)))
  (hard) add(mul(Z, Y), mul(X, add(Y, Y))) = add(mul(X, add(Y, Y)), mul(Y, Z))
  (hard) mul(add(X, one), add(X, one)) = add(one, add(X, mul(X, add(X, one))))
583. add(X, add(X, neg(Y))) = add(Y, mul(add(one, one), add(X, neg(Y))))
584. mul(add(one, one), add(X, neg(one))) = add(X, add(X, neg(add(one, one))))
585. add(X, add(neg(add(one, one)), X)) = mul(add(one, one), add(X, neg(one)))
586. add(X, add(X, neg(add(one, one)))) = mul(add(one, one), add(neg(one), X))
587. add(neg(add(one, one)), add(X, X)) = mul(add(one, one), add(X, neg(one)))
588. add(mul(X, add(one, one)), neg(add(X, X))) -> zero
589. add(X, add(Y, add(Z, add(neg(X), W)))) -> add(Y, add(Z, W))
  (hard) add(neg(X), mul(Y, add(Z, X))) = add(neg(X), mul(add(X, Z), Y))
590. mul(X, add(Y, neg(mul(Y, Z)))) = mul(add(X, neg(mul(X, Z))), Y)
591. mul(X, add(Y, neg(mul(Y, Z)))) = mul(Y, add(X, neg(mul(Z, X))))
  (hard) add(Z, add(Y, neg(mul(X, Z)))) = add(Y, add(Z, neg(mul(Z, X))))
  (hard) add(X, add(Y, neg(mul(Z, X)))) = add(X, add(neg(mul(X, Z)), Y))
  (hard) add(X, add(neg(mul(X, Y)), Z)) = add(Z, add(X, neg(mul(X, Y))))
  (hard) add(X, add(neg(mul(Y, X)), Z)) = add(Z, add(X, neg(mul(X, Y))))
  (hard) add(neg(add(X, Y)), mul(Z, X)) = add(mul(X, Z), neg(add(X, Y)))
  (hard) add(neg(add(X, Y)), mul(Y, Z)) = add(mul(Y, Z), neg(add(Y, X)))
  (hard) add(neg(add(X, Y)), mul(Y, Z)) = add(mul(Z, Y), neg(add(Y, X)))
  (hard) add(neg(add(X, Y)), mul(Z, Y)) = add(mul(Y, Z), neg(add(Y, X)))
  (hard) add(neg(add(X, Y)), mul(Z, Y)) = add(mul(Z, Y), neg(add(Y, X)))
  (hard) add(neg(add(X, Y)), mul(X, Z)) = add(mul(Z, X), neg(add(X, Y)))
592. mul(add(one, add(one, add(one, one))), X) = add(X, add(X, add(X, X)))
593. mul(X, add(Y, add(Z, Z))) = add(mul(add(X, X), Z), mul(X, Y))
594. add(mul(add(X, X), Y), mul(Z, X)) = mul(X, add(Y, add(Y, Z)))
595. add(mul(X, add(Y, Y)), mul(Z, X)) -> mul(X, add(Y, add(Y, Z)))
596. add(mul(add(X, X), Y), mul(Z, X)) = mul(X, add(Z, add(Y, Y)))
  (hard) mul(add(X, Z), add(Y, Y)) = mul(Y, add(X, add(Z, add(Z, X))))
597. mul(X, add(Y, add(Y, Z))) = add(mul(add(X, X), Y), mul(X, Z))
  (hard) add(X, add(mul(Y, X), mul(Z, Y))) = add(X, mul(add(Z, X), Y))
598. add(mul(X, Y), mul(add(X, X), Z)) = mul(X, add(Z, add(Y, Z)))
599. add(mul(X, Y), mul(add(X, X), Z)) = mul(X, add(Y, add(Z, Z)))
  (hard) mul(X, add(Y, add(Y, Z))) = add(mul(Y, add(X, X)), mul(Z, X))
  (hard) add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(add(X, add(X, Z)), Y)
  (hard) mul(add(Z, add(Z, Y)), X) = add(mul(X, Y), mul(Z, add(X, X)))
  (hard) add(mul(X, Y), mul(Z, add(Y, Y))) = mul(add(Z, add(Z, X)), Y)
  (hard) add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(add(Z, add(X, X)), Y)
  (hard) mul(add(Z, add(Y, Y)), X) = add(mul(X, Z), mul(Y, add(X, X)))
  (hard) add(mul(X, Y), mul(Z, add(Y, Y))) = mul(add(X, add(Z, Z)), Y)
  (hard) add(X, mul(add(X, Y), Z)) = add(X, add(mul(Z, X), mul(Y, Z)))
  (hard) add(X, mul(add(Y, X), Z)) = add(X, add(mul(Z, Y), mul(X, Z)))
  (hard) mul(X, add(Y, add(Z, Z))) = add(mul(X, Z), mul(add(Z, Y), X))
  (hard) mul(X, add(Y, add(Z, Z))) = add(mul(X, Z), mul(add(Y, Z), X))
  (hard) add(mul(X, Y), mul(add(Z, Y), X)) = mul(X, add(Y, add(Y, Z)))
  (hard) add(mul(X, Y), mul(add(Y, Y), Z)) = mul(Y, add(Z, add(Z, X)))
  (hard) add(mul(X, Y), mul(add(Y, Y), Z)) = mul(add(Z, add(Z, X)), Y)
  (hard) mul(Y, add(Z, add(X, Z))) = add(mul(X, Y), mul(add(Y, Y), Z))
600. add(X, add(neg(mul(X, add(one, one))), Y)) -> add(Y, neg(X))
601. add(X, add(Y, neg(mul(X, Z)))) = add(Y, mul(add(one, neg(Z)), X))
602. add(X, add(Y, neg(mul(X, Z)))) = add(Y, mul(X, add(one, neg(Z))))
  (simplifying rules with respect to one another...)
  (delete rule 204)
  (delete rule 311)
  (delete rule 312)
  (delete rule 317)
  (delete rule 323)
  (delete rule 329)
  (delete rule 331)
  (delete rule 339)
  (delete rule 342)
  (delete rule 347)
  (delete rule 351)
  (delete rule 464)
  (delete rule 467)
  (delete rule 469)
  (delete rule 471)
  (delete rule 474)
  (delete rule 475)
  (delete rule 476)
  (delete rule 477)
  (delete rule 515)
  (delete rule 519)
  (delete rule 525)
  (delete rule 530)
  (delete rule 536)
  (delete rule 540)
  (delete rule 542)
  (delete rule 558)
  (delete rule 561)
  (delete rule 593)
  (delete rule 594)
  (delete rule 596)
  (delete rule 598)
  (delete rule 601)
603. add(neg(X), mul(add(X, X), Y)) = mul(X, add(Y, add(Y, neg(one))))
604. add(X, mul(X, add(Y, add(Y, neg(one))))) -> mul(add(X, X), Y)
605. add(X, add(X, neg(mul(X, Y)))) = mul(X, add(one, add(one, neg(Y))))
  (hard) add(X, add(neg(mul(Y, X)), Z)) = add(X, add(Z, neg(mul(X, Y))))
  (hard) add(neg(mul(X, Y)), add(Z, X)) = add(X, add(Z, neg(mul(X, Y))))
  (hard) add(neg(mul(X, Y)), add(Z, Y)) = add(Y, add(Z, neg(mul(Y, X))))
  (hard) add(X, neg(mul(add(Y, X), Z))) = add(X, neg(mul(Z, add(X, Y))))
606. mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(add(X, X), mul(Y, Z)))
607. mul(add(X, X), add(Y, mul(Y, Z))) -> mul(X, mul(add(Y, Y), add(one, Z)))
608. mul(add(X, X), add(Y, mul(Z, Y))) -> mul(X, mul(add(Y, Y), add(Z, one)))
609. mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(Y, mul(add(X, X), Z)))
610. mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(X, mul(add(Y, Y), Z)))
611. mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Z, mul(add(X, X), Y)))
612. mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Z, mul(add(Y, Y), X)))
613. mul(X, add(one, add(Y, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(X, Z))))
614. mul(X, add(Y, mul(Y, add(Z, one)))) = mul(Y, add(X, add(X, mul(Z, X))))
615. mul(X, add(Y, mul(Y, add(one, Z)))) = mul(Y, add(X, add(X, mul(Z, X))))
  (hard) mul(X, add(Y, mul(Y, add(Z, W)))) = mul(Y, mul(X, add(Z, add(W, one))))
616. mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(X, Z))))
  (hard) mul(W, mul(Z, add(Y, mul(X, Y)))) = mul(Y, mul(Z, add(W, mul(X, W))))
  (hard) mul(X, mul(X, add(Y, mul(Y, Z)))) = mul(Y, mul(X, add(X, mul(Z, X))))
  (hard) mul(X, mul(Y, add(Z, mul(Z, W)))) = mul(Z, mul(X, add(Y, mul(W, Y))))
617. mul(X, add(one, add(Y, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(Z, X))))
618. mul(X, add(one, add(Y, mul(Z, Y)))) = add(X, mul(Y, add(X, mul(X, Z))))
619. mul(X, add(Y, mul(add(one, Z), Y))) = mul(Y, add(X, add(X, mul(X, Z))))
620. mul(X, add(Y, mul(add(Z, one), Y))) = mul(Y, add(X, add(X, mul(Z, X))))
621. mul(X, add(Y, mul(add(one, Z), Y))) = mul(Y, add(X, add(X, mul(Z, X))))
  (hard) mul(X, add(Y, mul(add(Z, W), Y))) = mul(Y, mul(X, add(Z, add(W, one))))
622. mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(Z, X))))
623. mul(X, add(Y, add(one, mul(Z, Y)))) = add(X, mul(Y, add(X, mul(X, Z))))
  (hard) mul(X, add(Y, mul(X, mul(Z, Y)))) = mul(Y, mul(X, add(one, mul(X, Z))))
624. add(X, add(Y, add(Z, mul(X, W)))) = add(mul(X, add(one, W)), add(Y, Z))
  (hard) add(Z, add(Y, add(X, Z))) = add(Z, add(Z, add(Y, X)))
  (hard) add(Z, add(Y, add(X, Z))) = add(Y, add(X, add(Z, Z)))
625. add(mul(X, add(one, Y)), add(Z, W)) = add(X, add(Z, add(mul(X, Y), W)))
  (hard) add(Z, add(Y, add(Z, X))) = add(Y, add(X, add(Z, Z)))
626. add(X, add(mul(Y, add(one, Z)), W)) = add(Y, add(X, add(W, mul(Y, Z))))
  (hard) add(Z, add(Y, add(X, Z))) = add(Y, add(Z, add(Z, X)))
627. add(X, add(Y, mul(X, add(Z, W)))) = add(mul(X, add(Z, add(one, W))), Y)
628. add(mul(X, add(one, mul(Y, Z))), W) = add(X, add(W, mul(Y, mul(X, Z))))
629. add(mul(X, add(one, add(Y, Y))), Z) = add(X, add(Z, mul(Y, add(X, X))))
630. add(mul(X, add(one, add(Y, Y))), Z) = add(X, add(Z, mul(add(X, X), Y)))
  (hard) add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(add(one, add(W, Z)), X))
631. add(X, add(Y, mul(Z, add(one, W)))) = add(Z, add(X, add(Y, mul(Z, W))))
  (hard) add(X, add(one, add(Y, X))) = add(Y, add(X, add(X, one)))
632. add(X, add(Y, mul(add(one, Z), W))) = add(W, add(X, add(mul(W, Z), Y)))
  (hard) add(Z, add(Y, add(Z, X))) = add(Y, add(Z, add(X, Z)))
633. add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(X, add(Z, add(one, W))))
634. add(X, mul(Y, add(one, add(Z, Z)))) = add(Y, add(X, mul(Z, add(Y, Y))))
  (hard) add(Y, add(one, add(X, X))) = add(one, add(X, add(Y, X)))
  (hard) add(Y, add(one, add(X, X))) = add(one, add(X, add(X, Y)))
  (hard) add(Y, add(one, add(X, Y))) = add(X, add(one, add(Y, Y)))
635. add(X, mul(Y, add(one, add(Z, Z)))) = add(Y, add(X, mul(add(Y, Y), Z)))
  (hard) add(Y, add(one, add(Y, X))) = add(X, add(one, add(Y, Y)))
  (hard) add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(X, add(one, add(W, Z))))
636. mul(X, add(Y, add(Z, add(one, W)))) = add(X, mul(X, add(Y, add(Z, W))))
  (hard) mul(X, add(Y, mul(Y, add(Z, W)))) = mul(Y, mul(X, add(Z, add(one, W))))
  (hard) mul(X, add(Y, mul(add(Z, W), Y))) = mul(Y, mul(X, add(Z, add(one, W))))
  (hard) add(X, mul(X, add(Y, add(Z, W)))) = mul(X, add(Y, add(one, add(W, Z))))
  (hard) mul(X, mul(Y, add(Z, mul(Z, W)))) = mul(Y, mul(Z, mul(X, add(one, W))))
637. mul(X, mul(Y, add(one, add(Z, Z)))) = mul(Y, add(X, mul(Z, add(X, X))))
638. mul(X, mul(Y, add(one, add(Z, Z)))) = mul(Y, add(X, mul(add(X, X), Z)))
  (hard) mul(X, add(Y, mul(Y, add(Z, W)))) = mul(Y, mul(X, add(one, add(W, Z))))
639. mul(X, mul(add(one, add(Y, Y)), Z)) = mul(add(X, mul(Y, add(X, X))), Z)
640. mul(X, mul(add(one, add(Y, Y)), Z)) = mul(add(X, mul(add(X, X), Y)), Z)
641. add(X, mul(add(X, X), mul(Y, Z))) = mul(X, add(one, mul(Y, add(Z, Z))))
642. add(X, add(Y, mul(X, add(Z, W)))) = add(mul(add(Z, add(W, one)), X), Y)
643. add(X, mul(X, add(Y, add(Z, W)))) = mul(add(Y, add(Z, add(W, one))), X)
644. add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(add(Z, add(W, one)), X))
645. mul(add(X, add(Y, one)), mul(Z, W)) = mul(Z, add(W, mul(W, add(X, Y))))
646. mul(X, add(Y, neg(mul(Y, Z)))) = mul(add(one, neg(Z)), mul(X, Y))
647. mul(X, add(Y, mul(Y, add(Z, W)))) = mul(Y, mul(add(Z, add(W, one)), X))
  (hard) mul(X, add(Y, neg(mul(Y, Z)))) = mul(X, mul(Y, add(neg(Z), one)))
648. mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, add(Z, mul(Z, Y))))
  (hard) add(Z, mul(Y, mul(Z, X))) = mul(add(one, mul(X, Y)), Z)
649. mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, add(Z, mul(Y, Z))))
650. mul(X, add(Y, mul(add(Z, W), Y))) = mul(Y, mul(add(Z, add(W, one)), X))
  (hard) mul(X, add(Y, neg(mul(Z, Y)))) = mul(X, mul(Y, add(neg(Z), one)))
651. mul(add(X, add(one, mul(Y, X))), Z) = add(Z, mul(X, add(Z, mul(Z, Y))))
652. add(X, add(Y, mul(add(X, X), Z))) = add(mul(add(Z, add(Z, one)), X), Y)
  (hard) mul(add(X, add(X, one)), mul(Y, Z)) = mul(Y, mul(add(one, add(X, X)), Z))
653. add(X, add(Y, mul(Z, add(X, X)))) = add(Y, mul(add(Z, add(Z, one)), X))
654. mul(add(X, add(X, one)), mul(Y, Z)) = mul(Y, add(Z, mul(X, add(Z, Z))))
655. add(X, mul(add(Y, add(Z, W)), X)) = mul(X, add(Y, add(Z, add(W, one))))
656. add(X, add(Y, mul(add(Z, W), X))) = add(Y, mul(X, add(Z, add(W, one))))
657. add(X, add(Y, neg(mul(Z, X)))) = add(Y, mul(X, add(one, neg(Z))))
  (hard) add(neg(mul(X, Y)), add(Z, X)) = add(X, add(Z, neg(mul(Y, X))))
  (hard) add(X, neg(mul(add(X, Y), Z))) = add(X, neg(mul(Z, add(Y, X))))
  (hard) add(neg(mul(X, Y)), add(Z, Y)) = add(Y, add(Z, neg(mul(X, Y))))
658. add(X, add(Y, mul(Z, add(W, one)))) = add(Z, add(X, add(Y, mul(W, Z))))
659. add(X, add(Y, mul(add(Z, one), W))) = add(W, add(X, add(mul(Z, W), Y)))
660. add(mul(X, add(Y, one)), add(Z, W)) = add(X, add(Z, add(mul(Y, X), W)))
661. add(X, add(Y, mul(add(Z, W), X))) = add(mul(X, add(Z, add(W, one))), Y)
662. add(X, add(Y, add(Z, mul(W, X)))) = add(Y, add(mul(X, add(W, one)), Z))
663. add(mul(X, add(Y, one)), add(Z, W)) = add(Z, add(X, add(W, mul(Y, X))))
664. add(mul(X, add(Y, add(Y, one))), Z) = add(X, add(Z, mul(Y, add(X, X))))
  (hard) add(X, add(Y, mul(X, add(Z, W)))) = add(mul(X, add(W, add(one, Z))), Y)
  (hard) add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(X, add(W, add(one, Z))))
  (hard) add(X, mul(X, add(Y, add(Z, W)))) = mul(X, add(Y, add(W, add(one, Z))))
  (hard) mul(X, mul(Y, add(Z, mul(Z, W)))) = mul(Y, mul(X, mul(Z, add(W, one))))
  (hard) mul(X, mul(Y, mul(add(Z, one), W))) = mul(W, mul(X, add(Y, mul(Y, Z))))
665. mul(X, mul(Y, mul(add(Z, one), W))) = mul(Y, mul(add(Z, one), mul(W, X)))
666. mul(X, mul(add(one, mul(Y, Z)), W)) = mul(W, add(X, mul(Y, mul(X, Z))))
667. mul(X, mul(add(Y, add(Y, one)), Z)) = mul(Z, add(X, mul(Y, add(X, X))))
668. mul(X, mul(add(Y, add(Y, one)), Z)) = mul(Z, add(X, mul(add(X, X), Y)))
669. mul(add(X, mul(Y, mul(Z, X))), W) -> mul(X, mul(add(one, mul(Y, Z)), W))
670. add(X, mul(Y, mul(Z, mul(W, X)))) = mul(X, add(one, mul(Y, mul(Z, W))))
671. add(X, add(Y, mul(Z, mul(W, X)))) = add(Y, mul(X, add(one, mul(Z, W))))
672. mul(X, mul(Y, add(one, mul(Z, W)))) = mul(Y, add(X, mul(Z, mul(W, X))))
673. add(X, mul(Y, mul(add(Z, Z), X))) = mul(X, add(one, mul(add(Y, Y), Z)))
674. add(X, mul(add(Y, Y), mul(Z, X))) = mul(X, add(one, mul(Y, add(Z, Z))))
  (hard) mul(X, add(Y, mul(Y, mul(Z, X)))) = mul(Y, mul(X, add(one, mul(X, Z))))
  (hard) mul(X, add(Y, mul(Z, mul(X, Y)))) = mul(Y, mul(X, add(one, mul(X, Z))))
675. mul(X, add(one, add(Y, add(Y, Y)))) = add(X, mul(Y, add(X, add(X, X))))
676. mul(X, add(Y, add(Y, add(Y, one)))) = add(X, mul(Y, add(X, add(X, X))))
  (hard) mul(X, add(Y, add(one, add(Y, Y)))) = add(X, mul(Y, add(X, add(X, X))))
677. mul(add(X, add(X, add(X, one))), Y) = add(Y, mul(X, add(Y, add(Y, Y))))
678. add(X, mul(Y, mul(Z, add(X, X)))) = mul(add(one, mul(Y, add(Z, Z))), X)
  (hard) add(W, mul(W, mul(add(Z, X), Y))) = mul(add(one, mul(Y, add(X, Z))), W)
679. add(X, mul(Y, mul(Z, add(X, X)))) = mul(add(one, mul(Z, add(Y, Y))), X)
  (hard) add(X, mul(add(Y, W), mul(Z, X))) = mul(X, add(one, mul(Z, add(W, Y))))
  (hard) mul(add(X, X), add(Y, Z)) = add(mul(add(X, X), Z), mul(Y, add(X, X)))
680. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, mul(X, add(Z, one))))
681. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, mul(add(Z, one), X)))
  (hard) add(X, mul(X, mul(Z, add(Y, W)))) = mul(X, add(one, mul(Z, add(W, Y))))
  (hard) add(W, mul(W, mul(Y, add(Z, X)))) = mul(add(one, mul(Y, add(X, Z))), W)
  (hard) add(X, mul(Z, mul(add(Y, W), X))) = mul(X, add(one, mul(Z, add(W, Y))))
  (hard) add(X, mul(add(add(one, Z), one), Y)) = add(X, add(Y, add(Y, mul(Y, Z))))
  (hard) add(mul(X, add(one, Y)), add(Z, X)) = add(Z, add(X, add(X, mul(X, Y))))
682. add(mul(X, add(Y, add(Z, one))), W) = add(X, add(mul(X, add(Y, Z)), W))
683. add(X, add(neg(mul(X, Y)), Z)) = add(Z, mul(add(one, neg(Y)), X))
  (hard) add(neg(mul(X, Y)), add(Z, X)) = add(X, add(neg(mul(X, Y)), Z))
  (hard) add(neg(mul(X, Y)), add(Z, Y)) = add(Y, add(neg(mul(Y, X)), Z))
  (hard) add(X, mul(X, add(Y, add(Z, W)))) = mul(X, add(Z, add(W, add(one, Y))))
684. add(X, mul(add(Y, add(Z, W)), X)) = mul(X, add(Z, add(W, add(one, Y))))
  (hard) mul(add(X, X), add(Y, one)) = add(X, mul(add(one, add(Y, Y)), X))
685. add(X, mul(Y, add(Z, add(W, one)))) = add(Y, add(mul(Y, add(Z, W)), X))
686. add(X, add(neg(mul(X, Y)), Z)) = add(Z, mul(X, add(one, neg(Y))))
687. mul(add(X, mul(Y, mul(X, Z))), W) -> mul(X, mul(W, add(one, mul(Y, Z))))
  (hard) mul(X, mul(Y, add(one, mul(Z, W)))) = mul(Y, add(X, mul(X, mul(W, Z))))
688. mul(add(X, mul(Y, add(X, X))), Z) = mul(X, mul(Z, add(Y, add(Y, one))))
689. mul(add(X, mul(add(X, X), Y)), Z) = mul(X, mul(Z, add(Y, add(Y, one))))
690. mul(add(one, mul(X, mul(Y, Z))), W) = add(W, mul(X, mul(Y, mul(W, Z))))
  (hard) mul(X, add(one, mul(Y, mul(Z, W)))) = add(X, mul(Y, mul(X, mul(W, Z))))
691. add(X, mul(Y, add(one, mul(Z, W)))) = add(Y, add(mul(Z, mul(Y, W)), X))
692. add(X, add(Y, mul(Z, mul(X, W)))) = add(Y, mul(add(one, mul(Z, W)), X))
  (hard) add(X, mul(Y, add(one, mul(Z, W)))) = add(Y, add(X, mul(Y, mul(W, Z))))
693. add(X, mul(add(X, X), mul(Y, Z))) = mul(X, add(one, mul(Z, add(Y, Y))))
694. mul(add(one, mul(X, add(Y, Y))), Z) = add(Z, mul(Y, mul(Z, add(X, X))))
695. mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(X, mul(Z, add(Y, Y))))
696. add(X, mul(Y, mul(Z, add(X, X)))) = mul(add(one, mul(add(Y, Y), Z)), X)
697. mul(add(one, mul(add(X, X), Y)), Z) = add(Z, mul(X, mul(Z, add(Y, Y))))
  (hard) mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(X, mul(add(Z, Z), Y)))
698. mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(X, mul(Y, add(Z, Z))))
699. mul(add(one, mul(X, add(Y, Y))), Z) = add(Z, mul(add(X, X), mul(Z, Y)))
  (hard) add(X, mul(Y, add(Z, add(Z, one)))) = add(Y, add(X, mul(add(Y, Y), Z)))
700. add(mul(X, add(Y, add(Y, one))), Z) = add(X, add(mul(add(X, X), Y), Z))
701. add(X, mul(Y, add(Z, add(Z, one)))) = add(Y, add(mul(Z, add(Y, Y)), X))
  (hard) add(X, add(X, add(one, Y))) = add(Y, add(X, add(X, one)))
  (hard) mul(X, add(Y, add(Z, add(Z, one)))) = add(X, mul(X, add(Z, add(Z, Y))))
  (hard) mul(X, add(Y, add(Z, add(Z, one)))) = add(X, mul(X, add(Z, add(Y, Z))))
  (hard) mul(Y, add(X, add(Z, add(Z, one)))) = add(Y, mul(add(Z, add(X, Z)), Y))
702. mul(X, add(Y, mul(Y, add(one, Z)))) = mul(add(X, add(X, mul(X, Z))), Y)
703. mul(X, add(one, add(Y, mul(Z, Y)))) = add(X, mul(Y, add(X, mul(Z, X))))
  (hard) mul(add(X, Z), add(Y, one)) = add(X, add(Z, mul(Y, add(Z, X))))
704. add(X, mul(Y, add(X, mul(Z, X)))) = mul(X, add(Y, add(one, mul(Z, Y))))
  (hard) add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(one, add(Y, mul(Z, Y))))
705. add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(Y, add(one, mul(Z, Y))))
706. add(mul(X, Y), neg(mul(X, Z))) -> mul(X, add(Y, neg(Z)))
707. add(X, mul(Y, mul(X, add(one, Z)))) = mul(X, add(one, add(Y, mul(Z, Y))))
708. add(X, mul(add(Y, one), mul(X, Z))) = mul(X, add(one, add(Z, mul(Y, Z))))
709. add(X, mul(Y, mul(add(one, Z), X))) = mul(X, add(Y, add(one, mul(Y, Z))))
710. add(X, mul(Y, mul(add(one, Z), X))) = mul(add(Y, add(one, mul(Y, Z))), X)
  (hard) mul(add(X, add(one, X)), Y) = add(Y, mul(add(Y, Y), X))
  (hard) add(Z, mul(Y, mul(X, Z))) = mul(add(one, mul(X, Y)), Z)
711. add(X, mul(add(one, mul(X, Y)), Z)) = add(Z, mul(X, add(one, mul(Z, Y))))
  (hard) add(Y, add(one, mul(Y, X))) = add(one, mul(add(X, one), Y))
712. add(X, mul(add(one, add(X, X)), Y)) = add(Y, mul(X, add(Y, add(Y, one))))
713. add(X, mul(X, mul(Y, add(one, Z)))) = mul(X, add(Y, add(one, mul(Y, Z))))
714. add(X, mul(Y, add(one, mul(X, Z)))) <-> add(Y, mul(X, add(one, mul(Y, Z))))
715. add(X, mul(Y, mul(X, add(one, Z)))) = mul(X, add(Y, add(one, mul(Y, Z))))
716. add(X, mul(Y, add(one, add(X, X)))) = add(Y, mul(X, add(Y, add(Y, one))))
  (hard) mul(X, mul(Y, add(Z, add(W, one)))) = mul(Y, mul(X, add(Z, add(one, W))))
  (hard) mul(X, mul(Y, add(one, add(Z, W)))) = mul(Y, mul(X, add(Z, add(W, one))))
  (hard) mul(X, mul(Y, add(one, add(Z, W)))) = mul(Y, mul(X, add(Z, add(one, W))))
  (hard) add(X, mul(add(one, add(X, X)), Y)) = add(Y, mul(X, add(one, add(Y, Y))))
  (hard) add(Y, mul(X, add(one, add(Y, Y)))) = add(X, mul(Y, add(one, add(X, X))))
717. mul(add(X, add(Y, one)), mul(Z, W)) = mul(Z, mul(W, add(X, add(Y, one))))
718. mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, mul(Z, add(Y, one))))
719. mul(X, mul(Y, add(Z, add(W, one)))) = mul(Y, mul(add(Z, add(W, one)), X))
720. mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, mul(Z, add(one, Y))))
  (hard) mul(X, mul(Y, add(one, add(Z, W)))) = mul(Y, mul(add(Z, add(W, one)), X))
  (hard) add(X, mul(add(one, add(X, X)), Y)) = add(Y, mul(add(Y, add(Y, one)), X))
  (hard) add(Y, mul(X, add(one, add(Y, Y)))) = add(X, mul(add(X, add(X, one)), Y))
721. mul(X, add(Y, add(one, mul(Z, Y)))) = add(X, mul(Y, mul(add(Z, one), X)))
722. add(mul(add(X, one), mul(Y, Z)), W) = add(W, mul(Z, mul(add(X, one), Y)))
723. add(X, mul(Y, mul(X, add(Z, one)))) = mul(X, add(Y, add(one, mul(Z, Y))))
  (hard) mul(X, mul(Y, mul(Z, add(W, one)))) = mul(Z, mul(X, add(Y, mul(W, Y))))
724. mul(add(X, add(one, mul(Y, X))), Z) = add(Z, mul(X, mul(Z, add(Y, one))))
725. add(X, mul(Y, mul(add(Z, one), X))) = mul(add(Y, add(one, mul(Z, Y))), X)
726. mul(X, add(one, add(Y, mul(Y, Z)))) = add(X, mul(Y, mul(add(Z, one), X)))
727. mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, mul(add(Z, one), X)))
  (hard) mul(X, mul(add(Y, add(Z, one)), W)) = mul(W, mul(X, add(Y, add(one, Z))))
728. mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, mul(add(Y, one), Z)))
  (hard) mul(W, mul(add(Z, add(Y, one)), X)) = mul(X, mul(add(Z, add(Y, one)), W))
729. add(X, mul(Y, mul(add(one, Z), X))) = mul(X, add(one, add(Y, mul(Y, Z))))
730. add(X, mul(Y, mul(add(Z, one), X))) = mul(X, add(one, add(Y, mul(Z, Y))))
731. add(X, mul(Y, mul(add(one, Z), X))) = mul(X, add(one, add(Y, mul(Z, Y))))
732. add(X, mul(add(Y, one), mul(Z, X))) = mul(X, add(one, add(Z, mul(Y, Z))))
  (hard) mul(Z, mul(Y, add(one, mul(X, Z)))) = mul(Y, mul(Z, add(one, mul(Z, X))))
733. add(X, mul(add(one, mul(Y, X)), Z)) = add(Z, mul(X, add(one, mul(Z, Y))))
734. add(X, mul(Y, add(one, mul(Z, X)))) <-> add(Y, mul(X, add(one, mul(Y, Z))))
  (hard) mul(X, mul(add(one, mul(Y, X)), Z)) = mul(Z, mul(X, add(one, mul(X, Y))))
735. add(X, add(Y, mul(add(X, one), Z))) = add(Z, add(mul(X, add(Z, one)), Y))
  (hard) add(X, add(Y, add(X, one))) = add(one, add(X, add(X, Y)))
  (hard) add(X, add(Y, add(X, one))) = add(one, add(X, add(Y, X)))
  (hard) add(X, add(X, add(one, Y))) = add(X, add(Y, add(X, one)))
  (hard) add(X, add(one, add(Y, X))) = add(X, add(Y, add(X, one)))
  (hard) add(Y, add(Y, add(one, X))) = add(X, add(one, add(Y, Y)))
  (hard) add(Y, add(X, add(Y, one))) = add(one, add(X, add(Y, Y)))
736. add(X, mul(Y, add(one, neg(X)))) = add(Y, mul(add(one, neg(Y)), X))
737. add(neg(X), mul(Y, add(X, one))) = add(Y, mul(X, add(Y, neg(one))))
  (hard) add(neg(one), add(X, add(X, one))) = add(X, X)
738. add(X, add(Y, mul(Z, add(X, one)))) = add(Y, add(Z, mul(X, add(Z, one))))
739. mul(X, mul(add(Y, Y), add(Z, one))) = mul(Y, mul(X, add(Z, add(one, add(Z, one)))))
  (hard) mul(X, mul(add(Y, Y), add(Z, one))) = mul(add(X, X), mul(Y, add(one, Z)))
  (hard) add(mul(X, add(Y, add(Z, one))), W) = add(W, mul(X, add(one, add(Y, Z))))
  (hard) mul(X, add(one, add(Y, add(Z, W)))) = mul(X, add(Y, add(Z, add(W, one))))
  (hard) mul(X, add(Y, add(Z, add(W, one)))) = mul(X, add(Y, add(one, add(Z, W))))
  (hard) add(X, mul(Y, add(Z, add(W, one)))) = add(mul(Y, add(one, add(Z, W))), X)
  (hard) add(Z, mul(Y, add(one, mul(X, Z)))) = add(Y, mul(Z, add(one, mul(X, Y))))
  (hard) mul(X, add(Y, mul(Y, add(one, X)))) = mul(Y, add(X, mul(X, add(X, one))))
740. add(X, mul(Y, mul(X, add(Z, one)))) = mul(add(one, add(Y, mul(Y, Z))), X)
741. add(X, mul(X, mul(Y, add(Z, one)))) = mul(X, add(one, add(Y, mul(Y, Z))))
  (hard) add(X, mul(X, mul(Y, add(Z, one)))) = mul(X, add(Y, add(one, mul(Y, Z))))
  (hard) mul(X, add(Y, mul(Y, add(one, Z)))) = mul(Y, add(X, mul(X, add(Z, one))))
742. mul(add(one, add(X, mul(X, Y))), Z) = add(Z, mul(X, mul(Z, add(one, Y))))
  (hard) add(X, mul(X, mul(Y, add(one, Z)))) = mul(X, add(one, add(Y, mul(Y, Z))))
743. mul(add(one, add(X, mul(Y, X))), Z) = add(Z, mul(X, mul(Z, add(Y, one))))
744. add(X, mul(X, mul(Y, add(Z, one)))) = mul(X, add(one, add(Y, mul(Z, Y))))
745. add(neg(mul(X, Y)), mul(add(X, Z), Y)) -> mul(Y, Z)
746. add(neg(mul(X, Y)), mul(Z, Y)) -> mul(Y, add(Z, neg(X)))
  (hard) add(X, add(X, mul(X, add(Y, add(one, Y))))) = add(X, mul(add(X, X), add(Y, one)))
747. add(X, mul(add(Y, add(Z, neg(one))), X)) -> mul(X, add(Y, Z))
748. add(X, mul(X, add(Y, add(neg(one), Z)))) -> mul(X, add(Y, Z))
749. add(X, mul(add(Y, add(neg(one), Z)), X)) -> mul(X, add(Y, Z))
750. add(X, add(Y, mul(X, add(Z, neg(one))))) -> add(Y, mul(X, Z))
751. add(X, add(Y, mul(X, add(neg(one), Z)))) -> add(Y, mul(X, Z))
752. add(neg(X), add(mul(X, add(Y, one)), Z)) -> add(Z, mul(X, Y))
753. add(neg(X), mul(X, add(Y, add(Z, one)))) -> mul(X, add(Y, Z))
754. add(neg(X), add(Y, mul(X, add(Z, one)))) -> add(Y, mul(X, Z))
755. add(neg(X), add(Y, mul(X, Z))) = add(mul(X, add(Z, neg(one))), Y)
  (hard) add(neg(X), add(Y, add(Z, X))) = add(Y, Z)
  (hard) add(Y, Z) = add(neg(X), add(Y, add(Z, X)))
  (hard) add(Y, Z) = add(neg(X), add(Y, add(Z, X)))
  (hard) add(Y, Z) = add(Y, add(neg(X), add(Z, X)))
756. mul(add(neg(X), mul(X, Y)), Z) -> mul(X, mul(Z, add(neg(one), Y)))
757. add(neg(X), add(Y, mul(X, Z))) = add(Y, mul(X, add(Z, neg(one))))
  (hard) add(Y, Z) = add(Y, add(neg(X), add(Z, X)))
758. mul(X, mul(Y, add(Z, neg(one)))) = mul(Y, add(neg(X), mul(X, Z)))
759. mul(X, add(neg(one), mul(Y, Z))) = add(neg(X), mul(Y, mul(X, Z)))
760. mul(X, add(Y, add(Y, neg(one)))) = add(neg(X), mul(Y, add(X, X)))
761. add(neg(X), mul(add(Y, Z), X)) = mul(X, add(Y, add(Z, neg(one))))
762. mul(add(neg(X), mul(Y, X)), Z) -> mul(X, mul(Z, add(neg(one), Y)))
763. add(neg(X), add(Y, mul(Z, X))) = add(Y, mul(X, add(Z, neg(one))))
  (hard) add(Y, Z) = add(Y, add(neg(X), add(Z, X)))
764. mul(X, mul(Y, add(Z, neg(one)))) = mul(Y, add(neg(X), mul(Z, X)))
765. mul(X, add(one, neg(add(Y, Y)))) = add(X, neg(mul(Y, add(X, X))))
766. add(neg(X), mul(X, add(Y, add(Y, one)))) -> mul(Y, add(X, X))
767. add(neg(X), mul(X, add(Y, Z))) = mul(X, add(Z, add(neg(one), Y)))
  (hard) mul(X, add(one, add(Y, Z))) = mul(X, add(Z, add(one, Y)))
  (hard) mul(X, add(one, add(Y, Z))) = mul(X, add(Z, add(Y, one)))
768. add(neg(X), mul(X, add(Y, Z))) = mul(X, add(Y, add(neg(one), Z)))
769. add(neg(X), mul(X, add(Y, Z))) = mul(add(Y, add(Z, neg(one))), X)
770. mul(add(one, neg(mul(X, Y))), Z) = add(Z, neg(mul(X, mul(Z, Y))))
  (hard) mul(X, add(one, neg(mul(Y, Z)))) = add(X, neg(mul(X, mul(Z, Y))))
771. mul(X, add(one, neg(mul(Y, Z)))) = add(X, neg(mul(Y, mul(Z, X))))
772. add(X, neg(mul(Y, mul(Z, X)))) = mul(add(one, neg(mul(Y, Z))), X)
773. mul(X, mul(Y, add(one, neg(Z)))) = mul(add(Y, neg(mul(Y, Z))), X)
774. add(mul(X, add(one, neg(Y))), Z) = add(X, add(Z, neg(mul(X, Y))))
775. add(X, add(Y, neg(mul(X, Z)))) = add(Y, mul(add(one, neg(Z)), X))
776. add(X, neg(mul(Y, add(X, X)))) = mul(add(one, neg(add(Y, Y))), X)
777. add(mul(add(one, neg(X)), Y), Z) = add(Y, add(Z, neg(mul(Y, X))))
778. add(neg(X), mul(add(X, X), Y)) = mul(X, add(Y, add(neg(one), Y)))
779. add(X, mul(X, add(Y, add(neg(one), Y)))) -> mul(add(X, X), Y)
780. add(neg(X), mul(add(X, X), Y)) = mul(X, add(neg(one), add(Y, Y)))
781. mul(add(X, neg(mul(X, Y))), Z) -> mul(add(one, neg(Y)), mul(Z, X))
782. mul(add(one, neg(X)), mul(Y, Z)) = mul(Y, add(Z, neg(mul(X, Z))))
783. mul(X, add(Y, neg(mul(Y, Z)))) = mul(add(one, neg(Z)), mul(Y, X))
  (simplifying rules with respect to one another...)
  (delete rule 314)
  (delete rule 352)
  (delete rule 358)
  (delete rule 359)
  (delete rule 368)
  (delete rule 372)
  (delete rule 382)
  (delete rule 386)
  (delete rule 388)
  (delete rule 391)
  (delete rule 473)
  (delete rule 481)
  (delete rule 482)
  (delete rule 485)
  (delete rule 486)
  (delete rule 590)
  (delete rule 603)
  (delete rule 618)
  (delete rule 623)
  (delete rule 624)
  (delete rule 639)
  (delete rule 640)
  (delete rule 647)
  (delete rule 650)
  (delete rule 705)
  (delete rule 711)
  (delete rule 712)
  (delete rule 713)
  (delete rule 721)
  (delete rule 726)
  (delete rule 733)
  (delete rule 745)
  (delete rule 767)
  (delete rule 770)
  (delete rule 773)
784. add(X, add(neg(mul(Y, X)), Z)) = add(Z, mul(add(one, neg(Y)), X))
785. add(mul(X, add(one, neg(Y))), Z) = add(X, add(Z, neg(mul(Y, X))))
786. add(X, mul(Y, add(one, neg(Z)))) = add(Y, add(neg(mul(Z, Y)), X))
787. add(X, add(Y, neg(mul(Z, X)))) = add(Y, mul(add(one, neg(Z)), X))
788. add(X, neg(mul(add(X, X), Y))) = mul(X, add(one, neg(add(Y, Y))))
789. add(mul(add(one, neg(X)), Y), Z) = add(Y, add(Z, neg(mul(X, Y))))
790. add(mul(add(one, neg(X)), Y), Z) = add(Y, add(neg(mul(Y, X)), Z))
  (hard) add(X, mul(X, add(Z, neg(Y)))) = add(X, add(neg(mul(X, Y)), mul(Z, X)))
  (hard) add(neg(mul(X, Y)), mul(add(one, neg(Y)), X)) = add(X, neg(mul(X, add(Y, Y))))
791. add(mul(X, add(one, neg(Y))), Z) = add(X, add(neg(mul(X, Y)), Z))
792. add(neg(X), mul(add(X, X), Y)) = mul(X, add(Y, add(Y, neg(one))))
793. add(mul(X, add(Y, Y)), mul(Z, add(X, X))) -> mul(add(X, X), add(Z, Y))
  (hard) mul(X, add(Y, add(Z, add(Z, Y)))) = mul(add(X, X), add(Y, Z))
794. add(mul(X, add(Y, Y)), mul(Y, add(Z, Z))) -> mul(add(X, Z), add(Y, Y))
795. add(mul(X, add(Y, Y)), mul(add(X, X), Z)) -> mul(add(X, X), add(Y, Z))
796. mul(add(one, one), add(X, neg(Y))) = add(X, add(X, neg(add(Y, Y))))
797. add(X, add(neg(add(Y, Y)), X)) = mul(add(one, one), add(X, neg(Y)))
798. add(X, add(X, neg(add(Y, Y)))) = mul(add(one, one), add(neg(Y), X))
799. add(neg(add(X, X)), add(Y, Y)) = mul(add(one, one), add(Y, neg(X)))
800. add(neg(X), mul(add(X, one), Y)) = add(Y, mul(X, add(Y, neg(one))))
801. add(X, add(Y, mul(X, add(Y, neg(one))))) -> mul(add(X, one), Y)
802. mul(X, mul(Y, add(Z, neg(one)))) = mul(add(neg(one), Z), mul(X, Y))
803. add(neg(X), mul(add(one, X), Y)) = add(Y, mul(X, add(Y, neg(one))))
804. add(neg(X), mul(Y, add(one, X))) = add(Y, mul(X, add(Y, neg(one))))
805. add(mul(X, Y), mul(Z, add(one, Y))) = add(Z, mul(Y, add(X, Z)))
806. add(mul(add(X, one), Y), mul(X, Z)) = add(Y, mul(X, add(Y, Z)))
  (hard) mul(add(one, one), add(X, Y)) = add(Y, add(X, add(Y, X)))
807. add(mul(add(X, one), Y), mul(Z, X)) = add(Y, mul(X, add(Y, Z)))
808. add(mul(add(X, one), Y), mul(Z, X)) = add(Y, mul(add(Z, Y), X))
  (hard) add(mul(X, Y), mul(Z, add(one, Y))) = add(Z, mul(Y, add(Z, X)))
809. add(X, mul(Y, add(X, Z))) = add(mul(add(one, Y), X), mul(Z, Y))
810. add(X, mul(Y, add(X, Z))) = add(mul(X, add(one, Y)), mul(Y, Z))
811. add(X, mul(Y, add(X, Z))) = add(mul(Z, Y), mul(add(Y, one), X))
812. add(X, mul(Y, add(X, Z))) = add(mul(Y, Z), mul(add(Y, one), X))
813. add(mul(add(X, one), Y), mul(X, Z)) = add(Y, mul(X, add(Z, Y)))
814. add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(X, add(Y, one)))
815. add(X, mul(Y, add(Z, X))) = add(mul(Z, Y), mul(X, add(Y, one)))
816. add(mul(add(one, X), Y), mul(Z, X)) = add(Y, mul(X, add(Z, Y)))
817. add(mul(X, Y), mul(add(one, Y), Z)) = add(Z, mul(Y, add(Z, X)))
818. add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(X, add(one, Y)))
819. add(X, mul(Y, add(Z, X))) = add(mul(add(one, Y), X), mul(Y, Z))
820. add(X, mul(Y, add(X, Z))) = add(mul(Y, Z), mul(add(one, Y), X))
821. add(X, mul(add(X, Y), Z)) = add(mul(add(Z, one), X), mul(Z, Y))
822. add(X, mul(add(X, Y), Z)) = add(mul(add(Z, one), X), mul(Y, Z))
823. add(X, mul(X, add(one, X))) = add(neg(one), mul(add(X, one), add(X, one)))
  (hard) add(neg(add(X, add(X, one))), Y) = add(Y, neg(add(one, add(X, X))))
824. add(neg(mul(X, Y)), mul(add(Z, one), X)) -> add(X, mul(X, add(Z, neg(Y))))
825. add(neg(mul(X, Y)), mul(add(one, Z), X)) -> add(X, mul(X, add(Z, neg(Y))))
826. add(X, mul(Y, add(one, neg(X)))) <-> add(Y, mul(X, add(one, neg(Y))))
827. add(neg(X), mul(Y, add(X, one))) = add(Y, mul(X, add(neg(one), Y)))
828. mul(X, mul(Y, add(one, add(Z, one)))) = mul(Y, add(X, add(X, mul(Z, X))))
829. mul(X, add(one, mul(Y, add(one, Z)))) = add(X, mul(Y, add(X, mul(Z, X))))
830. add(X, add(X, add(Y, add(Y, Z)))) = add(mul(add(X, Y), add(one, one)), Z)
831. mul(add(X, add(Y, add(X, Y))), Z) -> mul(add(X, Y), mul(add(one, one), Z))
  (hard) neg(mul(add(one, one), mul(X, Y))) = neg(mul(add(Y, Y), X))
832. mul(X, add(mul(add(one, one), Y), Z)) = add(mul(add(X, X), Y), mul(X, Z))
  (hard) add(mul(add(X, X), Y), mul(X, Z)) = mul(X, add(Y, add(Z, Y)))
  (hard) add(mul(add(X, X), Y), mul(X, Z)) = mul(X, add(Z, add(Y, Y)))
833. mul(X, add(Y, mul(add(one, one), Z))) = add(mul(X, Y), mul(add(X, X), Z))
  (hard) add(mul(X, Y), mul(add(X, X), Z)) = mul(X, add(Z, add(Y, Z)))
834. mul(add(X, add(X, add(Y, Y))), Z) -> mul(add(X, Y), mul(add(one, one), Z))
835. mul(X, add(Y, mul(add(one, one), Z))) = add(mul(Y, X), mul(add(X, X), Z))
  (hard) add(neg(mul(X, Y)), mul(add(Y, Y), X)) = mul(X, Y)
836. mul(X, add(mul(Y, add(one, one)), Z)) = add(mul(Y, add(X, X)), mul(X, Z))
837. mul(X, add(mul(Y, add(one, one)), Z)) = add(mul(add(X, X), Y), mul(X, Z))
838. mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(X, Y), mul(add(X, X), Z))
  (hard) add(mul(X, Y), mul(add(X, X), Z)) = mul(add(Z, add(Z, Y)), X)
  (hard) mul(add(Z, add(Y, Y)), X) = add(mul(X, Z), mul(add(X, X), Y))
839. mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(Y, X), mul(add(X, X), Z))
  (hard) add(mul(X, Y), mul(add(Y, Y), X)) = mul(add(Y, add(Y, Y)), X)
  (hard) mul(add(X, add(Y, Y)), Z) = add(mul(X, Z), mul(add(Z, Z), Y))
  (hard) mul(Y, add(X, add(X, add(X, X)))) = mul(X, mul(add(Y, Y), add(one, one)))
840. mul(add(one, one), mul(add(X, X), Y)) = mul(X, add(Y, add(Y, add(Y, Y))))
841. add(mul(X, add(one, add(one, Y))), Z) = add(X, add(Z, add(X, mul(X, Y))))
842. add(mul(X, add(one, add(Y, one))), Z) = add(X, add(Z, add(X, mul(Y, X))))
843. add(mul(X, add(one, add(one, Y))), Z) = add(X, add(Z, add(X, mul(Y, X))))
844. add(X, mul(Y, add(one, add(one, Z)))) = add(Y, add(X, add(Y, mul(Y, Z))))
845. add(X, mul(Y, add(one, add(Z, one)))) = add(Y, add(X, add(Y, mul(Z, Y))))
846. add(X, mul(Y, add(one, add(one, Z)))) = add(Y, add(X, add(Y, mul(Z, Y))))
847. add(mul(X, add(one, add(one, Y))), Z) = add(X, add(X, add(Z, mul(X, Y))))
848. add(X, mul(Y, add(one, add(one, Z)))) = add(Y, add(Y, add(X, mul(Y, Z))))
849. mul(X, add(one, add(Y, add(one, Z)))) = add(X, add(X, mul(X, add(Y, Z))))
850. mul(X, add(Y, add(one, add(Z, one)))) = add(X, add(X, mul(X, add(Y, Z))))
851. add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(one, mul(add(one, Z), Y)))
852. add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(one, mul(Y, add(one, Z))))
853. mul(X, add(Y, add(one, add(one, Z)))) = add(X, add(X, mul(X, add(Y, Z))))
854. mul(X, mul(Y, add(one, add(one, Z)))) = mul(Y, add(X, add(X, mul(X, Z))))
855. mul(X, add(Y, neg(mul(Y, Z)))) = mul(Y, mul(X, add(one, neg(Z))))
856. mul(X, add(Y, neg(mul(Y, Z)))) = mul(Y, add(X, neg(mul(X, Z))))
857. mul(X, mul(Y, add(one, neg(Z)))) = mul(Y, add(X, neg(mul(Z, X))))
858. mul(add(X, neg(mul(X, Y))), Z) -> mul(X, mul(Z, add(one, neg(Y))))
859. mul(X, mul(Y, add(one, add(one, Z)))) = mul(Y, add(X, add(X, mul(Z, X))))
  (hard) mul(X, add(one, add(one, add(Y, Y)))) = mul(add(X, X), add(Y, one))
860. mul(add(X, add(Y, add(one, one))), Z) = add(Z, add(Z, mul(Z, add(X, Y))))
861. add(X, add(X, neg(mul(X, Y)))) = mul(add(one, add(one, neg(Y))), X)
862. mul(add(one, add(one, mul(X, Y))), Z) = add(Z, add(Z, mul(X, mul(Z, Y))))
863. mul(add(X, add(one, add(Y, one))), Z) = add(Z, add(Z, mul(Z, add(X, Y))))
  (hard) mul(add(one, add(X, add(X, one))), Y) = mul(add(X, one), add(Y, Y))
864. mul(X, add(one, add(Y, add(Z, one)))) = add(X, add(X, mul(add(Y, Z), X)))
865. add(X, add(X, neg(mul(Y, X)))) = mul(X, add(one, add(one, neg(Y))))
866. mul(X, add(Y, add(Z, add(one, one)))) = add(X, add(X, mul(add(Y, Z), X)))
867. mul(X, add(Y, add(one, add(Z, one)))) = add(X, add(X, mul(add(Y, Z), X)))
868. mul(add(X, add(Y, add(one, one))), Z) = add(Z, add(Z, mul(add(X, Y), Z)))
869. add(X, add(X, neg(mul(Y, X)))) = mul(add(one, add(one, neg(Y))), X)
870. add(X, mul(Y, add(Z, add(one, one)))) = add(Y, add(X, add(Y, mul(Z, Y))))
871. add(mul(X, add(one, add(Y, one))), Z) = add(X, add(X, add(Z, mul(Y, X))))
872. add(X, mul(Y, add(one, add(Z, one)))) = add(Y, add(Y, add(X, mul(Z, Y))))
873. add(X, mul(X, add(Y, mul(Z, Y)))) = mul(X, add(one, mul(Y, add(Z, one))))
874. add(mul(X, add(Y, add(one, one))), Z) = add(X, add(Z, add(X, mul(Y, X))))
875. add(X, mul(X, add(Y, mul(Z, Y)))) = mul(X, add(one, mul(add(Z, one), Y)))
876. mul(X, add(one, mul(add(Y, one), Z))) = add(X, mul(Z, add(X, mul(X, Y))))
877. mul(X, mul(add(one, add(Y, one)), Z)) = mul(Z, add(X, add(X, mul(X, Y))))
878. mul(X, mul(add(Y, add(one, one)), Z)) = mul(Z, add(X, add(X, mul(Y, X))))
879. mul(X, mul(add(one, add(Y, one)), Z)) = mul(Z, add(X, add(X, mul(Y, X))))
880. mul(add(one, add(one, mul(X, Y))), Z) = add(Z, add(Z, mul(X, mul(Y, Z))))
881. mul(add(X, add(X, mul(X, Y))), Z) -> mul(X, mul(Z, add(one, add(Y, one))))
882. mul(add(X, add(X, mul(Y, X))), Z) -> mul(X, mul(Z, add(Y, add(one, one))))
883. mul(add(one, one), add(X, mul(Y, Z))) = add(X, add(X, mul(Y, add(Z, Z))))
884. add(X, add(Y, add(X, add(Z, Z)))) = add(Y, mul(add(X, Z), add(one, one)))
885. mul(add(X, Y), mul(add(one, one), Z)) = mul(Z, add(X, add(X, add(Y, Y))))
  (hard) add(X, add(X, mul(X, add(Y, Z)))) = mul(X, add(one, add(one, add(Z, Y))))
  (hard) add(X, add(X, mul(X, add(Y, Z)))) = mul(X, add(one, add(Z, add(one, Y))))
886. add(X, add(Y, add(X, mul(Z, X)))) = add(Y, mul(add(Z, add(one, one)), X))
887. mul(add(X, add(one, one)), mul(Y, Z)) = mul(Y, add(Z, add(Z, mul(X, Z))))
888. add(X, mul(Y, add(Z, add(one, one)))) = add(mul(Z, Y), add(X, add(Y, Y)))
  (hard) mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(Z, add(X, X)), mul(Y, X))
  (hard) add(X, mul(add(Y, add(one, Y)), X)) = mul(add(Y, one), add(X, X))
889. add(mul(X, Y), mul(add(Z, one), X)) -> add(X, mul(X, add(Y, Z)))
890. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(add(one, one), mul(Y, add(X, X)))
891. add(X, mul(Y, add(Z, add(one, one)))) = add(mul(Y, Z), add(X, add(Y, Y)))
892. mul(X, add(Y, add(Y, add(Z, Z)))) = mul(add(one, one), mul(X, add(Y, Z)))
893. mul(add(X, add(X, add(X, X))), Y) = mul(X, mul(add(one, one), add(Y, Y)))
894. mul(add(X, add(X, add(Y, Y))), Z) -> mul(add(X, Y), mul(Z, add(one, one)))
895. mul(add(X, add(X, add(X, X))), Y) = mul(add(one, one), mul(X, add(Y, Y)))
896. mul(add(X, add(X, add(Y, Y))), Z) = mul(Z, mul(add(X, Y), add(one, one)))
897. add(mul(X, Y), mul(Z, add(X, X))) = mul(X, add(mul(Z, add(one, one)), Y))
898. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, mul(X, add(Z, add(one, one))))
899. mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, mul(add(Z, add(one, one)), X))
900. add(X, mul(Y, add(one, add(Z, one)))) = add(Y, add(X, add(Y, mul(Y, Z))))
901. add(X, mul(Y, add(one, add(Z, one)))) = add(Y, add(Y, add(X, mul(Y, Z))))
902. add(X, mul(Y, add(Z, add(one, one)))) = add(Y, add(Y, add(X, mul(Z, Y))))
  (hard) mul(add(X, X), add(Y, one)) = add(X, mul(X, add(Y, add(one, Y))))
903. add(X, add(X, mul(X, add(Y, Z)))) = mul(add(one, add(Y, add(Z, one))), X)
  (hard) mul(X, add(one, add(one, mul(Y, Z)))) = add(X, add(X, mul(X, mul(Z, Y))))
904. add(X, mul(Y, add(X, mul(X, Z)))) = mul(X, add(one, mul(add(one, Z), Y)))
  (hard) add(X, neg(mul(Y, mul(X, Z)))) = mul(X, add(one, neg(mul(Z, Y))))
905. add(X, mul(Y, add(X, mul(X, Z)))) = mul(add(one, mul(Y, add(one, Z))), X)
  (hard) add(Z, neg(mul(Y, mul(Z, X)))) = mul(add(one, neg(mul(Y, X))), Z)
  (hard) add(X, mul(Y, add(X, mul(X, Z)))) = mul(X, add(one, mul(Y, add(Z, one))))
906. mul(X, add(one, mul(Y, add(one, Z)))) = add(X, mul(X, add(Y, mul(Z, Y))))
907. add(X, mul(Y, add(X, mul(Z, X)))) = mul(X, add(one, mul(add(Z, one), Y)))
908. add(X, mul(Y, add(X, mul(Z, X)))) = mul(add(one, mul(Y, add(Z, one))), X)
909. add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(one, mul(add(Z, one), Y)))
910. add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(add(Z, mul(X, add(one, one))), Y)
  (hard) add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(add(X, add(Z, X)), Y)
  (hard) add(neg(Y), mul(add(Y, Y), X)) = mul(add(X, add(X, neg(one))), Y)
911. mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(add(X, X), Z), mul(X, Y))
  (hard) add(mul(add(X, X), Y), mul(X, Z)) = mul(add(Y, add(Y, Z)), X)
  (hard) mul(add(Z, add(Y, Y)), X) = add(mul(add(X, X), Y), mul(X, Z))
912. add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(Y, add(Z, mul(add(one, one), X)))
913. add(X, mul(add(Y, Z), add(one, one))) = add(Y, add(Y, add(Z, add(Z, X))))
914. add(mul(X, Y), mul(Z, add(X, X))) = mul(add(Y, mul(Z, add(one, one))), X)
  (hard) add(mul(X, Y), mul(Z, add(X, X))) = mul(add(Z, add(Y, Z)), X)
915. add(neg(X), mul(Y, add(X, X))) = mul(add(Y, add(Y, neg(one))), X)
916. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(add(one, one), mul(add(Y, Y), X))
917. mul(X, add(Y, add(Y, add(Z, Z)))) = mul(add(Z, Y), mul(X, add(one, one)))
  (hard) mul(X, add(Y, add(Y, add(Z, Z)))) = mul(add(Z, Y), add(X, X))
  (hard) mul(add(X, X), add(Y, Z)) = mul(X, add(Z, add(Z, add(Y, Y))))
918. mul(add(X, Y), mul(Z, add(one, one))) = mul(Z, add(X, add(Y, add(Y, X))))
919. mul(add(X, Y), mul(Z, add(one, one))) = mul(Z, add(X, add(Y, add(X, Y))))
  (hard) mul(X, add(Y, add(Z, add(Y, Z)))) = mul(add(Y, Z), add(X, X))
  (hard) mul(add(X, X), add(Y, Z)) = mul(X, add(Y, add(Z, add(Y, Z))))
920. add(X, add(X, mul(add(Y, Z), X))) = mul(X, add(one, add(Y, add(one, Z))))
921. add(mul(X, Y), mul(Z, add(Y, Y))) = mul(add(X, mul(Z, add(one, one))), Y)
  (hard) add(mul(X, Y), mul(Z, add(Y, Y))) = mul(add(Z, add(X, Z)), Y)
922. add(X, add(neg(add(Y, X)), Z)) -> add(Z, neg(Y))
923. add(X, mul(add(one, one), add(Y, Z))) = add(Z, add(X, add(Y, add(Y, Z))))
  (hard) mul(add(one, one), X) = add(neg(one), add(X, add(one, X)))
924. add(neg(one), mul(add(X, one), add(X, one))) -> mul(X, add(one, add(one, X)))
  (hard) add(neg(one), add(X, add(X, one))) = mul(add(one, one), X)
  (hard) mul(add(X, one), add(Y, one)) = add(one, add(X, add(Y, mul(Y, X))))
  (hard) mul(X, add(one, add(one, add(Y, Z)))) = add(X, mul(X, add(Y, add(Z, one))))
  (hard) mul(X, add(one, add(one, add(Y, Z)))) = add(X, mul(X, add(Y, add(one, Z))))
  (hard) mul(X, add(one, add(one, add(Y, Z)))) = add(X, mul(add(Y, add(Z, one)), X))
  (hard) add(X, add(Y, add(one, mul(Y, X)))) = mul(add(X, one), add(Y, one))
925. add(mul(X, Y), mul(Z, add(one, mul(X, Y)))) = add(Z, mul(X, mul(Y, add(Z, one))))
926. add(X, mul(add(one, add(X, one)), Y)) = add(Y, add(Y, mul(X, add(Y, one))))
927. add(X, mul(Y, add(one, add(X, one)))) = add(Y, add(Y, mul(X, add(Y, one))))
  (hard) mul(X, add(one, add(one, mul(Y, X)))) = add(X, mul(X, add(one, mul(X, Y))))
  (hard) mul(X, add(Y, mul(Y, add(one, Z)))) = mul(Y, mul(X, add(Z, add(one, one))))
  (hard) mul(add(X, one), add(Y, one)) = add(X, add(one, add(Y, mul(Y, X))))
928. add(X, mul(Y, add(X, add(one, one)))) = add(Y, add(Y, mul(X, add(Y, one))))
  (hard) add(X, mul(Y, mul(X, add(Z, one)))) = mul(X, add(one, mul(add(one, Z), Y)))
  (hard) add(X, mul(Y, mul(X, add(Z, one)))) = mul(X, add(one, mul(Y, add(one, Z))))
  (hard) add(X, mul(X, add(Y, add(Z, one)))) = mul(X, add(one, add(Y, add(one, Z))))
  (hard) mul(X, add(one, add(Y, add(Z, one)))) = add(X, mul(X, add(Y, add(one, Z))))
  (hard) mul(X, add(one, mul(Y, add(Z, one)))) = add(X, mul(Y, mul(X, add(one, Z))))
  (hard) mul(add(X, add(one, mul(X, Y))), Z) = mul(Z, add(one, mul(X, add(Y, one))))
  (hard) mul(X, add(one, add(one, mul(X, Y)))) = add(X, mul(X, add(one, mul(Y, X))))
929. add(X, add(Y, add(Y, neg(add(X, X))))) -> add(Y, add(Y, neg(X)))
  (hard) mul(add(X, X), add(one, Y)) = mul(X, add(Y, add(one, add(Y, one))))
930. mul(X, add(Y, neg(add(one, one)))) = add(neg(add(X, X)), mul(X, Y))
931. mul(X, add(Y, neg(add(one, one)))) = add(mul(Y, X), neg(add(X, X)))
  (hard) mul(X, add(Y, Z)) = mul(X, add(neg(one), add(Y, add(Z, one))))
  (hard) mul(Y, mul(X, add(Y, add(one, one)))) = mul(X, mul(Y, add(one, add(one, Y))))
932. add(X, mul(X, add(Y, neg(add(one, add(one, one)))))) = add(mul(X, Y), neg(add(X, X)))
933. add(mul(X, Y), neg(add(X, X))) = mul(X, add(neg(add(one, one)), Y))
934. add(mul(X, Y), neg(add(X, X))) = mul(add(Y, neg(add(one, one))), X)
935. add(neg(X), mul(add(one, one), add(Y, X))) = add(X, add(Y, Y))
  (hard) add(Y, add(Y, add(X, one))) = add(one, add(X, add(Y, Y)))
936. add(X, add(X, Y)) = add(neg(Y), mul(add(one, one), add(X, Y)))
  (hard) add(X, add(X, add(Y, one))) = add(one, add(X, add(X, Y)))
  (hard) mul(add(one, one), Y) = add(neg(X), add(Y, add(Y, X)))
937. add(X, add(neg(add(Y, Y)), X)) = mul(add(one, one), add(neg(Y), X))
938. mul(add(one, one), add(neg(X), Y)) = add(neg(add(X, X)), add(Y, Y))
939. add(X, add(mul(add(one, one), Y), Z)) = add(Y, add(Y, add(X, Z)))
940. mul(X, mul(add(Y, Y), Z)) = mul(add(one, one), mul(Y, mul(X, Z)))
941. mul(add(X, X), mul(Y, Z)) = mul(Y, mul(add(one, one), mul(X, Z)))
942. mul(add(X, X), mul(Y, Z)) = mul(add(one, one), mul(Y, mul(X, Z)))
  (hard) mul(add(one, one), mul(X, Y)) = mul(add(Y, Y), X)
943. mul(X, mul(Y, add(Z, Z))) = mul(add(one, one), mul(X, mul(Y, Z)))
944. mul(X, mul(Y, add(Z, Z))) = mul(add(one, one), mul(Z, mul(X, Y)))
  (hard) mul(Z, mul(add(Y, Y), X)) = mul(Y, mul(add(one, one), mul(X, Z)))
  (hard) mul(Z, mul(Y, add(X, X))) = mul(X, mul(add(one, one), mul(Y, Z)))
945. add(X, add(Y, mul(Z, add(one, one)))) = add(Z, add(Z, add(X, Y)))
  (hard) mul(add(X, X), mul(Y, Z)) = mul(X, mul(Z, mul(Y, add(one, one))))
946. mul(X, mul(Y, add(Z, Z))) = mul(add(one, one), mul(X, mul(Z, Y)))
  (hard) mul(add(X, X), mul(Y, Z)) = mul(X, mul(Z, mul(add(one, one), Y)))
  (hard) mul(X, mul(Y, mul(Z, add(one, one)))) = mul(Z, mul(Y, add(X, X)))
  (hard) mul(add(X, X), mul(Y, Z)) = mul(X, mul(add(one, one), mul(Z, Y)))
  (hard) mul(Z, mul(add(Y, Y), X)) = mul(X, mul(Z, mul(add(one, one), Y)))
  (hard) mul(Z, mul(add(Y, Y), X)) = mul(X, mul(add(one, one), mul(Z, Y)))
  (hard) mul(Z, mul(add(Y, Y), X)) = mul(X, mul(Z, mul(Y, add(one, one))))
947. mul(X, add(Y, mul(add(one, one), Z))) = mul(add(Z, add(Z, Y)), X)
948. add(X, mul(add(Y, add(Y, neg(one))), X)) -> mul(Y, add(X, X))
949. mul(X, add(Y, mul(add(one, one), Z))) = mul(add(Y, add(Z, Z)), X)
  (simplifying rules with respect to one another...)
  (delete rule 254)
  (delete rule 393)
  (delete rule 401)
  (delete rule 402)
  (delete rule 408)
  (delete rule 411)
  (delete rule 415)
  (delete rule 420)
  (delete rule 465)
  (delete rule 501)
  (delete rule 552)
  (hard) mul(X, add(Y, add(Y, mul(Z, Y)))) = mul(X, mul(Y, add(one, add(Z, one))))
  (delete rule 583)
  (delete rule 584)
  (delete rule 585)
  (delete rule 586)
  (delete rule 587)
  (delete rule 592)
  (delete rule 646)
  (delete rule 683)
  (delete rule 702)
  (delete rule 736)
  (delete rule 737)
  (delete rule 775)
  (delete rule 777)
  (delete rule 781)
  (delete rule 783)
  (delete rule 800)
  (delete rule 806)
  (delete rule 817)
  (delete rule 828)
  (delete rule 831)
  (delete rule 834)
  (delete rule 840)
  (delete rule 841)
  (delete rule 844)
  (delete rule 845)
  (delete rule 849)
  (delete rule 850)
  (delete rule 861)
  (delete rule 862)
  (delete rule 870)
  (delete rule 900)
  (delete rule 901)
  (delete rule 935)
  (delete rule 942)
950. mul(X, add(Y, mul(add(one, one), Y))) = mul(add(X, add(X, X)), Y)
951. add(X, mul(Y, add(Z, Z))) = add(mul(Y, mul(add(one, one), Z)), X)
952. add(mul(add(X, X), Y), Z) = add(Z, mul(X, mul(Y, add(one, one))))
953. add(X, mul(Y, add(Z, Z))) = add(mul(add(one, one), mul(Y, Z)), X)
954. add(mul(add(X, X), Y), Z) = add(Z, mul(X, mul(add(one, one), Y)))
  (hard) add(mul(add(X, X), Y), Z) = add(Z, mul(add(Y, Y), X))
955. add(mul(add(X, X), Y), Z) = add(Z, mul(Y, mul(X, add(one, one))))
956. add(mul(add(X, X), Y), Z) = add(Z, mul(add(one, one), mul(X, Y)))
957. add(mul(X, Y), add(mul(X, Y), Z)) -> add(mul(add(X, X), Y), Z)
958. add(mul(add(X, X), Y), Z) = add(Z, mul(Y, mul(add(one, one), X)))
959. add(X, mul(Y, add(Z, Z))) = add(mul(Z, mul(add(one, one), Y)), X)
960. add(X, mul(Y, add(Z, Z))) = add(mul(add(one, one), mul(Z, Y)), X)
961. add(X, mul(Y, add(Z, Z))) = add(mul(Z, mul(Y, add(one, one))), X)
962. add(X, mul(Y, mul(Z, add(one, one)))) = add(mul(Y, add(Z, Z)), X)
963. add(mul(add(X, X), Y), Z) = add(Z, mul(add(one, one), mul(Y, X)))
  (hard) mul(Y, mul(add(Z, Z), X)) = mul(X, mul(Z, mul(Y, add(one, one))))
964. add(X, mul(Y, mul(Z, add(one, one)))) = add(mul(Z, add(Y, Y)), X)
965. add(mul(X, mul(Y, add(one, one))), Z) = add(Z, mul(add(X, X), Y))
966. mul(X, add(Y, mul(add(one, one), Z))) = add(mul(add(X, X), Z), mul(X, Y))
  (hard) mul(add(Z, add(X, Z)), Y) = mul(Y, add(X, mul(add(one, one), Z)))
  (hard) mul(add(X, X), add(Y, Y)) = add(mul(add(X, X), Y), mul(X, add(Y, Y)))
  (hard) add(X, add(Y, mul(add(one, one), X))) = add(Y, add(X, add(X, X)))
  (hard) add(X, add(X, add(Y, Z))) = add(mul(X, add(one, one)), add(Z, Y))
967. add(X, add(mul(Y, add(one, one)), Z)) = add(Y, add(Y, add(Z, X)))
  (hard) add(Z, add(Z, add(Y, X))) = add(X, add(Z, add(Z, Y)))
  (hard) add(Z, add(Z, add(Y, X))) = add(X, add(Z, add(Y, Z)))
  (hard) add(Z, add(Z, add(Y, X))) = add(X, add(Y, add(Z, Z)))
  (hard) add(Z, add(Z, add(Y, X))) = add(Z, add(X, add(Y, Z)))
968. add(X, add(X, add(Y, Z))) = add(Y, add(Z, mul(add(one, one), X)))
  (hard) mul(add(one, one), mul(X, mul(Y, Z))) = mul(X, mul(add(Z, Z), Y))
  (hard) mul(X, mul(Y, mul(add(one, one), Z))) = mul(Y, mul(X, add(Z, Z)))
  (hard) mul(X, mul(Y, mul(Z, add(one, one)))) = mul(Y, mul(X, add(Z, Z)))
  (hard) mul(add(one, one), mul(X, mul(Y, Z))) = mul(Z, mul(add(X, X), Y))
  (hard) mul(Z, mul(add(Y, Y), X)) = mul(add(one, one), mul(X, mul(Z, Y)))
  (hard) mul(Z, mul(Y, add(X, X))) = mul(Y, mul(add(one, one), mul(X, Z)))
  (hard) mul(add(one, one), mul(X, mul(Y, Z))) = mul(Y, mul(X, add(Z, Z)))
  (hard) mul(X, mul(add(one, one), mul(Y, Z))) = mul(Z, mul(add(Y, Y), X))
969. mul(add(X, add(X, Y)), Z) = mul(Z, add(mul(X, add(one, one)), Y))
970. mul(add(X, add(X, Y)), Z) = mul(Z, add(mul(add(one, one), X), Y))
  (hard) mul(add(Z, Y), add(X, X)) = mul(X, mul(add(Y, Z), add(one, one)))
971. add(mul(X, mul(Y, add(one, one))), Z) = add(Z, mul(add(Y, Y), X))
  (hard) add(mul(X, add(one, one)), add(Y, X)) = add(X, add(Y, add(X, X)))
  (hard) mul(X, mul(Y, mul(Z, add(one, one)))) = mul(add(Y, Y), mul(X, Z))
  (hard) mul(X, mul(Y, mul(Z, add(one, one)))) = mul(Z, mul(X, add(Y, Y)))
  (hard) add(X, add(Y, mul(Y, add(one, one)))) = add(Y, add(Y, add(Y, X)))
  (hard) add(mul(add(one, one), X), add(Y, X)) = add(X, add(Y, add(X, X)))
  (hard) add(X, add(Y, add(X, Z))) = add(mul(add(one, one), X), add(Y, Z))
  (hard) add(Z, add(Y, add(Z, X))) = add(Y, add(X, mul(add(one, one), Z)))
972. add(X, add(Y, add(Z, X))) = add(Y, add(mul(add(one, one), X), Z))
  (hard) add(Z, add(Y, add(X, Z))) = add(Y, add(X, mul(add(one, one), Z)))
  (hard) add(Z, add(Y, add(X, Z))) = add(Y, add(X, mul(Z, add(one, one))))
  (hard) add(Z, add(Y, add(Z, X))) = add(Y, add(X, mul(Z, add(one, one))))
  (hard) add(X, add(Y, add(X, Z))) = add(Y, add(mul(X, add(one, one)), Z))
  (hard) add(X, add(Y, add(Z, Y))) = add(mul(add(one, one), Y), add(X, Z))
  (hard) add(Y, add(Y, add(Y, X))) = add(X, add(Y, mul(add(one, one), Y)))
973. add(mul(add(one, one), mul(X, Y)), Z) = add(Z, mul(add(Y, Y), X))
  (hard) add(mul(X, Y), add(mul(X, Y), Z)) = add(Z, mul(add(Y, Y), X))
974. add(X, mul(Y, mul(add(one, one), Z))) = add(mul(Z, add(Y, Y)), X)
975. add(X, mul(Y, mul(add(one, one), Z))) = add(mul(Y, add(Z, Z)), X)
976. add(mul(add(one, one), mul(X, Y)), Z) = add(Z, mul(add(X, X), Y))
977. add(mul(X, Y), add(mul(X, Y), Z)) -> add(Z, mul(add(X, X), Y))
  (hard) mul(add(X, X), add(Z, Y)) = mul(X, mul(add(one, one), add(Y, Z)))
  (hard) add(X, add(Y, add(Z, X))) = add(Y, add(mul(X, add(one, one)), Z))
978. add(X, neg(mul(Y, add(X, one)))) = add(neg(Y), mul(add(one, neg(Y)), X))
979. add(mul(X, add(Y, Y)), mul(add(Z, Z), Y)) -> mul(add(one, one), mul(add(Z, X), Y))
  (hard) mul(add(Z, Z), add(Y, X)) = mul(add(X, Y), mul(add(one, one), Z))
  (hard) mul(add(one, one), mul(X, add(Y, Z))) = mul(add(X, X), add(Z, Y))
  (hard) mul(add(X, X), add(Z, Y)) = mul(X, mul(add(Y, Z), add(one, one)))
  (hard) mul(add(Z, add(Y, Y)), X) = mul(X, add(mul(Y, add(one, one)), Z))
980. mul(add(X, mul(Y, add(one, one))), Z) = mul(Z, add(Y, add(Y, X)))
981. mul(add(X, mul(Y, add(one, one))), Z) = mul(Z, add(Y, add(X, Y)))
  (hard) mul(add(Z, X), add(Y, Y)) = mul(Y, mul(add(one, one), add(X, Z)))
982. mul(add(X, mul(Y, add(one, one))), Z) = mul(Z, add(X, add(Y, Y)))
  (hard) mul(add(X, Y), mul(Z, add(one, one))) = mul(add(Z, Z), add(Y, X))
  (hard) add(X, mul(Y, mul(add(one, one), X))) = mul(add(Y, add(Y, one)), X)
  (hard) mul(add(one, add(Y, Y)), X) = add(X, mul(Y, mul(add(one, one), X)))
983. add(X, add(X, add(X, add(X, Y)))) = add(mul(X, add(one, add(one, add(one, one)))), Y)
984. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(add(one, one), mul(X, add(one, one))))
  (hard) mul(add(one, add(Y, Y)), X) = add(X, mul(Y, mul(X, add(one, one))))
985. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(X, add(one, add(one, add(one, one)))))
986. mul(X, add(one, add(one, add(one, Y)))) = add(X, add(X, add(X, mul(X, Y))))
987. mul(add(one, add(one, add(one, X))), Y) = add(Y, add(Y, add(Y, mul(X, Y))))
  (hard) add(Y, add(Y, neg(mul(Y, X)))) = mul(add(one, add(one, neg(X))), Y)
  (hard) add(X, mul(add(one, one), mul(Y, X))) = mul(add(one, add(Y, Y)), X)
988. mul(X, add(one, add(one, add(Y, one)))) = add(X, add(X, add(X, mul(Y, X))))
989. mul(X, add(one, add(one, add(one, Y)))) = add(X, add(X, add(X, mul(Y, X))))
990. mul(X, add(one, add(one, add(Y, one)))) = add(X, add(X, add(X, mul(X, Y))))
991. mul(add(one, add(one, add(X, one))), Y) = add(Y, add(Y, add(Y, mul(Y, X))))
992. mul(X, add(one, add(Y, add(one, one)))) = add(X, add(X, add(X, mul(Y, X))))
993. mul(X, add(Y, add(one, add(one, one)))) = add(X, add(X, add(X, mul(Y, X))))
994. mul(add(X, add(one, add(one, one))), Y) = add(Y, add(Y, add(Y, mul(X, Y))))
995. add(X, mul(add(Y, Y), Z)) = add(mul(Y, mul(add(one, one), Z)), X)
  (hard) add(X, mul(add(one, one), mul(Y, X))) = mul(add(Y, add(Y, one)), X)
  (hard) mul(add(Y, add(Y, one)), X) = add(X, mul(add(one, one), mul(X, Y)))
  (hard) add(X, mul(X, mul(Y, add(one, one)))) = mul(X, add(Y, add(Y, one)))
  (hard) add(X, mul(Y, mul(add(one, one), X))) = mul(X, add(Y, add(Y, one)))
  (hard) add(X, mul(Y, mul(X, add(one, one)))) = mul(X, add(one, add(Y, Y)))
  (hard) mul(add(one, add(Y, Y)), X) = add(X, mul(X, mul(Y, add(one, one))))
  (hard) mul(add(one, add(Y, Y)), X) = add(X, mul(X, mul(add(one, one), Y)))
  (hard) add(X, mul(X, mul(Y, add(one, one)))) = mul(X, add(Y, add(one, Y)))
  (hard) add(X, mul(Y, mul(add(one, one), X))) = mul(X, add(Y, add(one, Y)))
  (hard) add(X, mul(Y, mul(add(one, one), X))) = mul(add(Y, add(one, Y)), X)
  (hard) add(X, mul(Y, mul(X, add(one, one)))) = mul(X, add(Y, add(one, Y)))
  (hard) mul(add(X, add(one, add(X, one))), Y) = mul(add(X, one), add(Y, Y))
  (hard) add(mul(add(one, X), Y), mul(Z, add(one, X))) = mul(add(X, one), add(Y, Z))
  (hard) mul(X, add(Y, add(one, Y))) = add(X, mul(add(one, one), mul(Y, X)))
  (hard) mul(add(Y, add(one, Y)), X) = add(X, mul(Y, mul(X, add(one, one))))
  (hard) mul(add(Y, add(one, Y)), X) = add(X, mul(X, mul(Y, add(one, one))))
  (hard) mul(add(Y, add(one, Y)), X) = add(X, mul(add(one, one), mul(Y, X)))
  (hard) mul(add(Y, add(one, Y)), X) = add(X, mul(X, mul(add(one, one), Y)))
996. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(add(one, one), mul(add(one, one), X)))
997. mul(X, add(Y, add(Y, add(Y, Y)))) = mul(add(one, one), mul(Y, mul(X, add(one, one))))
  (hard) mul(add(Y, add(Y, one)), X) = add(X, mul(Y, mul(X, add(one, one))))
  (hard) mul(X, add(one, add(Y, add(one, Y)))) = mul(add(X, X), add(Y, one))
  (hard) add(X, mul(add(one, one), mul(X, Y))) = mul(add(one, add(Y, Y)), X)
  (hard) mul(add(X, one), mul(Y, add(one, one))) = add(Y, mul(Y, add(X, add(one, X))))
  (hard) add(Y, mul(Y, add(X, add(one, X)))) = mul(add(X, one), mul(add(one, one), Y))
  (hard) add(Y, mul(Y, add(X, add(one, X)))) = mul(add(one, one), mul(add(X, one), Y))
  (hard) add(Y, mul(add(X, add(one, X)), Y)) = mul(add(X, one), mul(Y, add(one, one)))
998. add(X, neg(mul(X, add(Y, add(Z, one))))) -> neg(mul(X, add(Y, Z)))
999. add(X, mul(X, add(Y, neg(add(Z, one))))) -> mul(X, add(Y, neg(Z)))
1000. add(X, neg(mul(X, add(Y, add(one, Z))))) -> neg(mul(X, add(Y, Z)))

Gave up on reaching the given resource limit.
Here is the final rewrite system:
  neg(zero) -> zero
  add(X, X) = mul(add(one, one), X)
  add(X, Y) <-> add(Y, X)
  add(X, zero) -> X
  add(zero, X) -> X
  neg(neg(X)) -> X
  mul(X, Y) <-> mul(Y, X)
  mul(X, zero) -> zero
  mul(X, one) -> X
  mul(zero, X) -> zero
  mul(one, X) -> X
  add(X, neg(X)) -> zero
  mul(X, neg(Y)) -> neg(mul(X, Y))
  mul(neg(X), Y) -> neg(mul(X, Y))
  add(X, add(X, Y)) = add(mul(X, add(one, one)), Y)
  add(X, add(X, Y)) = add(mul(add(one, one), X), Y)
  add(X, add(Y, Z)) <-> add(Y, add(X, Z))
  add(X, mul(X, Y)) = mul(X, add(one, Y))
  add(X, add(X, Y)) <-> add(Y, add(X, X))
  add(X, add(X, Y)) = add(Y, mul(add(one, one), X))
  add(X, add(X, Y)) = add(neg(Y), mul(add(one, one), add(X, Y)))
  add(neg(X), neg(Y)) -> neg(add(X, Y))
  add(add(X, Y), Z) -> add(X, add(Y, Z))
  neg(add(X, neg(Y))) -> add(Y, neg(X))
  neg(add(neg(X), Y)) -> add(X, neg(Y))
  mul(X, add(Y, Y)) = mul(add(X, X), Y)
  mul(X, add(Y, one)) = add(X, mul(X, Y))
  mul(X, add(Y, one)) = add(X, mul(Y, X))
  mul(X, add(one, Y)) = add(X, mul(Y, X))
  mul(X, add(one, one)) = add(X, X)
  mul(X, add(Y, Y)) = mul(Y, add(X, X))
  mul(X, add(Y, Y)) = mul(Y, mul(add(one, one), X))
  mul(X, add(Y, Y)) = mul(add(one, one), mul(Y, X))
  mul(X, mul(Y, Z)) <-> mul(Y, mul(X, Z))
  mul(add(X, X), Y) = mul(X, mul(Y, add(one, one)))
  mul(add(X, one), Y) = add(Y, mul(X, Y))
  mul(add(one, X), Y) = add(Y, mul(X, Y))
  mul(mul(X, Y), Z) -> mul(X, mul(Y, Z))
  add(X, add(Y, neg(X))) -> Y
  add(X, add(neg(X), Y)) -> Y
  add(X, neg(add(X, Y))) -> neg(Y)
  add(X, neg(add(Y, X))) -> neg(Y)
  add(X, neg(mul(X, Y))) = mul(X, add(one, neg(Y)))
  add(X, neg(mul(Y, X))) = mul(X, add(one, neg(Y)))
  add(X, neg(mul(Y, X))) = mul(add(one, neg(Y)), X)
  add(neg(X), mul(X, Y)) = mul(X, add(Y, neg(one)))
  add(neg(X), mul(X, Y)) = mul(X, add(neg(one), Y))
  add(neg(X), mul(Y, X)) = mul(X, add(Y, neg(one)))
  add(neg(X), mul(Y, X)) = mul(X, add(neg(one), Y))
  mul(X, add(Y, neg(one))) = mul(add(neg(one), Y), X)
  mul(X, add(neg(one), Y)) = mul(add(Y, neg(one)), X)
  mul(add(X, neg(one)), Y) = add(neg(Y), mul(X, Y))
  mul(add(neg(one), X), Y) = add(neg(Y), mul(X, Y))
  add(X, add(X, add(X, X))) = mul(X, add(one, add(one, add(one, one))))
  add(X, add(X, add(X, X))) = mul(add(one, one), mul(X, add(one, one)))
  add(X, add(X, add(X, X))) = mul(add(one, one), mul(add(one, one), X))
  add(X, add(X, add(X, Y))) = add(mul(X, add(one, add(one, one))), Y)
  add(X, add(X, add(X, Y))) = add(mul(add(one, add(one, one)), X), Y)
  add(X, add(X, add(Y, Y))) = mul(add(X, Y), add(one, one))
  add(X, add(Y, add(Z, X))) = add(Y, add(mul(add(one, one), X), Z))
  add(X, add(Y, mul(X, Z))) = add(Y, mul(X, add(one, Z)))
  add(X, add(Y, mul(Z, X))) = add(Y, mul(X, add(Z, one)))
  add(X, add(Y, mul(Z, X))) = add(Y, mul(X, add(one, Z)))
  add(X, add(Y, mul(X, Z))) = add(mul(X, add(one, Z)), Y)
  add(X, mul(X, add(Y, Y))) = mul(X, add(one, mul(add(one, one), Y)))
  add(X, mul(X, add(Y, Z))) = mul(X, add(Y, add(Z, one)))
  add(X, mul(X, add(Y, Z))) = mul(X, add(Y, add(one, Z)))
  add(X, mul(X, add(one, X))) = add(neg(one), mul(add(X, one), add(X, one)))
  add(X, mul(Y, add(X, Z))) = add(mul(X, add(one, Y)), mul(Y, Z))
  add(X, mul(Y, add(X, Z))) = add(mul(add(one, Y), X), mul(Z, Y))
  add(X, mul(Y, add(X, one))) <-> add(Y, mul(X, add(Y, one)))
  add(X, mul(Y, add(Z, X))) = add(mul(add(Y, one), X), mul(Z, Y))
  add(X, mul(Y, add(Z, X))) = add(mul(add(one, Y), X), mul(Y, Z))
  add(X, mul(Y, add(Z, one))) = add(Y, add(mul(Z, Y), X))
  add(X, mul(Y, add(one, Z))) = add(Y, add(mul(Y, Z), X))
  add(X, mul(Y, add(one, Z))) = add(Y, add(mul(Z, Y), X))
  add(X, mul(Y, add(one, one))) = add(Y, add(Y, X))
  add(X, mul(add(Y, Y), X)) = mul(X, add(one, mul(add(one, one), Y)))
  add(X, mul(add(Y, Z), X)) = mul(X, add(Y, add(Z, one)))
  add(X, mul(add(Y, Z), X)) = mul(X, add(Y, add(one, Z)))
  add(X, mul(add(Y, one), Z)) = add(Z, add(mul(Y, Z), X))
  add(X, mul(add(one, Y), Z)) = add(Z, add(mul(Y, Z), X))
  add(X, add(Y, add(X, X))) = add(Y, mul(X, add(one, add(one, one))))
  add(X, add(Y, add(X, X))) = add(Y, mul(add(one, add(one, one)), X))
  add(X, mul(Y, add(X, X))) = mul(add(one, mul(Y, add(one, one))), X)
  add(X, mul(Y, add(X, X))) = mul(add(one, mul(add(one, one), Y)), X)
  add(X, mul(Y, add(X, one))) = add(Y, mul(X, add(one, Y)))
  add(X, mul(X, add(Y, Y))) = mul(add(one, mul(add(one, one), Y)), X)
  add(X, add(Y, mul(Z, X))) = add(Y, mul(add(Z, one), X))
  add(X, add(Y, mul(Z, X))) = add(Y, mul(add(one, Z), X))
  add(X, add(X, add(Y, Z))) = add(Y, add(Z, mul(add(one, one), X)))
  add(X, mul(Y, add(Z, Z))) = add(mul(Z, add(Y, Y)), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(add(Y, Y), Z), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(add(Z, Z), Y), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(Y, mul(Z, add(one, one))), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(Z, mul(Y, add(one, one))), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(Z, mul(add(one, one), Y)), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(Y, mul(add(one, one), Z)), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(add(one, one), mul(Z, Y)), X)
  add(X, mul(Y, add(Z, Z))) = add(mul(add(one, one), mul(Y, Z)), X)
  add(X, mul(Y, add(Z, X))) = add(mul(Z, Y), mul(X, add(Y, one)))
  add(X, mul(Y, add(Z, X))) = add(mul(Z, Y), mul(add(one, Y), X))
  add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(X, add(Y, one)))
  add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(X, add(one, Y)))
  add(X, mul(Y, add(Z, X))) = add(mul(Y, Z), mul(add(one, Y), X))
  add(X, mul(Y, add(Z, one))) = add(mul(add(Z, one), Y), X)
  add(X, mul(Y, add(X, Z))) = add(mul(Z, Y), mul(add(Y, one), X))
  add(X, mul(Y, add(X, Z))) = add(mul(Y, Z), mul(add(Y, one), X))
  add(X, mul(Y, add(X, Z))) = add(mul(Y, Z), mul(add(one, Y), X))
  add(X, mul(Y, add(one, Z))) = add(mul(add(one, Z), Y), X)
  add(X, mul(X, add(Y, Z))) = mul(add(Y, add(Z, one)), X)
  add(X, mul(X, add(Y, Z))) = mul(add(Y, add(one, Z)), X)
  add(X, mul(add(Y, Y), Z)) = add(mul(Y, mul(add(one, one), Z)), X)
  add(X, mul(add(Y, Z), X)) = mul(add(Y, add(Z, one)), X)
  add(X, mul(add(Y, Z), X)) = mul(add(Y, add(one, Z)), X)
  add(X, mul(add(Y, one), Z)) = add(mul(Z, add(Y, one)), X)
  add(X, mul(add(X, Y), Z)) = add(mul(add(Z, one), X), mul(Z, Y))
  add(X, mul(add(X, Y), Z)) = add(mul(add(Z, one), X), mul(Y, Z))
  add(X, mul(add(one, Y), Z)) = add(mul(Z, add(one, Y)), X)
  add(neg(X), add(Y, neg(Z))) -> add(Y, neg(add(X, Z)))
  add(neg(X), add(neg(Y), Z)) -> add(Z, neg(add(Y, X)))
  add(mul(X, Y), mul(X, Y)) -> mul(Y, add(X, X))
  add(mul(X, Y), mul(X, Z)) -> mul(X, add(Y, Z))
  add(mul(X, Y), mul(Z, Y)) -> mul(add(X, Z), Y)
  add(mul(X, Y), mul(Y, Z)) -> mul(Y, add(X, Z))
  add(mul(X, add(Y, Y)), Z) = add(Z, mul(add(X, X), Y))
  add(mul(X, add(Y, Y)), Z) = add(Z, mul(add(Y, Y), X))
  add(mul(X, add(Y, one)), Z) = add(X, add(Z, mul(Y, X)))
  add(mul(X, add(Y, one)), Z) = add(X, add(mul(Y, X), Z))
  add(mul(X, add(one, Y)), Z) = add(X, add(Z, mul(Y, X)))
  add(mul(X, add(one, Y)), Z) = add(X, add(mul(X, Y), Z))
  add(mul(X, add(one, Y)), Z) = add(X, add(mul(Y, X), Z))
  add(mul(add(X, one), Y), Z) = add(Y, add(Z, mul(X, Y)))
  add(mul(add(X, one), Y), Z) = add(Y, add(mul(X, Y), Z))
  add(mul(add(X, X), Y), Z) = add(Z, mul(X, mul(Y, add(one, one))))
  add(mul(add(X, X), Y), Z) = add(Z, mul(X, mul(add(one, one), Y)))
  add(mul(add(X, X), Y), Z) = add(Z, mul(add(one, one), mul(Y, X)))
  add(mul(add(X, X), Y), Z) = add(Z, mul(add(one, one), mul(X, Y)))
  add(mul(add(one, X), Y), Z) = add(Y, add(Z, mul(X, Y)))
  add(mul(add(one, X), Y), Z) = add(Y, add(mul(X, Y), Z))
  neg(add(X, add(Y, neg(Z)))) -> add(Z, neg(add(X, Y)))
  neg(add(X, add(neg(Y), Z))) -> add(Y, neg(add(X, Z)))
  neg(mul(X, add(Y, neg(Z)))) -> mul(X, add(Z, neg(Y)))
  neg(mul(X, add(neg(Y), Z))) -> mul(X, add(Y, neg(Z)))
  neg(mul(add(X, neg(Y)), Z)) -> mul(Z, add(Y, neg(X)))
  neg(mul(add(neg(X), Y), Z)) -> mul(Z, add(X, neg(Y)))
  mul(X, add(Y, add(Y, Y))) = mul(add(X, add(X, X)), Y)
  mul(X, add(Y, add(Y, Z))) = add(mul(Y, add(X, X)), mul(X, Z))
  mul(X, add(Y, add(Y, Z))) = add(mul(add(X, X), Y), mul(X, Z))
  mul(X, add(Y, add(Y, one))) = add(X, mul(Y, add(X, X)))
  mul(X, add(Y, add(Y, one))) = add(X, mul(Y, mul(X, add(one, one))))
  mul(X, add(Y, add(Z, Z))) = add(mul(X, Y), mul(Z, add(X, X)))
  mul(X, add(Y, add(Z, Z))) = add(mul(Y, X), mul(Z, add(X, X)))
  mul(X, add(Y, add(Z, Z))) = add(mul(Y, X), mul(add(X, X), Z))
  mul(X, add(Y, add(one, one))) = add(X, add(X, mul(Y, X)))
  mul(X, add(one, add(Y, Y))) = add(X, mul(Y, add(X, X)))
  mul(X, add(one, add(Y, Y))) = add(X, mul(add(X, X), Y))
  mul(X, add(one, add(Y, Y))) = add(X, mul(X, mul(add(one, one), Y)))
  mul(X, add(one, add(Y, Y))) = add(X, mul(Y, mul(add(one, one), X)))
  mul(X, add(one, add(one, Y))) = add(X, add(X, mul(X, Y)))
  mul(X, add(one, add(one, Y))) = add(X, add(X, mul(Y, X)))
  mul(X, add(one, add(one, one))) = add(X, add(X, X))
  mul(X, add(one, mul(Y, Z))) = add(X, mul(Y, mul(X, Z)))
  mul(X, add(one, mul(Y, Z))) = add(X, mul(Y, mul(Z, X)))
  mul(X, mul(Y, add(Z, Z))) = mul(Y, mul(add(one, one), mul(X, Z)))
  mul(X, mul(Y, add(Z, Z))) = mul(add(one, one), mul(X, mul(Y, Z)))
  mul(X, mul(Y, add(Z, Z))) = mul(add(one, one), mul(X, mul(Z, Y)))
  mul(X, mul(Y, add(Z, one))) = mul(Y, add(X, mul(X, Z)))
  mul(X, mul(Y, add(Z, one))) = mul(Y, add(X, mul(Z, X)))
  mul(X, mul(Y, add(one, Z))) = mul(Y, add(X, mul(X, Z)))
  mul(X, mul(Y, add(one, Z))) = mul(Y, add(X, mul(Z, X)))
  mul(X, mul(Y, add(one, one))) = mul(Y, add(X, X))
  mul(X, mul(Y, add(Z, Z))) = mul(add(one, one), mul(Z, mul(X, Y)))
  mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(X, add(Z, Z)))
  mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(Z, add(X, X)))
  mul(X, mul(add(Y, Y), Z)) = mul(add(X, X), mul(Y, Z))
  mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(add(one, one), mul(X, Z)))
  mul(X, mul(add(Y, Y), Z)) = mul(add(one, one), mul(X, mul(Y, Z)))
  mul(X, mul(add(Y, one), Z)) = mul(Z, add(X, mul(X, Y)))
  mul(X, mul(add(one, one), Y)) = mul(add(X, X), Y)
  mul(X, add(Y, add(Y, Y))) = mul(Y, add(X, add(X, X)))
  mul(X, add(Y, add(Y, Y))) = mul(Y, add(X, mul(X, add(one, one))))
  mul(X, mul(add(Y, Y), Z)) = mul(Z, mul(X, add(Y, Y)))
  mul(X, mul(add(Y, Y), Z)) = mul(add(one, one), mul(Y, mul(X, Z)))
  mul(X, add(Y, mul(Z, Y))) = mul(Y, add(X, mul(Z, X)))
  mul(X, add(Y, mul(Y, Z))) = mul(Y, add(X, mul(Z, X)))
  mul(X, add(Y, mul(Y, Z))) = mul(Y, add(X, mul(X, Z)))
  mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(Y, add(X, X)))
  mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(X, add(Y, Y)))
  mul(X, mul(Y, add(Z, Z))) = mul(add(Z, Z), mul(X, Y))
  mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(X, mul(Y, add(one, one))))
  mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(X, mul(add(one, one), Y)))
  mul(X, mul(Y, add(Z, Z))) = mul(Z, mul(add(one, one), mul(X, Y)))
  mul(X, mul(Y, add(Z, one))) = mul(Y, mul(add(Z, one), X))
  mul(X, mul(Y, add(Z, one))) = mul(Y, mul(add(one, Z), X))
  mul(X, mul(Y, add(one, Z))) = mul(add(one, Z), mul(X, Y))
  mul(X, mul(add(Y, Y), Z)) = mul(Z, mul(add(X, X), Y))
  mul(X, mul(add(Y, Y), Z)) = mul(Y, mul(add(X, X), Z))
  mul(add(X, X), add(Y, Y)) = mul(X, mul(Y, add(one, add(one, add(one, one)))))
  mul(add(X, X), add(Y, Z)) = mul(X, add(Y, add(Y, add(Z, Z))))
  mul(add(X, X), mul(Y, Z)) = mul(X, mul(Y, add(Z, Z)))
  mul(add(X, X), mul(Y, Z)) = mul(X, mul(Z, add(Y, Y)))
  mul(add(X, X), mul(Y, Z)) = mul(X, mul(Y, mul(Z, add(one, one))))
  mul(add(X, X), mul(Y, Z)) = mul(X, mul(Y, mul(add(one, one), Z)))
  mul(add(X, X), mul(Y, Z)) = mul(Y, mul(add(one, one), mul(X, Z)))
  mul(add(X, Y), add(Z, Z)) = mul(add(X, add(X, add(Y, Y))), Z)
  mul(add(X, one), mul(Y, Z)) = mul(Y, add(Z, mul(X, Z)))
  mul(add(X, one), mul(Y, Z)) = mul(Y, mul(Z, add(X, one)))
  mul(add(X, Y), add(Z, Z)) = mul(Z, add(X, add(X, add(Y, Y))))
  mul(add(one, X), mul(Y, Z)) = mul(Y, add(Z, mul(X, Z)))
  mul(add(one, one), add(X, Y)) = add(X, add(X, add(Y, Y)))
  mul(add(one, one), mul(X, Y)) = mul(X, add(Y, Y))
  mul(add(one, one), mul(X, Y)) = mul(add(X, X), Y)
  mul(add(X, add(X, one)), Y) = add(Y, mul(X, add(Y, Y)))
  mul(add(X, add(one, one)), Y) = add(Y, add(Y, mul(X, Y)))
  mul(add(X, mul(X, Y)), Z) = mul(X, add(Z, mul(Y, Z)))
  mul(add(X, mul(X, Y)), Z) -> mul(X, mul(Z, add(Y, one)))
  mul(add(X, mul(Y, X)), Z) = mul(X, add(Z, mul(Y, Z)))
  mul(add(X, mul(Y, X)), Z) -> mul(X, mul(Z, add(Y, one)))
  mul(add(X, add(X, Y)), Z) = mul(Z, add(mul(X, add(one, one)), Y))
  mul(add(X, add(X, Y)), Z) = mul(Z, add(mul(add(one, one), X), Y))
  mul(add(one, add(X, X)), Y) = add(Y, mul(X, add(Y, Y)))
  mul(add(one, add(one, X)), Y) = add(Y, add(Y, mul(X, Y)))
  mul(add(one, add(one, one)), X) = add(X, add(X, X))
  mul(add(one, mul(X, Y)), Z) = add(Z, mul(X, mul(Y, Z)))
  add(X, add(X, neg(mul(X, Y)))) = mul(X, add(one, add(one, neg(Y))))
  add(X, add(X, neg(mul(Y, X)))) = mul(X, add(one, add(one, neg(Y))))
  add(X, add(Y, add(Z, neg(X)))) -> add(Y, Z)
  add(X, add(Y, add(neg(X), Z))) -> add(Y, Z)
  add(X, add(Y, neg(mul(X, Z)))) = add(Y, mul(X, add(one, neg(Z))))
  add(X, add(Y, neg(mul(Z, X)))) = add(Y, mul(X, add(one, neg(Z))))
  add(X, add(Y, neg(add(X, Z)))) -> add(Y, neg(Z))
  add(X, add(Y, neg(add(Z, X)))) -> add(Y, neg(Z))
  add(X, add(neg(add(X, Y)), Z)) -> add(Z, neg(Y))
  add(X, add(neg(add(Y, X)), Z)) -> add(Z, neg(Y))
  add(X, add(neg(add(Y, Y)), X)) = mul(add(one, one), add(X, neg(Y)))
  add(X, neg(add(Y, add(Z, X)))) -> neg(add(Y, Z))
  add(X, neg(add(Y, add(X, Z)))) -> neg(add(Z, Y))
  add(X, neg(mul(X, add(Y, one)))) -> neg(mul(X, Y))
  add(X, neg(mul(X, add(one, Y)))) -> neg(mul(X, Y))
  add(X, neg(mul(Y, mul(X, Z)))) = mul(X, add(one, neg(mul(Y, Z))))
  add(X, neg(mul(add(X, X), Y))) = mul(X, add(one, neg(add(Y, Y))))
  add(X, neg(mul(add(Y, one), X))) -> neg(mul(X, Y))
  add(X, neg(mul(add(one, Y), X))) -> neg(mul(X, Y))
  add(X, mul(X, add(Y, neg(one)))) -> mul(X, Y)
  add(X, mul(X, add(neg(one), Y))) -> mul(X, Y)
  add(X, mul(Y, add(one, neg(Z)))) = add(Y, add(neg(mul(Z, Y)), X))
  add(X, mul(add(Y, neg(one)), X)) -> mul(X, Y)
  add(X, mul(add(neg(one), Y), X)) -> mul(X, Y)
  add(X, add(X, neg(add(Y, Y)))) = mul(add(one, one), add(neg(Y), X))
  add(X, add(X, neg(mul(Y, X)))) = mul(add(one, add(one, neg(Y))), X)
  add(X, add(neg(add(Y, Y)), X)) = mul(add(one, one), add(neg(Y), X))
  add(X, neg(mul(Y, add(X, X)))) = mul(add(one, neg(add(Y, Y))), X)
  add(X, neg(mul(Y, add(X, one)))) = add(neg(Y), mul(add(one, neg(Y)), X))
  add(X, mul(Y, add(one, neg(X)))) <-> add(Y, mul(X, add(one, neg(Y))))
  add(X, add(Y, neg(mul(Z, X)))) = add(Y, mul(add(one, neg(Z)), X))
  add(X, add(neg(mul(Y, X)), Z)) = add(Z, mul(add(one, neg(Y)), X))
  add(X, add(neg(mul(X, Y)), Z)) = add(Z, mul(X, add(one, neg(Y))))
  add(X, neg(mul(Y, mul(Z, X)))) = mul(add(one, neg(mul(Y, Z))), X)
  add(neg(X), add(Y, mul(X, Z))) = add(Y, mul(X, add(Z, neg(one))))
  add(neg(X), add(Y, mul(Z, X))) = add(Y, mul(X, add(Z, neg(one))))
  add(neg(X), add(Y, mul(X, Z))) = add(mul(X, add(Z, neg(one))), Y)
  add(neg(X), mul(X, add(Y, Z))) = mul(X, add(Y, add(neg(one), Z)))
  add(neg(X), mul(X, add(Y, one))) -> mul(X, Y)
  add(neg(X), mul(X, add(one, Y))) -> mul(X, Y)
  add(neg(X), mul(Y, add(X, one))) = add(Y, mul(X, add(neg(one), Y)))
  add(neg(X), mul(Y, add(one, X))) = add(Y, mul(X, add(Y, neg(one))))
  add(neg(X), mul(add(X, X), Y)) = mul(X, add(Y, add(Y, neg(one))))
  add(neg(X), mul(add(X, X), Y)) = mul(X, add(Y, add(neg(one), Y)))
  add(neg(X), mul(add(X, X), Y)) = mul(X, add(neg(one), add(Y, Y)))
  add(neg(X), mul(add(Y, Z), X)) = mul(X, add(Y, add(Z, neg(one))))
  add(neg(X), mul(add(Y, one), X)) -> mul(X, Y)
  add(neg(X), mul(add(one, X), Y)) = add(Y, mul(X, add(Y, neg(one))))
  add(neg(X), mul(add(one, Y), X)) -> mul(X, Y)
  add(neg(X), mul(Y, add(X, X))) = mul(add(Y, add(Y, neg(one))), X)
  add(neg(X), mul(X, add(Y, Z))) = mul(add(Y, add(Z, neg(one))), X)
  add(mul(X, Y), neg(add(X, X))) = mul(X, add(neg(add(one, one)), Y))
  add(mul(X, Y), neg(mul(X, Z))) -> mul(X, add(Y, neg(Z)))
  add(mul(X, Y), neg(mul(Z, X))) -> mul(X, add(Y, neg(Z)))
  add(mul(X, Y), neg(add(X, X))) = mul(add(Y, neg(add(one, one))), X)
  add(mul(X, Y), neg(mul(Y, Z))) -> mul(Y, add(X, neg(Z)))
  add(mul(X, Y), neg(mul(Z, Y))) -> mul(Y, add(X, neg(Z)))
  add(neg(add(X, X)), add(Y, Y)) = mul(add(one, one), add(Y, neg(X)))
  add(neg(mul(X, Y)), mul(X, Z)) -> mul(X, add(Z, neg(Y)))
  add(neg(mul(X, Y)), mul(Z, Y)) -> mul(Y, add(Z, neg(X)))
  add(neg(mul(X, Y)), mul(Y, Z)) -> mul(Y, add(Z, neg(X)))
  add(mul(X, add(one, neg(Y))), Z) = add(X, add(Z, neg(mul(X, Y))))
  add(mul(X, add(one, neg(Y))), Z) = add(X, add(Z, neg(mul(Y, X))))
  add(mul(X, add(one, neg(Y))), Z) = add(X, add(neg(mul(X, Y)), Z))
  add(mul(add(one, neg(X)), Y), Z) = add(Y, add(Z, neg(mul(X, Y))))
  add(mul(add(one, neg(X)), Y), Z) = add(Y, add(neg(mul(Y, X)), Z))
  mul(X, add(Y, add(Y, neg(one)))) = add(neg(X), mul(Y, add(X, X)))
  mul(X, add(Y, neg(add(one, one)))) = add(mul(Y, X), neg(add(X, X)))
  mul(X, add(Y, neg(add(one, one)))) = add(neg(add(X, X)), mul(X, Y))
  mul(X, add(Y, neg(mul(Y, Z)))) = mul(Y, mul(X, add(one, neg(Z))))
  mul(X, add(one, neg(add(Y, Y)))) = add(X, neg(mul(Y, add(X, X))))
  mul(X, add(one, neg(mul(Y, Z)))) = add(X, neg(mul(Y, mul(Z, X))))
  mul(X, add(neg(one), mul(Y, Z))) = add(neg(X), mul(Y, mul(X, Z)))
  mul(X, mul(Y, add(Z, neg(one)))) = mul(Y, add(neg(X), mul(X, Z)))
  mul(X, mul(Y, add(Z, neg(one)))) = mul(Y, add(neg(X), mul(Z, X)))
  mul(X, mul(Y, add(one, neg(Z)))) = mul(Y, add(X, neg(mul(Z, X))))
  mul(X, add(Y, neg(mul(Y, Z)))) = mul(Y, add(X, neg(mul(Z, X))))
  mul(X, add(Y, neg(mul(Y, Z)))) = mul(Y, add(X, neg(mul(X, Z))))
  mul(X, mul(Y, add(Z, neg(one)))) = mul(add(neg(one), Z), mul(X, Y))
  mul(add(X, one), add(X, neg(one))) -> add(neg(one), mul(X, X))
  mul(add(X, one), add(one, neg(X))) -> add(one, neg(mul(X, X)))
  mul(add(one, X), add(X, neg(one))) -> add(neg(one), mul(X, X))
  mul(add(one, X), add(one, neg(X))) -> add(one, neg(mul(X, X)))
  mul(add(one, X), add(neg(one), X)) -> add(neg(one), mul(X, X))
  mul(add(one, one), add(X, neg(Y))) = add(X, add(X, neg(add(Y, Y))))
  mul(add(one, one), add(neg(X), Y)) = add(neg(add(X, X)), add(Y, Y))
  mul(add(one, neg(X)), mul(Y, Z)) = mul(Y, add(Z, neg(mul(X, Z))))
  mul(add(X, neg(mul(X, Y))), Z) -> mul(X, mul(Z, add(one, neg(Y))))
  mul(add(neg(X), mul(X, Y)), Z) -> mul(X, mul(Z, add(neg(one), Y)))
  mul(add(neg(X), mul(Y, X)), Z) -> mul(X, mul(Z, add(neg(one), Y)))
  add(X, add(X, add(X, add(X, Y)))) = add(mul(X, add(one, add(one, add(one, one)))), Y)
  add(X, add(X, add(Y, add(Y, Z)))) = add(mul(add(X, Y), add(one, one)), Z)
  add(X, add(X, mul(X, add(Y, Y)))) -> mul(add(X, X), add(Y, one))
  add(X, add(X, mul(Y, add(one, one)))) -> mul(add(X, Y), add(one, one))
  add(X, add(X, mul(add(Y, Y), X))) -> mul(add(X, X), add(Y, one))
  add(X, add(X, mul(add(Y, Z), X))) = mul(X, add(one, add(Y, add(one, Z))))
  add(X, add(X, mul(add(one, one), Y))) -> mul(add(X, Y), add(one, one))
  add(X, add(Y, add(X, add(Z, Z)))) = add(Y, mul(add(X, Z), add(one, one)))
  add(X, add(Y, add(Z, mul(W, X)))) = add(Y, add(mul(X, add(W, one)), Z))
  add(X, add(Y, add(one, mul(X, Y)))) -> mul(add(X, one), add(Y, one))
  add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(X, add(Z, add(W, one))))
  add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(X, add(Z, add(one, W))))
  add(X, add(Y, mul(X, add(Z, W)))) = add(Y, mul(add(Z, add(W, one)), X))
  add(X, add(Y, mul(X, add(Z, W)))) = add(mul(X, add(Z, add(one, W))), Y)
  add(X, add(Y, mul(Z, add(X, X)))) = add(Y, mul(X, add(Z, add(Z, one))))
  add(X, add(Y, mul(Z, add(X, Y)))) -> mul(add(X, Y), add(Z, one))
  add(X, add(Y, mul(Z, add(W, one)))) = add(Z, add(X, add(Y, mul(W, Z))))
  add(X, add(Y, mul(Z, add(one, W)))) = add(Z, add(X, add(Y, mul(Z, W))))
  add(X, add(Y, mul(Z, add(one, one)))) = add(Z, add(Z, add(X, Y)))
  add(X, add(Y, mul(Z, mul(X, W)))) = add(Y, mul(X, add(one, mul(Z, W))))
  add(X, add(Y, mul(Z, mul(X, W)))) = add(Y, mul(add(one, mul(Z, W)), X))
  add(X, add(Y, mul(Z, mul(W, X)))) = add(Y, mul(X, add(one, mul(Z, W))))
  add(X, add(Y, mul(add(X, Y), Z))) -> mul(add(X, Y), add(Z, one))
  add(X, add(Y, mul(add(Z, W), X))) = add(Y, mul(X, add(Z, add(W, one))))
  add(X, add(Y, mul(add(Z, W), X))) = add(mul(X, add(Z, add(W, one))), Y)
  add(X, add(Y, mul(add(X, X), Z))) = add(mul(add(Z, add(Z, one)), X), Y)
  add(X, add(Y, mul(add(X, one), Z))) = add(Z, add(mul(X, add(Z, one)), Y))
  add(X, add(Y, mul(X, add(Z, W)))) = add(mul(add(Z, add(W, one)), X), Y)
  add(X, add(Y, mul(add(Z, one), W))) = add(W, add(X, add(mul(Z, W), Y)))
  add(X, add(Y, mul(add(one, Z), W))) = add(W, add(X, add(mul(W, Z), Y)))
  add(X, add(one, add(Y, mul(X, Y)))) -> mul(add(X, one), add(Y, one))
  add(X, add(mul(Y, add(one, Z)), W)) = add(Y, add(X, add(W, mul(Y, Z))))
  add(X, add(mul(Y, add(one, one)), X)) -> mul(add(X, Y), add(one, one))
  add(X, add(mul(Y, add(one, one)), Z)) = add(Y, add(Y, add(X, Z)))
  add(X, add(mul(Y, add(one, one)), Z)) = add(Y, add(Y, add(Z, X)))
  add(X, add(mul(add(one, one), Y), X)) -> mul(add(one, one), add(X, Y))
  add(X, add(mul(add(one, one), Y), Z)) = add(Y, add(Y, add(X, Z)))
  add(X, mul(X, add(Y, add(Y, one)))) -> mul(add(X, X), add(Y, one))
  add(X, mul(X, add(Y, add(Z, W)))) = mul(X, add(Y, add(Z, add(W, one))))
  add(X, mul(X, add(Y, add(Z, W)))) = mul(add(Y, add(Z, add(W, one))), X)
  add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(one, mul(Y, add(one, Z))))
  add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(one, mul(add(Z, one), Y)))
  add(X, mul(X, add(Y, mul(Y, Z)))) = mul(X, add(one, mul(add(one, Z), Y)))
  add(X, mul(X, add(Y, mul(Z, Y)))) = mul(X, add(one, mul(Y, add(Z, one))))
  add(X, mul(X, add(Y, mul(Z, Y)))) = mul(X, add(one, mul(add(Z, one), Y)))
  add(X, mul(X, add(one, add(Y, Y)))) -> mul(add(X, X), add(one, Y))
  add(X, mul(X, mul(Y, add(Z, one)))) = mul(X, add(one, add(Y, mul(Y, Z))))
  add(X, mul(X, mul(Y, add(Z, one)))) = mul(X, add(one, add(Y, mul(Z, Y))))
  add(X, mul(X, mul(Y, add(one, one)))) = mul(X, add(one, add(Y, Y)))
  add(X, mul(Y, add(X, add(Y, one)))) -> mul(add(X, Y), add(Y, one))
  add(X, mul(Y, add(X, add(one, Y)))) -> mul(add(X, Y), add(Y, one))
  add(X, mul(Y, add(X, add(one, one)))) = add(Y, add(Y, mul(X, add(Y, one))))
  add(X, mul(Y, add(X, mul(X, Z)))) = mul(X, add(one, mul(add(one, Z), Y)))
  add(X, mul(Y, add(X, mul(Z, X)))) = mul(X, add(Y, add(one, mul(Z, Y))))
  add(X, mul(Y, add(X, mul(Z, X)))) = mul(X, add(one, mul(add(Z, one), Y)))
  add(X, mul(Y, add(Y, add(X, one)))) -> mul(add(X, Y), add(Y, one))
  add(X, mul(Y, add(Z, add(Z, one)))) = add(Y, add(mul(Z, add(Y, Y)), X))
  add(X, mul(Y, add(Z, add(W, one)))) = add(Y, add(mul(Y, add(Z, W)), X))
  add(X, mul(Y, add(Z, add(one, one)))) = add(Y, add(Y, add(X, mul(Z, Y))))
  add(X, mul(Y, add(one, add(X, X)))) = add(Y, mul(X, add(Y, add(Y, one))))
  add(X, mul(Y, add(one, add(X, Y)))) -> mul(add(X, Y), add(Y, one))
  add(X, mul(Y, add(one, add(X, one)))) = add(Y, add(Y, mul(X, add(Y, one))))
  add(X, mul(Y, add(one, add(Y, X)))) -> mul(add(X, Y), add(Y, one))
  add(X, mul(Y, add(one, add(Z, Z)))) = add(Y, add(X, mul(Z, add(Y, Y))))
  add(X, mul(Y, add(one, add(Z, Z)))) = add(Y, add(X, mul(add(Y, Y), Z)))
  add(X, mul(Y, add(one, add(Z, one)))) = add(Y, add(Y, add(X, mul(Z, Y))))
  add(X, mul(Y, add(one, add(one, Z)))) = add(Y, add(X, add(Y, mul(Z, Y))))
  add(X, mul(Y, add(one, add(one, Z)))) = add(Y, add(Y, add(X, mul(Y, Z))))
  add(X, mul(Y, add(one, mul(Z, W)))) = add(Y, add(mul(Z, mul(Y, W)), X))
  add(X, mul(Y, mul(X, add(Z, Z)))) = mul(X, add(one, mul(Z, add(Y, Y))))
  add(X, mul(Y, mul(X, add(Z, Z)))) = mul(X, add(one, mul(add(Y, Y), Z)))
  add(X, mul(Y, mul(X, add(Z, one)))) = mul(X, add(Y, add(one, mul(Z, Y))))
  add(X, mul(Y, mul(X, add(Z, one)))) = mul(X, add(one, add(Y, mul(Z, Y))))
  add(X, mul(Y, mul(X, add(one, Z)))) = mul(X, add(Y, add(one, mul(Y, Z))))
  add(X, mul(Y, mul(X, add(one, Z)))) = mul(X, add(one, add(Y, mul(Y, Z))))
  add(X, mul(Y, mul(X, add(one, Z)))) = mul(X, add(one, add(Y, mul(Z, Y))))
  add(X, mul(Y, mul(Z, add(X, X)))) = mul(X, add(one, mul(Z, add(Y, Y))))
  add(X, mul(Y, mul(Z, add(one, one)))) = add(mul(Y, add(Z, Z)), X)
  add(X, mul(Y, mul(Z, add(one, one)))) = add(mul(Z, add(Y, Y)), X)
  add(X, mul(Y, mul(Z, mul(X, W)))) = mul(X, add(one, mul(Y, mul(Z, W))))
  add(X, mul(Y, mul(Z, mul(W, X)))) = mul(X, add(one, mul(Y, mul(Z, W))))
  add(X, mul(Y, mul(add(Z, Z), X))) = mul(X, add(one, mul(add(Y, Y), Z)))
  add(X, mul(Y, mul(add(Z, one), X))) = mul(X, add(one, add(Y, mul(Z, Y))))
  add(X, mul(Y, mul(add(one, Z), X))) = mul(X, add(Y, add(one, mul(Y, Z))))
  add(X, mul(Y, mul(add(one, Z), X))) = mul(X, add(one, add(Y, mul(Y, Z))))
  add(X, mul(Y, mul(add(one, Z), X))) = mul(X, add(one, add(Y, mul(Z, Y))))
  add(X, mul(Y, mul(add(one, one), Z))) = add(mul(Y, add(Z, Z)), X)
  add(X, mul(Y, mul(add(one, one), Z))) = add(mul(Z, add(Y, Y)), X)
  add(X, mul(add(X, X), mul(Y, Z))) = mul(X, add(one, mul(Y, add(Z, Z))))
  add(X, mul(add(X, X), mul(Y, Z))) = mul(X, add(one, mul(Z, add(Y, Y))))
  add(X, mul(add(X, Y), mul(Z, W))) = add(X, mul(Z, mul(W, add(Y, X))))
  add(X, mul(add(Y, Y), mul(X, Z))) = mul(X, add(one, mul(Y, add(Z, Z))))
  add(X, mul(add(Y, Y), mul(Z, X))) = mul(X, add(one, mul(Y, add(Z, Z))))
  add(X, mul(add(Y, Z), add(one, one))) = add(Y, add(Y, add(Z, add(Z, X))))
  add(X, mul(add(Y, one), mul(X, Z))) = mul(X, add(one, add(Z, mul(Y, Z))))
  add(X, mul(add(Y, one), mul(Z, X))) = mul(X, add(one, add(Z, mul(Y, Z))))
  add(X, mul(add(one, one), add(Y, Z))) = add(Z, add(X, add(Y, add(Y, Z))))
  add(X, mul(add(one, one), mul(X, Y))) = mul(X, add(one, add(Y, Y)))
  add(X, mul(add(one, one), mul(Y, X))) = mul(X, add(one, add(Y, Y)))
  add(X, mul(add(Y, add(Y, one)), X)) -> mul(add(X, X), add(Y, one))
  add(X, mul(add(Y, add(Z, W)), X)) = mul(X, add(Y, add(Z, add(W, one))))
  add(X, mul(add(Y, add(Z, W)), X)) = mul(X, add(Z, add(W, add(one, Y))))
  add(X, mul(add(one, add(X, one)), Y)) = add(Y, add(Y, mul(X, add(Y, one))))
  add(X, mul(add(one, add(Y, Y)), X)) -> mul(add(X, X), add(one, Y))
  add(X, add(Y, add(X, mul(Z, X)))) = add(Y, mul(add(Z, add(one, one)), X))
  add(X, add(Y, mul(Z, add(X, X)))) = add(Y, mul(add(Z, add(Z, one)), X))
  add(X, add(Y, mul(Z, add(X, one)))) = add(Y, add(Z, mul(X, add(Z, one))))
  add(X, add(X, mul(X, add(Y, Z)))) = mul(add(one, add(Y, add(Z, one))), X)
  add(X, mul(Y, add(Z, add(one, one)))) = add(mul(Z, Y), add(X, add(Y, Y)))
  add(X, mul(Y, add(Z, add(one, one)))) = add(mul(Y, Z), add(X, add(Y, Y)))
  add(X, mul(Y, add(X, mul(Z, X)))) = mul(add(one, mul(Y, add(Z, one))), X)
  add(X, mul(Y, add(X, mul(X, Z)))) = mul(add(one, mul(Y, add(one, Z))), X)
  add(X, mul(Y, add(one, mul(Z, X)))) <-> add(Y, mul(X, add(one, mul(Y, Z))))
  add(X, mul(Y, add(one, mul(X, Z)))) <-> add(Y, mul(X, add(one, mul(Y, Z))))
  add(X, mul(Y, mul(Z, add(X, X)))) = mul(add(one, mul(Z, add(Y, Y))), X)
  add(X, mul(Y, mul(Z, add(X, X)))) = mul(add(one, mul(Y, add(Z, Z))), X)
  add(X, mul(Y, mul(Z, add(X, X)))) = mul(add(one, mul(add(Y, Y), Z)), X)
  add(X, mul(Y, mul(X, add(Z, one)))) = mul(add(one, add(Y, mul(Y, Z))), X)
  add(X, mul(Y, mul(add(Z, one), X))) = mul(add(Y, add(one, mul(Z, Y))), X)
  add(X, mul(Y, mul(add(one, Z), X))) = mul(add(Y, add(one, mul(Y, Z))), X)
  add(one, add(X, add(Y, mul(X, Y)))) -> mul(add(X, one), add(Y, one))
  add(mul(X, Y), add(Z, mul(Y, W))) -> add(Z, mul(Y, add(X, W)))
  add(mul(X, Y), add(Z, mul(X, W))) -> add(Z, mul(X, add(Y, W)))
  add(mul(X, Y), add(Z, mul(W, Y))) -> add(Z, mul(Y, add(X, W)))
  add(mul(X, Y), add(mul(X, Y), Z)) -> add(Z, mul(add(X, X), Y))
  add(mul(X, Y), mul(Z, add(X, X))) = mul(X, add(Z, add(Z, Y)))
  add(mul(X, Y), mul(Z, add(X, X))) = mul(X, add(mul(Z, add(one, one)), Y))
  add(mul(X, Y), mul(Z, add(X, one))) = add(Z, mul(X, add(Z, Y)))
  add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Y, add(X, Z)), mul(Z, W))
  add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Y, add(Z, X)), mul(Z, W))
  add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(add(X, Z), Y), mul(Z, W))
  add(mul(X, Y), mul(Z, add(W, X))) = add(mul(X, add(Y, Z)), mul(W, Z))
  add(mul(X, Y), mul(Z, add(W, X))) = add(mul(X, add(Z, Y)), mul(Z, W))
  add(mul(X, Y), mul(Z, add(one, X))) = add(Z, mul(X, add(Z, Y)))
  add(mul(X, Y), mul(Z, add(one, Y))) = add(Z, mul(Y, add(X, Z)))
  add(mul(X, Y), mul(Z, mul(X, W))) -> mul(X, add(Y, mul(Z, W)))
  add(mul(X, Y), mul(Z, mul(W, X))) -> mul(X, add(Y, mul(Z, W)))
  add(mul(X, Y), mul(add(X, X), Z)) = mul(X, add(Y, add(Z, Z)))
  add(mul(X, Y), mul(add(X, X), Z)) = mul(X, add(Z, add(Z, Y)))
  add(mul(X, Y), mul(add(Z, one), X)) -> add(X, mul(X, add(Y, Z)))
  add(mul(X, Y), mul(add(Z, Z), X)) -> mul(X, add(Z, add(Z, Y)))
  add(mul(X, Y), mul(Z, mul(W, Y))) -> mul(Y, add(X, mul(Z, W)))
  add(mul(X, Y), mul(Z, mul(Y, W))) -> mul(Y, add(X, mul(Z, W)))
  add(mul(X, Y), add(Z, mul(W, X))) -> add(Z, mul(X, add(Y, W)))
  add(mul(X, Y), add(mul(Z, Y), W)) -> add(W, mul(Y, add(Z, X)))
  add(mul(X, Y), add(mul(X, Z), W)) -> add(W, mul(X, add(Y, Z)))
  add(mul(X, Y), add(mul(Y, Z), W)) -> add(W, mul(Y, add(Z, X)))
  add(mul(X, Y), mul(Z, add(W, Y))) = add(mul(W, Z), mul(Y, add(X, Z)))
  add(mul(X, Y), mul(Z, add(W, Y))) = add(mul(W, Z), mul(add(Z, X), Y))
  add(mul(X, Y), mul(Z, add(W, Y))) = add(mul(Z, W), mul(add(X, Z), Y))
  add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(X, add(Y, Z)))
  add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(add(Z, Y), X))
  add(mul(X, Y), mul(Z, add(W, X))) = add(mul(W, Z), mul(add(Y, Z), X))
  add(mul(X, Y), mul(Z, add(W, X))) = add(mul(add(Z, Y), X), mul(W, Z))
  add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(W, Z), mul(add(X, Z), Y))
  add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Z, W), mul(Y, add(Z, X)))
  add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Z, W), mul(add(Z, X), Y))
  add(mul(X, Y), mul(Z, add(Y, W))) = add(mul(Z, W), mul(add(X, Z), Y))
  add(mul(X, Y), mul(Z, add(X, W))) = add(mul(W, Z), mul(X, add(Y, Z)))
  add(mul(X, Y), mul(Z, add(X, W))) = add(mul(W, Z), mul(add(Z, Y), X))
  add(mul(X, Y), mul(Z, add(X, W))) = add(mul(Z, W), mul(X, add(Z, Y)))
  add(mul(X, Y), mul(add(Y, Z), W)) = add(mul(W, add(Y, Z)), mul(X, Y))
  add(mul(X, add(Y, Y)), mul(Z, X)) -> mul(X, add(Y, add(Y, Z)))
  add(mul(X, add(Y, Z)), mul(Z, W)) = add(mul(X, Y), mul(Z, add(X, W)))
  add(mul(X, add(Y, Z)), mul(Z, W)) = add(mul(Z, W), mul(add(Y, Z), X))
  add(mul(X, add(Y, Z)), mul(W, Y)) = add(mul(X, Z), mul(Y, add(X, W)))
  add(mul(X, add(Y, one)), add(Z, W)) = add(X, add(Z, add(mul(Y, X), W)))
  add(mul(X, add(Y, one)), add(Z, W)) = add(Z, add(X, add(W, mul(Y, X))))
  add(mul(X, add(Y, one)), mul(Y, Z)) = add(X, mul(Y, add(X, Z)))
  add(mul(X, add(Y, one)), mul(Z, X)) -> add(X, mul(X, add(Y, Z)))
  add(mul(X, add(one, Y)), add(Z, W)) = add(X, add(Z, add(mul(X, Y), W)))
  add(mul(X, add(one, Y)), mul(Z, X)) -> add(X, mul(X, add(Y, Z)))
  add(mul(X, add(one, one)), add(Y, Y)) -> mul(add(X, Y), add(one, one))
  add(mul(X, mul(Y, Z)), mul(W, Z)) -> mul(Z, add(W, mul(X, Y)))
  add(mul(X, mul(Y, Z)), mul(Y, W)) -> mul(Y, add(W, mul(X, Z)))
  add(mul(X, mul(Y, Z)), mul(Z, W)) -> mul(Z, add(W, mul(X, Y)))
  add(mul(X, add(Y, Y)), mul(Y, Z)) = mul(Y, add(Z, mul(add(one, one), X)))
  add(mul(X, add(Y, Z)), mul(W, Y)) = add(mul(Y, add(W, X)), mul(X, Z))
  add(mul(add(X, one), Y), mul(X, Z)) = add(Y, mul(X, add(Z, Y)))
  add(mul(add(X, one), Y), mul(Z, X)) = add(Y, mul(X, add(Y, Z)))
  add(mul(add(X, one), Y), mul(Z, X)) = add(Y, mul(add(Z, Y), X))
  add(mul(add(one, X), Y), mul(Z, X)) = add(Y, mul(X, add(Z, Y)))
  add(mul(add(one, one), X), add(Y, Y)) -> mul(add(one, one), add(X, Y))
  add(mul(X, add(Y, add(Y, one))), Z) = add(X, add(Z, mul(Y, add(X, X))))
  add(mul(X, add(Y, add(Y, one))), Z) = add(X, add(mul(add(X, X), Y), Z))
  add(mul(X, add(Y, add(Z, one))), W) = add(X, add(mul(X, add(Y, Z)), W))
  add(mul(X, add(Y, add(one, one))), Z) = add(X, add(Z, add(X, mul(Y, X))))
  add(mul(X, add(one, add(Y, Y))), Z) = add(X, add(Z, mul(Y, add(X, X))))
  add(mul(X, add(one, add(Y, Y))), Z) = add(X, add(Z, mul(add(X, X), Y)))
  add(mul(X, add(one, add(Y, one))), Z) = add(X, add(X, add(Z, mul(Y, X))))
  add(mul(X, add(one, add(Y, one))), Z) = add(X, add(Z, add(X, mul(Y, X))))
  add(mul(X, add(one, add(one, Y))), Z) = add(X, add(X, add(Z, mul(X, Y))))
  add(mul(X, add(one, add(one, Y))), Z) = add(X, add(Z, add(X, mul(Y, X))))
  add(mul(X, add(one, mul(Y, Z))), W) = add(X, add(W, mul(Y, mul(X, Z))))
  add(mul(X, mul(Y, add(one, one))), Z) = add(Z, mul(add(X, X), Y))
  add(mul(add(X, one), mul(Y, Z)), W) = add(W, mul(Z, mul(add(X, one), Y)))
  add(mul(add(one, one), mul(X, Y)), Z) = add(Z, mul(add(X, X), Y))
  mul(X, add(Y, add(Y, add(Y, one)))) = add(X, mul(Y, add(X, add(X, X))))
  mul(X, add(Y, add(Y, add(Z, Z)))) = mul(add(one, one), mul(X, add(Y, Z)))
  mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, mul(X, add(one, Z))))
  mul(X, add(Y, add(Z, add(one, W)))) = add(X, mul(X, add(Y, add(Z, W))))
  mul(X, add(Y, add(Z, add(one, one)))) = add(X, add(X, mul(X, add(Y, Z))))
  mul(X, add(Y, add(Z, add(one, one)))) = add(X, add(X, mul(add(Y, Z), X)))
  mul(X, add(Y, add(one, add(Z, one)))) = add(X, add(X, mul(add(Y, Z), X)))
  mul(X, add(Y, add(one, add(one, Z)))) = add(X, add(X, mul(X, add(Y, Z))))
  mul(X, add(Y, add(one, add(one, one)))) = add(X, add(X, add(X, mul(Y, X))))
  mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(X, Z))))
  mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(Z, X))))
  mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, mul(X, add(Z, one))))
  mul(X, add(Y, add(one, mul(Y, Z)))) = add(X, mul(Y, mul(add(Z, one), X)))
  mul(X, add(Y, mul(Y, add(Z, Z)))) = mul(Y, add(X, mul(add(X, X), Z)))
  mul(X, add(Y, mul(Y, add(Z, one)))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, add(Y, mul(Y, add(one, Z)))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, add(Y, mul(Y, add(one, one)))) = mul(add(X, add(X, X)), Y)
  mul(X, add(Y, mul(Z, add(one, one)))) = mul(add(Y, add(Z, Z)), X)
  mul(X, add(Y, mul(Z, add(one, one)))) = mul(add(Z, add(Z, Y)), X)
  mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(X, Y), mul(Z, add(X, X)))
  mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(X, Y), mul(add(X, X), Z))
  mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(Y, X), mul(Z, add(X, X)))
  mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(Y, X), mul(add(X, X), Z))
  mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(Z, add(X, X)), mul(X, Y))
  mul(X, add(Y, mul(Z, add(one, one)))) = add(mul(add(X, X), Z), mul(X, Y))
  mul(X, add(Y, mul(add(Z, Z), Y))) = mul(Y, add(X, mul(Z, add(X, X))))
  mul(X, add(Y, mul(add(Z, one), Y))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, add(Y, mul(add(one, Z), Y))) = mul(Y, add(X, add(X, mul(X, Z))))
  mul(X, add(Y, mul(add(one, Z), Y))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, add(Y, mul(add(one, one), Y))) = mul(Y, add(X, add(X, X)))
  mul(X, add(Y, mul(add(one, one), Y))) = mul(add(X, add(X, X)), Y)
  mul(X, add(Y, mul(add(one, one), Z))) = mul(add(Y, add(Z, Z)), X)
  mul(X, add(Y, mul(add(one, one), Z))) = mul(add(Z, add(Z, Y)), X)
  mul(X, add(Y, mul(add(one, one), Z))) = add(mul(X, Y), mul(Z, add(X, X)))
  mul(X, add(Y, mul(add(one, one), Z))) = add(mul(X, Y), mul(add(X, X), Z))
  mul(X, add(Y, mul(add(one, one), Z))) = add(mul(Y, X), mul(Z, add(X, X)))
  mul(X, add(Y, mul(add(one, one), Z))) = add(mul(Y, X), mul(add(X, X), Z))
  mul(X, add(Y, mul(add(one, one), Z))) = add(mul(add(X, X), Z), mul(X, Y))
  mul(X, add(one, add(Y, add(Y, Y)))) = add(X, mul(Y, add(X, add(X, X))))
  mul(X, add(one, add(Y, add(Z, one)))) = add(X, add(X, mul(add(Y, Z), X)))
  mul(X, add(one, add(Y, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(X, Z))))
  mul(X, add(one, add(Y, mul(Y, Z)))) = add(X, mul(Y, add(X, mul(Z, X))))
  mul(X, add(one, add(Y, mul(Z, Y)))) = add(X, mul(Y, add(X, mul(Z, X))))
  mul(X, add(one, add(one, add(one, Y)))) = add(X, add(X, add(X, mul(X, Y))))
  mul(X, add(one, add(one, add(one, Y)))) = add(X, add(X, add(X, mul(Y, X))))
  mul(X, add(one, add(one, mul(Y, Z)))) = add(X, add(X, mul(Y, mul(X, Z))))
  mul(X, add(one, add(one, mul(Y, Z)))) = add(X, add(X, mul(Y, mul(Z, X))))
  mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(X, mul(Z, add(Y, Y))))
  mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(X, mul(add(Y, Y), Z)))
  mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Y, mul(Z, add(X, X))))
  mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Y, mul(add(X, X), Z)))
  mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Z, mul(add(X, X), Y)))
  mul(X, add(one, mul(Y, add(Z, Z)))) = add(X, mul(Z, mul(add(Y, Y), X)))
  mul(X, add(one, mul(Y, add(Z, one)))) = add(X, mul(Y, add(X, mul(Z, X))))
  mul(X, add(one, mul(Y, add(one, Z)))) = add(X, mul(X, add(Y, mul(Z, Y))))
  mul(X, add(one, mul(Y, add(one, Z)))) = add(X, mul(Y, add(X, mul(X, Z))))
  mul(X, add(one, mul(Y, add(one, Z)))) = add(X, mul(Y, add(X, mul(Z, X))))
  mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(X, add(Y, Y)))
  mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(Y, add(X, X)))
  mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(add(X, X), Y))
  mul(X, add(one, mul(Y, add(one, one)))) = add(X, mul(add(Y, Y), X))
  mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(X, mul(Y, add(Z, Z))))
  mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(Y, mul(Z, add(X, X))))
  mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(Y, mul(add(X, X), Z)))
  mul(X, add(one, mul(add(Y, Y), Z))) = add(X, mul(add(X, X), mul(Y, Z)))
  mul(X, add(one, mul(add(Y, one), Z))) = add(X, mul(Z, add(X, mul(X, Y))))
  mul(X, add(one, mul(add(one, one), Y))) = add(X, mul(Y, add(X, X)))
  mul(X, add(one, mul(add(one, one), Y))) = add(X, mul(add(X, X), Y))
  mul(X, add(mul(Y, add(one, one)), Z)) = add(mul(Y, add(X, X)), mul(X, Z))
  mul(X, add(mul(Y, add(one, one)), Z)) = add(mul(add(X, X), Y), mul(X, Z))
  mul(X, add(mul(add(one, one), Y), Z)) = add(mul(add(X, X), Y), mul(X, Z))
  mul(X, mul(Y, add(Z, add(Z, Z)))) = mul(Z, mul(X, add(Y, add(Y, Y))))
  mul(X, mul(Y, add(Z, add(Z, one)))) = mul(Y, add(X, mul(Z, add(X, X))))
  mul(X, mul(Y, add(Z, add(W, one)))) = mul(Y, mul(add(Z, add(W, one)), X))
  mul(X, mul(Y, add(Z, add(one, one)))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, mul(Y, add(one, add(Z, Z)))) = mul(Y, add(X, mul(Z, add(X, X))))
  mul(X, mul(Y, add(one, add(Z, Z)))) = mul(Y, add(X, mul(add(X, X), Z)))
  mul(X, mul(Y, add(one, add(one, Z)))) = mul(Y, add(X, add(X, mul(X, Z))))
  mul(X, mul(Y, add(one, add(one, Z)))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, mul(Y, add(one, add(one, one)))) = mul(Y, add(X, add(X, X)))
  mul(X, mul(Y, add(one, add(one, one)))) = mul(add(X, add(X, X)), Y)
  mul(X, mul(Y, add(one, mul(Z, W)))) = mul(Y, add(X, mul(Z, mul(X, W))))
  mul(X, mul(Y, add(one, mul(Z, W)))) = mul(Y, add(X, mul(Z, mul(W, X))))
  mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(Y, mul(add(X, X), mul(Y, Z)))
  mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(Y, mul(add(X, X), mul(Z, Y)))
  mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(add(X, X), mul(Z, mul(Y, Y)))
  mul(X, mul(Y, mul(Z, add(one, one)))) = mul(Y, mul(add(X, X), Z))
  mul(X, mul(Y, mul(Z, add(W, W)))) = mul(Y, mul(W, mul(add(X, X), Z)))
  mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Y, mul(Z, mul(X, add(W, W))))
  mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Y, mul(Z, mul(W, add(X, X))))
  mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Y, mul(add(X, X), mul(Z, W)))
  mul(X, mul(Y, mul(add(one, one), Z))) = mul(Y, mul(add(X, X), Z))
  mul(X, mul(Y, mul(add(Z, Z), W))) = mul(add(X, X), mul(Z, mul(Y, W)))
  mul(X, mul(add(Y, Y), add(Z, one))) = mul(Y, mul(X, add(Z, add(one, add(Z, one)))))
  mul(X, mul(add(Y, Y), mul(Z, W))) = mul(add(X, X), mul(Z, mul(Y, W)))
  mul(X, mul(add(Y, add(Y, one)), Z)) = mul(Z, add(X, mul(Y, add(X, X))))
  mul(X, mul(add(Y, add(Y, one)), Z)) = mul(Z, add(X, mul(add(X, X), Y)))
  mul(X, mul(add(Y, add(one, one)), Z)) = mul(Z, add(X, add(X, mul(Y, X))))
  mul(X, mul(add(one, add(Y, one)), Z)) = mul(Z, add(X, add(X, mul(X, Y))))
  mul(X, mul(add(one, add(Y, one)), Z)) = mul(Z, add(X, add(X, mul(Y, X))))
  mul(X, mul(add(one, add(one, one)), Y)) = mul(Y, add(X, add(X, X)))
  mul(X, mul(add(one, add(one, one)), Y)) = mul(add(X, add(X, X)), Y)
  mul(X, mul(add(one, mul(Y, Z)), W)) = mul(W, add(X, mul(Y, mul(X, Z))))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, add(X, add(X, add(X, X))))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(add(one, one), add(X, X)))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(add(one, one), mul(Y, add(X, X)))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(add(one, one), mul(add(Y, Y), X))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(X, add(one, add(one, add(one, one)))))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(add(one, one), mul(X, add(one, one))))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(Y, mul(add(one, one), mul(add(one, one), X)))
  mul(X, add(Y, add(Y, add(Y, Y)))) = mul(add(one, one), mul(Y, mul(X, add(one, one))))
  mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, mul(X, add(Z, one))))
  mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, mul(add(Z, one), X)))
  mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, mul(X, add(Z, add(one, one))))
  mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, mul(add(Z, add(one, one)), X))
  mul(X, add(Y, add(Y, add(Z, Z)))) = mul(add(Z, Y), mul(X, add(one, one)))
  mul(X, add(Y, add(Y, add(Z, Z)))) = mul(add(Y, Z), mul(X, add(one, one)))
  mul(X, add(Y, add(Y, mul(Z, Y)))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, add(X, mul(Z, X))))
  mul(X, add(Y, add(Y, mul(Y, Z)))) = mul(Y, add(X, add(X, mul(X, Z))))
  mul(X, add(Y, mul(Z, add(Y, Y)))) = mul(Y, add(X, mul(Z, add(X, X))))
  mul(X, add(Y, mul(Y, add(Z, Z)))) = mul(Y, add(X, mul(Z, add(X, X))))
  mul(X, add(Y, mul(add(Z, Z), Y))) = mul(Y, add(X, mul(add(X, X), Z)))
  mul(X, add(Y, mul(add(Y, Y), Z))) = mul(Y, add(X, mul(add(X, X), Z)))
  mul(X, mul(Y, mul(Y, add(Z, Z)))) = mul(Y, mul(Y, mul(Z, add(X, X))))
  mul(X, mul(Y, mul(add(Z, one), W))) = mul(Y, mul(add(Z, one), mul(W, X)))
  mul(X, mul(add(Y, Y), add(Z, Z))) = mul(Y, mul(Z, add(X, add(X, add(X, X)))))
  mul(X, mul(add(Y, add(Y, Y)), Z)) = mul(Y, mul(add(X, add(X, X)), Z))
  mul(X, add(Y, mul(Z, mul(W, Y)))) = mul(Y, add(X, mul(Z, mul(X, W))))
  mul(X, add(Y, mul(Z, mul(Y, W)))) = mul(Y, add(X, mul(Z, mul(X, W))))
  mul(X, mul(Y, mul(Z, add(W, W)))) = mul(W, mul(Z, mul(add(X, X), Y)))
  mul(X, mul(Y, mul(Z, add(W, W)))) = mul(W, mul(Y, mul(Z, add(X, X))))
  mul(X, mul(Y, mul(Z, add(W, W)))) = mul(W, mul(Y, mul(add(X, X), Z)))
  mul(X, mul(Y, mul(Z, add(W, W)))) = mul(Y, mul(W, mul(Z, add(X, X))))
  mul(X, mul(Y, mul(add(Z, Z), W))) = mul(Z, mul(add(X, X), mul(Y, W)))
  mul(add(X, X), add(Y, add(Y, Y))) = mul(add(X, add(X, X)), add(Y, Y))
  mul(add(X, X), add(Y, mul(Y, Z))) -> mul(X, mul(add(Y, Y), add(one, Z)))
  mul(add(X, X), add(Y, mul(Z, Y))) -> mul(X, mul(add(Y, Y), add(Z, one)))
  mul(add(X, X), mul(Y, mul(Y, Z))) = mul(X, mul(Y, mul(Z, add(Y, Y))))
  mul(add(X, X), mul(Y, mul(Y, Z))) = mul(X, mul(Z, mul(Y, add(Y, Y))))
  mul(add(X, X), mul(Y, mul(Z, Y))) = mul(X, mul(Y, mul(Z, add(Y, Y))))
  mul(add(X, X), mul(Y, mul(Z, Y))) = mul(X, mul(Z, mul(Y, add(Y, Y))))
  mul(add(X, X), mul(Y, mul(Z, W))) = mul(X, mul(Y, mul(add(Z, Z), W)))
  mul(add(X, Y), mul(Z, add(one, one))) = mul(Z, add(X, add(Y, add(X, Y))))
  mul(add(X, Y), mul(Z, add(one, one))) = mul(Z, add(X, add(Y, add(Y, X))))
  mul(add(X, Y), mul(add(one, one), Z)) = mul(Z, add(X, add(X, add(Y, Y))))
  mul(add(X, X), add(Y, add(Y, Y))) = mul(add(Y, Y), add(X, add(X, X)))
  mul(add(one, one), add(X, mul(Y, Z))) = add(X, add(X, mul(Y, add(Z, Z))))
  mul(add(X, add(X, one)), mul(Y, Z)) = mul(Y, add(Z, mul(X, add(Z, Z))))
  mul(add(X, add(Y, one)), mul(Z, W)) = mul(Z, add(W, mul(W, add(X, Y))))
  mul(add(X, add(Y, one)), mul(Z, W)) = mul(Z, mul(W, add(X, add(Y, one))))
  mul(add(X, add(one, one)), mul(Y, Z)) = mul(Y, add(Z, add(Z, mul(X, Z))))
  mul(add(one, add(one, one)), mul(X, Y)) = mul(X, add(Y, add(Y, Y)))
  mul(add(one, add(one, one)), mul(X, Y)) = mul(add(X, add(X, X)), Y)
  mul(add(X, add(X, add(X, X))), Y) = mul(X, mul(add(one, one), add(Y, Y)))
  mul(add(X, add(X, add(X, X))), Y) = mul(add(one, one), mul(X, add(Y, Y)))
  mul(add(X, add(X, add(X, one))), Y) = add(Y, mul(X, add(Y, add(Y, Y))))
  mul(add(X, add(X, add(Y, Y))), Z) = mul(Z, mul(add(X, Y), add(one, one)))
  mul(add(X, add(X, add(Y, Y))), Z) -> mul(add(X, Y), mul(Z, add(one, one)))
  mul(add(X, add(X, mul(X, Y))), Z) -> mul(X, mul(Z, add(one, add(Y, one))))
  mul(add(X, add(X, mul(Y, X))), Z) = mul(X, add(Z, add(Z, mul(Z, Y))))
  mul(add(X, add(X, mul(Y, X))), Z) -> mul(X, mul(Z, add(Y, add(one, one))))
  mul(add(X, add(Y, add(one, one))), Z) = add(Z, add(Z, mul(Z, add(X, Y))))
  mul(add(X, add(Y, add(one, one))), Z) = add(Z, add(Z, mul(add(X, Y), Z)))
  mul(add(X, add(one, add(Y, one))), Z) = add(Z, add(Z, mul(Z, add(X, Y))))
  mul(add(X, add(one, add(one, one))), Y) = add(Y, add(Y, add(Y, mul(X, Y))))
  mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, add(Z, mul(Y, Z))))
  mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, add(Z, mul(Z, Y))))
  mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, mul(Z, add(Y, one))))
  mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, mul(Z, add(one, Y))))
  mul(add(X, add(one, mul(X, Y))), Z) = add(Z, mul(X, mul(add(Y, one), Z)))
  mul(add(X, add(one, mul(Y, X))), Z) = add(Z, mul(X, add(Z, mul(Z, Y))))
  mul(add(X, add(one, mul(Y, X))), Z) = add(Z, mul(X, mul(Z, add(Y, one))))
  mul(add(X, mul(Y, add(X, X))), Z) = mul(X, add(Z, mul(Y, add(Z, Z))))
  mul(add(X, mul(Y, add(X, X))), Z) = mul(X, add(Z, mul(Z, add(Y, Y))))
  mul(add(X, mul(Y, add(X, X))), Z) = mul(X, mul(Z, add(Y, add(Y, one))))
  mul(add(X, mul(Y, add(one, one))), Z) = mul(Z, add(X, add(Y, Y)))
  mul(add(X, mul(Y, add(one, one))), Z) = mul(Z, add(Y, add(Y, X)))
  mul(add(X, mul(Y, mul(X, Z))), W) -> mul(X, mul(W, add(one, mul(Y, Z))))
  mul(add(X, mul(Y, mul(Z, X))), W) -> mul(X, mul(add(one, mul(Y, Z)), W))
  mul(add(X, mul(add(X, X), Y)), Z) = mul(X, mul(Z, add(Y, add(Y, one))))
  mul(add(one, add(X, mul(X, Y))), Z) = add(Z, mul(X, mul(Z, add(one, Y))))
  mul(add(one, add(X, mul(Y, X))), Z) = add(Z, mul(X, mul(Z, add(Y, one))))
  mul(add(one, add(one, add(one, X))), Y) = add(Y, add(Y, add(Y, mul(X, Y))))
  mul(add(one, add(one, mul(X, Y))), Z) = add(Z, add(Z, mul(X, mul(Y, Z))))
  mul(add(one, mul(X, add(Y, Y))), Z) = add(Z, mul(Y, mul(Z, add(X, X))))
  mul(add(one, mul(X, add(Y, Y))), Z) = add(Z, mul(add(X, X), mul(Z, Y)))
  mul(add(one, mul(X, mul(Y, Z))), W) = add(W, mul(X, mul(Y, mul(W, Z))))
  mul(add(one, mul(add(X, X), Y)), Z) = add(Z, mul(X, mul(Z, add(Y, Y))))
  add(X, add(Y, add(Y, neg(add(X, X))))) -> add(Y, add(Y, neg(X)))
  add(X, add(Y, add(Z, add(neg(X), W)))) -> add(Y, add(Z, W))
  add(X, add(Y, mul(X, add(Y, neg(one))))) -> mul(add(X, one), Y)
  add(X, add(Y, mul(X, add(Z, neg(one))))) -> add(Y, mul(X, Z))
  add(X, add(Y, mul(X, add(neg(one), Z)))) -> add(Y, mul(X, Z))
  add(X, add(Y, add(Z, add(W, neg(X))))) -> add(Z, add(Y, W))
  add(X, add(neg(mul(X, add(one, one))), Y)) -> add(Y, neg(X))
  add(X, neg(mul(X, add(Y, add(Z, one))))) -> neg(mul(X, add(Y, Z)))
  add(X, neg(mul(X, add(Y, add(one, Z))))) -> neg(mul(X, add(Y, Z)))
  add(X, mul(X, add(Y, add(Y, neg(one))))) -> mul(add(X, X), Y)
  add(X, mul(X, add(Y, add(Z, neg(one))))) -> mul(X, add(Y, Z))
  add(X, mul(X, add(Y, add(neg(one), Y)))) -> mul(add(X, X), Y)
  add(X, mul(X, add(Y, add(neg(one), Z)))) -> mul(X, add(Y, Z))
  add(X, mul(X, add(Y, neg(add(Z, one))))) -> mul(X, add(Y, neg(Z)))
  add(X, mul(add(Y, add(Y, neg(one))), X)) -> mul(Y, add(X, X))
  add(X, mul(add(Y, add(Z, neg(one))), X)) -> mul(X, add(Y, Z))
  add(X, mul(add(Y, add(neg(one), Z)), X)) -> mul(X, add(Y, Z))
  add(one, neg(mul(add(X, one), add(X, one)))) -> neg(mul(X, add(X, add(one, one))))
  add(neg(X), add(Y, mul(X, add(Z, one)))) -> add(Y, mul(X, Z))
  add(neg(X), add(mul(X, add(Y, one)), Z)) -> add(Z, mul(X, Y))
  add(neg(X), mul(X, add(Y, add(Y, one)))) -> mul(Y, add(X, X))
  add(neg(X), mul(X, add(Y, add(Z, one)))) -> mul(X, add(Y, Z))
  add(neg(one), mul(add(X, one), add(X, one))) -> mul(X, add(one, add(one, X)))
  add(neg(mul(X, Y)), mul(add(one, Z), X)) -> add(X, mul(X, add(Z, neg(Y))))
  add(neg(mul(X, Y)), mul(add(Z, one), X)) -> add(X, mul(X, add(Z, neg(Y))))
  add(mul(X, add(one, one)), add(Y, neg(X))) -> add(X, Y)
  add(mul(X, add(one, one)), neg(add(X, X))) -> zero
  add(mul(X, Y), mul(Z, add(one, mul(X, Y)))) = add(Z, mul(X, mul(Y, add(Z, one))))
  add(mul(X, add(Y, Y)), mul(add(X, X), Z)) -> mul(add(X, X), add(Y, Z))
  add(mul(X, add(Y, Y)), mul(add(Z, Z), Y)) -> mul(add(one, one), mul(add(Z, X), Y))
  add(mul(X, add(Y, Y)), mul(Z, add(X, X))) -> mul(add(X, X), add(Z, Y))
  add(mul(X, add(Y, Y)), mul(Y, add(Z, Z))) -> mul(add(X, Z), add(Y, Y))
  add(X, mul(X, add(Y, neg(add(one, add(one, one)))))) = add(mul(X, Y), neg(add(X, X)))

RESULT: GaveUp (couldn't solve the problem).

Execution time: 24.883 seconds

